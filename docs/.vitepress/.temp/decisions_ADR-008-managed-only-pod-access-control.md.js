import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-008: Managed-Only Pod Access Control","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-008-managed-only-pod-access-control.md","filePath":"decisions/ADR-008-managed-only-pod-access-control.md"}');
const _sfc_main = { name: "decisions/ADR-008-managed-only-pod-access-control.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-008-managed-only-pod-access-control" tabindex="-1">ADR-008: Managed-Only Pod Access Control <a class="header-anchor" href="#adr-008-managed-only-pod-access-control" aria-label="Permalink to &quot;ADR-008: Managed-Only Pod Access Control&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-29</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The MCP server exposes Kubernetes pod management tools (<code>list_pods</code>, <code>get_pod</code>, <code>delete_pod</code>, <code>patch_pod</code>, <code>get_pod_logs</code>) that operate on <strong>all pods</strong> in a namespace. This creates two security and usability concerns:</p><ol><li><p><strong>Unintended access</strong>: Users can see, modify, and delete pods they did not create — including infrastructure pods (databases, ingress controllers, observability agents) that are critical to the cluster. A single misguided <code>delete_pod</code> call could take down shared infrastructure.</p></li><li><p><strong>Noisy listings</strong>: <code>list_pods</code> returns every pod in the namespace, making it hard for users (and agents) to find their own workspaces among dozens of system pods.</p></li></ol><p>The existing auth-based filtering (<code>nogoo9/user-sub</code> label) only scopes to the logged-in user&#39;s pods when <code>AUTH_ENABLED=true</code>, but it does not distinguish between pods managed by the MCP spawner and pods that happen to exist in the same namespace.</p><h3 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;Requirements&quot;">​</a></h3><ul><li>By default, MCP tools should only operate on pods that were created/managed by the MCP server itself.</li><li>The filter must be <strong>absolute</strong> — not even admin users should bypass it when enabled. This prevents accidental infrastructure damage.</li><li>Operators who need full namespace visibility can opt out by setting <code>MANAGED_ONLY=false</code>.</li><li><code>list_pods</code> should report how many unmanaged pods exist (count only, no details) for operational awareness.</li><li><code>create_pod</code> must auto-apply the managed-by label so new pods are immediately visible.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Introduce a <code>MANAGED_ONLY</code> configuration parameter (default: <code>true</code>) that gates all pod tools behind a <code>nogoo9/managed-by</code> label filter.</p><h3 id="filtering-behavior" tabindex="-1">Filtering behavior <a class="header-anchor" href="#filtering-behavior" aria-label="Permalink to &quot;Filtering behavior&quot;">​</a></h3><table tabindex="0"><thead><tr><th><code>MANAGED_ONLY</code></th><th>User role</th><th>Pods visible</th></tr></thead><tbody><tr><td><code>true</code></td><td>Any (including admin)</td><td>Only pods with <code>nogoo9/managed-by=nogoo9-spawner</code></td></tr><tr><td><code>false</code></td><td>Admin</td><td>All pods in namespace</td></tr><tr><td><code>false</code></td><td>Non-admin (auth on)</td><td>Own pods only (<code>nogoo9/user-sub</code> filter)</td></tr><tr><td><code>false</code></td><td>Any (auth off)</td><td>All pods in namespace</td></tr></tbody></table><h3 id="tool-level-changes" tabindex="-1">Tool-level changes <a class="header-anchor" href="#tool-level-changes" aria-label="Permalink to &quot;Tool-level changes&quot;">​</a></h3><ul><li><strong><code>list_pods</code></strong>: Appends <code>nogoo9/managed-by=nogoo9-spawner</code> to the label selector. Returns <code>unmanagedCount</code> in structured output (total pods minus managed pods).</li><li><strong><code>get_pod</code>, <code>delete_pod</code>, <code>patch_pod</code>, <code>get_pod_logs</code></strong>: After fetching the pod, verify it carries the managed-by label. Deny access with &quot;Pod not found or access denied&quot; if missing.</li><li><strong><code>create_pod</code></strong>: Auto-applies <code>nogoo9/managed-by=nogoo9-spawner</code> to labels. This ensures pods created via MCP tools are always visible under managed-only mode.</li><li><strong>Spawner tools</strong> (<code>spawn_workspace</code>, <code>stop_workspace</code>, <code>list_workspaces</code>, <code>get_workspace</code>): Already filter by <code>nogoo9/type=workspace</code> and apply <code>nogoo9/managed-by</code>. No changes needed.</li></ul><h3 id="ui-impact" tabindex="-1">UI impact <a class="header-anchor" href="#ui-impact" aria-label="Permalink to &quot;UI impact&quot;">​</a></h3><p>A new <code>get_capabilities</code> tool exposes <code>{ enabledTools, managedOnly, authEnabled, isAdmin }</code> to the embedded dashboard. The UI disables buttons for tools that are not in <code>enabledTools</code> (e.g., hides Delete button if <code>delete_pod</code> is not permitted by RBAC).</p><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="admin-bypass-when-managed-only-is-enabled" tabindex="-1">Admin Bypass When Managed-Only Is Enabled <a class="header-anchor" href="#admin-bypass-when-managed-only-is-enabled" aria-label="Permalink to &quot;Admin Bypass When Managed-Only Is Enabled&quot;">​</a></h3><ul><li><strong>Pros</strong>: Admins can debug any pod via the MCP dashboard.</li><li><strong>Cons</strong>: Defeats the purpose of the safety net. An admin accidentally deleting a database pod via the MCP UI would be catastrophic. Admins who need full access should use <code>kubectl</code> directly.</li><li><strong>Rejected</strong>: The managed-only gate is a safety mechanism, not a permissions mechanism. It should be absolute.</li></ul><h3 id="namespace-isolation-instead-of-label-filtering" tabindex="-1">Namespace Isolation Instead of Label Filtering <a class="header-anchor" href="#namespace-isolation-instead-of-label-filtering" aria-label="Permalink to &quot;Namespace Isolation Instead of Label Filtering&quot;">​</a></h3><ul><li><strong>Pros</strong>: Kubernetes-native approach; pods in separate namespaces can&#39;t interfere.</li><li><strong>Cons</strong>: Many deployments share a namespace (e.g., <code>default</code> or a team namespace). Requiring a dedicated namespace is an infrastructure burden.</li><li><strong>Rejected</strong>: Label-based filtering is more flexible and works within existing namespace layouts.</li></ul><h3 id="deny-create-for-unmanaged-pods" tabindex="-1">Deny Create for Unmanaged Pods <a class="header-anchor" href="#deny-create-for-unmanaged-pods" aria-label="Permalink to &quot;Deny Create for Unmanaged Pods&quot;">​</a></h3><ul><li><strong>Pros</strong>: Prevents any pod without the managed-by label from being created.</li><li><strong>Cons</strong>: Overly restrictive — the MCP <code>create_pod</code> tool is a general-purpose pod creation tool. Silently labeling is friendlier than rejecting.</li><li><strong>Rejected</strong>: Auto-labeling achieves the same effect without breaking the <code>create_pod</code> API contract.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Safer defaults</strong>: Out of the box, MCP tools cannot damage infrastructure pods.</li><li><strong>Operational awareness</strong>: <code>unmanagedCount</code> in <code>list_pods</code> tells operators there are other pods in the namespace without exposing details.</li><li><strong>No breaking change</strong>: Existing spawner workflows already apply the managed-by label. Only raw <code>create_pod</code> gains the auto-label behavior.</li><li><strong>Opt-out path</strong>: Operators who need full visibility set <code>MANAGED_ONLY=false</code> and accept the risk.</li><li><strong>UI adapts</strong>: The dashboard hides or disables actions that the server won&#39;t allow, preventing confusing error states.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-008-managed-only-pod-access-control.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR008ManagedOnlyPodAccessControl = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR008ManagedOnlyPodAccessControl as default
};
