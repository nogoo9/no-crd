import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-003: Peer Discovery for Session Key Sharing","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-003-peer-discovery-session-key.md","filePath":"decisions/ADR-003-peer-discovery-session-key.md"}');
const _sfc_main = { name: "decisions/ADR-003-peer-discovery-session-key.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-003-peer-discovery-for-session-key-sharing" tabindex="-1">ADR-003: Peer Discovery for Session Key Sharing <a class="header-anchor" href="#adr-003-peer-discovery-for-session-key-sharing" aria-label="Permalink to &quot;ADR-003: Peer Discovery for Session Key Sharing&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-28</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The stateless signed session cookie (ADR-002) requires all proxy replicas to share the same HMAC signing key. In production, operators set <code>PROXY_SESSION_SECRET</code> explicitly. However, for zero-config deployments and development, we need an automatic key sharing mechanism.</p><p>Constraints:</p><ul><li>The service account may <strong>not</strong> have RBAC permissions for Kubernetes Secrets</li><li>No external infrastructure (Redis, etcd) should be required</li><li>Must work for single-replica, multi-replica, and rolling-restart scenarios</li><li>Should use only permissions the pods already have</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Implement a <strong>peer discovery</strong> mechanism as step 3 in the key resolution cascade (after k8s Secret attempt, before in-memory fallback).</p><h3 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h3><ol><li>The server exposes <code>GET /internal/session-key</code> on the existing Fastify server</li><li>When a new pod starts and needs a key, it uses the existing <code>listNamespacedPod</code> RBAC to find sibling pods. The label selector is derived dynamically from the pod&#39;s own <code>app</code> label (falling back to <code>app=nogoo9-mcp</code> if introspection fails), so it works with custom deployment names.</li><li>Pods are sorted by <code>creationTimestamp</code> (then name for deterministic tiebreak). The <strong>oldest active pod</strong> is the leader.</li><li>If this pod <em>is</em> the oldest (or the only one), it generates a random key and serves it to future peers</li><li>If this pod is <em>not</em> the oldest, it queries each older peer&#39;s pod IP for the key via <code>GET http://&lt;podIP&gt;:&lt;port&gt;/internal/session-key</code></li><li>If any older peer responds, it adopts that key</li><li>If no older peer responds, it retries with a configurable delay (<code>PEER_DISCOVERY_DELAY_MS</code>, default 500ms) up to a configurable number of attempts (<code>PEER_DISCOVERY_RETRIES</code>, default 30)</li></ol><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h3><ul><li>The internal endpoint is <strong>cluster-internal only</strong> — not exposed via Ingress (Ingress rules only match <code>/mcp</code>, <code>/route</code>, etc.)</li><li>Requires <code>X-Nogoo9-Internal</code> header with a value derived from the pod&#39;s namespace (lightweight guard against accidental external access)</li><li>Excluded from standard auth middleware (pod-to-pod communication)</li></ul><h3 id="convergence" tabindex="-1">Convergence <a class="header-anchor" href="#convergence" aria-label="Permalink to &quot;Convergence&quot;">​</a></h3><ul><li><strong>Single pod restart</strong>: Queries older peers → adopts existing key → no disruption</li><li><strong>Full rollout</strong>: All pods restart simultaneously → oldest pod becomes leader → generates new key → others adopt → sessions invalidate (expected on deploy)</li><li><strong>Scale up</strong>: New pod queries existing older pods → adopts key immediately</li><li><strong>Leader failure</strong>: If the oldest pod dies, the next-oldest becomes the new leader and generates a key; surviving younger pods adopt it</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="kubernetes-secret-only" tabindex="-1">Kubernetes Secret only <a class="header-anchor" href="#kubernetes-secret-only" aria-label="Permalink to &quot;Kubernetes Secret only&quot;">​</a></h3><ul><li>Pros: Standard k8s pattern; persistent across restarts</li><li>Cons: Requires <code>secrets</code> RBAC which the service account may not have</li><li>Rejected as sole mechanism: Can&#39;t require additional RBAC. Kept as step 2 (best-effort).</li></ul><h3 id="leader-election-lease-based" tabindex="-1">Leader election (Lease-based) <a class="header-anchor" href="#leader-election-lease-based" aria-label="Permalink to &quot;Leader election (Lease-based)&quot;">​</a></h3><ul><li>Pros: Standard k8s pattern; deterministic leader</li><li>Cons: Complex to implement correctly; requires <code>leases</code> or <code>endpoints</code> RBAC; overkill for sharing a single secret</li><li>Rejected: Over-engineered for the problem. Peer query is simpler and sufficient.</li></ul><h3 id="redis-external-store" tabindex="-1">Redis / external store <a class="header-anchor" href="#redis-external-store" aria-label="Permalink to &quot;Redis / external store&quot;">​</a></h3><ul><li>Pros: Battle-tested; works across clusters</li><li>Cons: Adds infrastructure dependency; violates &quot;no external deps&quot; principle</li><li>Rejected: Contradicts the project&#39;s &quot;no CRDs, minimal dependencies&quot; philosophy</li></ul><h3 id="gossip-protocol-swim-etc" tabindex="-1">Gossip protocol (SWIM, etc.) <a class="header-anchor" href="#gossip-protocol-swim-etc" aria-label="Permalink to &quot;Gossip protocol (SWIM, etc.)&quot;">​</a></h3><ul><li>Pros: Decentralized; handles network partitions</li><li>Cons: Complex; requires background membership protocol; overkill</li><li>Rejected: We&#39;re sharing a single immutable value, not maintaining cluster state</li></ul><h3 id="in-memory-only-no-sharing" tabindex="-1">In-memory only (no sharing) <a class="header-anchor" href="#in-memory-only-no-sharing" aria-label="Permalink to &quot;In-memory only (no sharing)&quot;">​</a></h3><ul><li>Pros: Simplest possible approach; zero additional code</li><li>Cons: Multi-replica deployments get degraded behavior (sessions only valid on issuing replica)</li><li>Rejected as sole mechanism: Kept as final fallback (step 4), but peer discovery provides a better default for multi-replica.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>No new RBAC permissions required — uses existing pod-listing permission</li><li>Multi-replica deployments get automatic key sharing without explicit configuration</li><li>The internal endpoint adds one route to the Fastify server (minimal surface area)</li><li>If the leader pod dies and is replaced, the replacement queries surviving peers — seamless recovery</li><li>The 4-step cascade (env → k8s Secret → peer → in-memory) provides progressive fallback</li><li>Dynamic label selector derivation means the feature works with custom deployment names without configuration</li></ul><h2 id="amendments" tabindex="-1">Amendments <a class="header-anchor" href="#amendments" aria-label="Permalink to &quot;Amendments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>2026-06-13</td><td>Updated to document the actual deterministic leader-follower election protocol (oldest-pod-wins with creationTimestamp + name tiebreak). Added dynamic label selector derivation from pod&#39;s own <code>app</code> label. Added configurable retry parameters (<code>PEER_DISCOVERY_DELAY_MS</code>, <code>PEER_DISCOVERY_RETRIES</code>). Added leader failure convergence scenario. Fixed step numbering to align with ADR-002&#39;s corrected 4-step cascade.</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-003-peer-discovery-session-key.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR003PeerDiscoverySessionKey = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR003PeerDiscoverySessionKey as default
};
