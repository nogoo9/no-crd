import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-006: Packaged UI Asset and Built-in Resource Resolution","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-006-packaged-ui-asset-resolution.md","filePath":"decisions/ADR-006-packaged-ui-asset-resolution.md"}');
const _sfc_main = { name: "decisions/ADR-006-packaged-ui-asset-resolution.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-006-packaged-ui-asset-and-built-in-resource-resolution" tabindex="-1">ADR-006: Packaged UI Asset and Built-in Resource Resolution <a class="header-anchor" href="#adr-006-packaged-ui-asset-and-built-in-resource-resolution" aria-label="Permalink to &quot;ADR-006: Packaged UI Asset and Built-in Resource Resolution&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-28</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The project compiles TypeScript sources into a single bundled distribution folder (<code>dist/</code>), containing <code>dist/index.js</code>, <code>dist/server-entry.js</code>, <code>dist/cli.js</code>, etc. Additionally:</p><ul><li>The static frontend user interface compiles into <code>dist/ui/index.html</code>.</li><li>The documentation compiles into <code>dist/docs/</code>.</li><li>Built-in resources, such as themes (<code>themes/</code>) and templates (<code>templates/</code>), reside at the project root for local development and are packaged directly under the package root when published to NPM.</li><li>In Docker images, the build structure can be flattened (e.g. copying <code>dist/server-entry.js</code> directly to <code>/app/server-entry.js</code>, and copying assets like <code>themes/</code> and <code>templates/</code> directly to <code>/app/themes</code> and <code>/app/templates</code>).</li></ul><p>During a recent modularization refactor, <code>DIST_DIR</code> in <code>src/server/index.ts</code> was changed to resolve to <code>join(__dirname, &quot;..&quot;)</code> when running from compiled JS (<code>.js</code> files). Because compiled files live in the flat <code>dist/</code> directory, this pointed to the parent of <code>dist</code> (e.g. the package root), causing the server to look for <code>[root]/ui/index.html</code> (which does not exist) rather than <code>dist/ui/index.html</code>. This broke the Web UI in compiled/packaged runs and in Docker, serving the fallback string <code>&quot;UI not built. Run: bun run build&quot;</code>.</p><p>Additionally, the helper <code>resolveBuiltinDir</code> in <code>src/config.ts</code> was unable to resolve built-in directories (like <code>themes/</code> or <code>templates/</code>) when running from flattened layouts like Docker because it only checked parent directories (<code>join(__dirname, &quot;..&quot;)</code>).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Unify <code>DIST_DIR</code> Resolution</strong>: Update <code>DIST_DIR</code> in <code>src/server/index.ts</code> to resolve to <code>__dirname</code> when running from <code>.js</code> files. In standard compiled runs, this resolves to <code>dist/</code> (where <code>ui</code> and <code>docs</code> reside). In Docker container runs (with <code>/app/server-entry.js</code>), it resolves to <code>/app/</code> (where <code>ui</code> and <code>docs</code> reside).</li><li><strong>Harden <code>resolveBuiltinDir</code></strong>: Modify <code>resolveBuiltinDir</code> in <code>src/config.ts</code> to check <code>join(__dirname, dirName)</code> first before traversing up the directory tree. This enables the correct resolution of built-in assets in flattened container structures.</li><li><strong>Automate Package Bundling</strong>: Add the <code>&quot;prepublishOnly&quot;: &quot;bun run build&quot;</code> script hook in <code>package.json</code> to guarantee the UI and backend are built together and never published in a stale or incomplete state.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="retaining-the-join-dirname-logic-and-copying-ui-to-package-root" tabindex="-1">Retaining the <code>join(__dirname, &quot;..&quot;)</code> logic and copying UI to package root <a class="header-anchor" href="#retaining-the-join-dirname-logic-and-copying-ui-to-package-root" aria-label="Permalink to &quot;Retaining the \`join(__dirname, &quot;..&quot;)\` logic and copying UI to package root&quot;">​</a></h3><ul><li><strong>Pros</strong>: Matches directory structure of sources where UI is adjacent to the build parent directory.</li><li><strong>Cons</strong>: pollutes package root; requires custom copying steps in local build scripts; deviates from standard bundler conventions.</li><li><strong>Rejected</strong>: Building assets into <code>dist/</code> is the standard and clean approach.</li></ul><h3 id="relying-on-environment-variables-for-path-configuration" tabindex="-1">Relying on environment variables for path configuration <a class="header-anchor" href="#relying-on-environment-variables-for-path-configuration" aria-label="Permalink to &quot;Relying on environment variables for path configuration&quot;">​</a></h3><ul><li><strong>Pros</strong>: Gives maximum control to operators.</li><li><strong>Cons</strong>: Poor out-of-the-box user experience.</li><li><strong>Rejected</strong>: Out-of-the-box command execution should work seamlessly.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The Web UI loads correctly under Bun, Deno, Node.js, standard production runs, published npm packages, and Docker.</li><li>Built-in templates and themes load correctly in flattened Docker images.</li><li>Publishing workflow is safeguarded against missing static builds.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-006-packaged-ui-asset-resolution.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR006PackagedUiAssetResolution = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR006PackagedUiAssetResolution as default
};
