import { ssrRenderAttrs, ssrInterpolate } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-024: Non-blocking Workspace Template Upgrade & Multi-User Authorization","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-024-non-blocking-workspace-template-upgrade.md","filePath":"decisions/ADR-024-non-blocking-workspace-template-upgrade.md"}');
const _sfc_main = { name: "decisions/ADR-024-non-blocking-workspace-template-upgrade.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-024-non-blocking-workspace-template-upgrade-multi-user-authorization" tabindex="-1">ADR-024: Non-blocking Workspace Template Upgrade &amp; Multi-User Authorization <a class="header-anchor" href="#adr-024-non-blocking-workspace-template-upgrade-multi-user-authorization" aria-label="Permalink to &quot;ADR-024: Non-blocking Workspace Template Upgrade &amp; Multi-User Authorization&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-07-06 (Updated 2026-07-28)</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Upgrading workspace templates in Kubernetes was previously restricted by synchronous execution limits. Spawning a new pod, mounting volumes, pulling images, and verifying readiness can take anywhere from several seconds to minutes. This frequently exceeds standard HTTP request and Model Context Protocol (MCP) tool invocation timeouts (typically 30 seconds).</p><p>Furthermore, in multi-tenant environments with OIDC authentication and RBAC permissions enabled, upgrading workspaces introduces critical authorization, state preservation, and execution challenges:</p><ol><li><strong>ReadWriteOnce (RWO) PVC Constraints</strong>: A PersistentVolumeClaim with <code>ReadWriteOnce</code> access mode cannot be mounted by multiple pods concurrently. Doing a side-by-side upgrade (spawn new, wait for ready, delete old) would deadlock on volume locks because the new pod cannot start until the old pod releases the volume.</li><li><strong>Traffic Transition Routing</strong>: Terminal WebSocket connections must remain connected to the old pod until it is deleted, but new incoming HTTP requests must immediately route to the new pod once ready.</li><li><strong>Self-Healing/Orphan Cleanup</strong>: If the gateway restarts or the network fails during a transition, duplicate or orphaned pods must not linger in the cluster.</li><li><strong>Access Control &amp; RBAC Boundaries</strong>: <ul><li><code>upgrade_all_workspaces</code> touches all outdated workspaces cluster-wide across multiple users. Allowing arbitrary users to run bulk upgrades would allow low-privileged accounts to mutate other users&#39; pods.</li><li><code>upgrade_workspace</code> must enforce user isolation: normal users can only upgrade their own workspaces 1-by-1, while only administrators can upgrade workspaces owned by other users.</li></ul></li><li><strong>Owner Identity &amp; URL Stability</strong>: When an administrator upgrades another user&#39;s workspace (or when <code>upgrade_all_workspaces</code> is executed by an admin), the newly spawned pod must retain the <strong>original owner&#39;s sub/identity</strong> (<code>nogoo9/user-sub</code>) and routing URLs (<code>workspace-path</code>, <code>preview-path</code>, <code>workspace-port</code>). It must NOT mutate ownership to the administrator&#39;s identity.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Implement a non-blocking, background-driven template upgrade sequence with dynamic routing, automatic PVC fallback, strict RBAC authorization, owner identity preservation, and AsyncLocalStorage context propagation.</p><h3 id="_1-non-blocking-tool-execution" tabindex="-1">1. Non-blocking Tool Execution <a class="header-anchor" href="#_1-non-blocking-tool-execution" aria-label="Permalink to &quot;1. Non-blocking Tool Execution&quot;">​</a></h3><ul><li>The <code>upgrade_workspace</code> and <code>upgrade_all_workspaces</code> MCP tools initiate the upgrade asynchronously in a background Promise.</li><li>The tools return immediately with a status of <code>&quot;upgrading&quot;</code> and the newly generated tracking <code>podName</code>.</li></ul><h3 id="_2-dns-1123-compliant-unique-naming" tabindex="-1">2. DNS-1123 Compliant Unique Naming <a class="header-anchor" href="#_2-dns-1123-compliant-unique-naming" aria-label="Permalink to &quot;2. DNS-1123 Compliant Unique Naming&quot;">​</a></h3><ul><li>Upgraded pods are spawned under a unique name by appending a random suffix (e.g., <code>-up-abcde</code>).</li><li>Pod name prefix preserves the original owner&#39;s sub (<code>ws-{oldPodOwner}-{wsId}-up-XXXXX</code>).</li><li>The name is sliced dynamically to stay strictly under the 63-character limit enforced by Kubernetes.</li></ul><h3 id="_3-access-control-authorization-scoping" tabindex="-1">3. Access Control &amp; Authorization Scoping <a class="header-anchor" href="#_3-access-control-authorization-scoping" aria-label="Permalink to &quot;3. Access Control &amp; Authorization Scoping&quot;">​</a></h3><ul><li><strong><code>upgrade_all_workspaces</code></strong>: Strictly restricted to administrators (<code>capabilities.isAdmin</code> / <code>authCtx.isAdmin</code>). Rejects non-admin invocations with <code>Forbidden: Only admin users can upgrade all workspaces</code>.</li><li><strong><code>upgrade_workspace</code></strong>: Normal users can only upgrade their own workspaces (<code>nogoo9/user-sub</code> label match). Non-admin cross-user upgrades are rejected with <code>Access Denied</code>.</li><li><strong>UI Header Action Button</strong>: The &quot;Upgrade All Outdated&quot; button in the React UI header is conditionally rendered only for administrator accounts (<code>capabilities.isAdmin &amp;&amp; workspaces.some(...)</code>).</li></ul><h3 id="_4-owner-identity-routing-preservation" tabindex="-1">4. Owner Identity &amp; Routing Preservation <a class="header-anchor" href="#_4-owner-identity-routing-preservation" aria-label="Permalink to &quot;4. Owner Identity &amp; Routing Preservation&quot;">​</a></h3><ul><li>During single or bulk workspace upgrades, <code>upgradeWorkspaceInner</code> extracts the existing workspace owner identity (<code>oldPodOwner</code>) from <code>oldPod.metadata.labels[&#39;nogoo9/user-sub&#39;]</code> or annotations.</li><li>Newly spawned pods inherit <code>oldPodOwner</code> for labels (<code>nogoo9/user-sub</code>), annotations, template variable interpolation (<code>$${ssrInterpolate(_ctx.user)}</code>), and pod naming.</li><li>Workspace routing properties (<code>workspacePath</code>, <code>previewPath</code>, <code>workspacePort</code>, <code>workspaceName</code>) are preserved across the upgrade transition.</li></ul><h3 id="_5-asynclocalstorage-context-propagation" tabindex="-1">5. AsyncLocalStorage Context Propagation <a class="header-anchor" href="#_5-asynclocalstorage-context-propagation" aria-label="Permalink to &quot;5. AsyncLocalStorage Context Propagation&quot;">​</a></h3><ul><li>Wrap MCP transport execution (<code>transport.handleRequest</code>) with <code>requestContextStore.run({ jwtPayload: (request as any).jwtPayload }, ...)</code> in the Fastify MCP route handler (<code>/mcp</code>).</li><li>Ensures caller identity (<code>jwtPayload</code>) and administrative roles (<code>isAdmin</code>) remain accessible to tool handlers across asynchronous execution boundaries.</li></ul><h3 id="_6-rwo-pvc-detection-and-upgrade-fallback" tabindex="-1">6. RWO PVC Detection and Upgrade Fallback <a class="header-anchor" href="#_6-rwo-pvc-detection-and-upgrade-fallback" aria-label="Permalink to &quot;6. RWO PVC Detection and Upgrade Fallback&quot;">​</a></h3><ul><li>Check the old pod&#39;s volume specifications for PersistentVolumeClaims.</li><li><strong>PVC present</strong>: Fall back to a recreate-style upgrade (immediately delete the old pod to release volume locks, then spawn the new pod in the background).</li><li><strong>No PVC present</strong>: Run a zero-downtime side-by-side upgrade (spawn the new pod first with <code>-up-XXXXX</code> suffix, wait for readiness in the background, and delete the old pod once the new one is ready).</li></ul><h3 id="_7-dynamic-active-pod-routing" tabindex="-1">7. Dynamic Active Pod Routing <a class="header-anchor" href="#_7-dynamic-active-pod-routing" aria-label="Permalink to &quot;7. Dynamic Active Pod Routing&quot;">​</a></h3><ul><li>Update the HTTP proxy (<code>proxyPreHandler</code>) and WebSocket proxy (<code>handleUpgradeRequest</code>) to query all pods matching a workspace ID.</li><li>Sort matching pods by creation timestamp descending (newest first).</li><li>Route traffic to the first running/ready pod (having a phase of <code>Running</code> and a valid <code>podIP</code>), defaulting to the newest pod if none are ready.</li></ul><h3 id="_8-dynamic-check-and-list-reconciliation" tabindex="-1">8. Dynamic Check and List Reconciliation <a class="header-anchor" href="#_8-dynamic-check-and-list-reconciliation" aria-label="Permalink to &quot;8. Dynamic Check and List Reconciliation&quot;">​</a></h3><ul><li>Group listed pods by workspace ID.</li><li>If the newest pod is ready, trigger background deletion of any older duplicate pods.</li><li>If the newest pod is still transitioning (and not timed out), override the workspace status in the list to <code>&quot;Upgrading&quot;</code>.</li><li>If the newest pod has been pending/failing for over 10 minutes (600s): <ol><li>Extract failure details from container statuses (e.g. <code>ImagePullBackOff</code>).</li><li>Annotate the original pod with the error (<code>nogoo9/last-upgrade-error</code>).</li><li>Clean up and delete the failed new pod.</li></ol></li></ul><h3 id="_9-eager-startup-reconciliation" tabindex="-1">9. Eager Startup Reconciliation <a class="header-anchor" href="#_9-eager-startup-reconciliation" aria-label="Permalink to &quot;9. Eager Startup Reconciliation&quot;">​</a></h3><ul><li>Run a cluster-wide reconciliation scan (<code>reconcileUpgradingWorkspaces</code>) eagerly during HTTP/MCP server startup to sweep and resolve any orphaned/duplicate upgrade pods left behind by previous gateway restarts.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="_1-increase-request-timeouts" tabindex="-1">1. Increase Request Timeouts <a class="header-anchor" href="#_1-increase-request-timeouts" aria-label="Permalink to &quot;1. Increase Request Timeouts&quot;">​</a></h3><ul><li><strong>Rejected</strong>: Increasing tool timeouts blocks caller threads, violates MCP quick response specifications, and yields a poor client user experience.</li></ul><h3 id="_2-mandatory-recreate-style-upgrades-for-all-workspaces" tabindex="-1">2. Mandatory Recreate-Style Upgrades for All Workspaces <a class="header-anchor" href="#_2-mandatory-recreate-style-upgrades-for-all-workspaces" aria-label="Permalink to &quot;2. Mandatory Recreate-Style Upgrades for All Workspaces&quot;">​</a></h3><ul><li><strong>Rejected</strong>: Zero-downtime side-by-side upgrades provide a significantly better user experience for sandbox/stateless workspaces (which do not require persistent volumes).</li></ul><h3 id="_3-reassign-upgraded-pod-ownership-to-calling-admin" tabindex="-1">3. Reassign Upgraded Pod Ownership to Calling Admin <a class="header-anchor" href="#_3-reassign-upgraded-pod-ownership-to-calling-admin" aria-label="Permalink to &quot;3. Reassign Upgraded Pod Ownership to Calling Admin&quot;">​</a></h3><ul><li><strong>Rejected</strong>: Mutating workspace ownership to an admin user during maintenance upgrades breaks resource isolation, tenant visibility, and user workspace ownership.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Workspace template upgrades are completely non-blocking, timeout-safe, and RBAC-enforced.</li><li>Only administrators can perform bulk upgrades or cross-user upgrades.</li><li>Original workspace ownership (<code>nogoo9/user-sub</code>) and routing URLs (<code>workspacePath</code>/<code>previewPath</code>) are preserved across all upgrades.</li><li>Zero-downtime transitions are preserved where volume locks permit, with automatic fallback for RWO PVC volumes.</li><li>Gateway crashes during upgrade transitions are reconciled automatically upon server boot.</li><li>The multi-user E2E upgrade script (<code>scripts/test-e2e-upgrade.ts</code>) validates all aspects of the non-blocking upgrade lifecycle, access controls, PVC fallback, and state preservation.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-024-non-blocking-workspace-template-upgrade.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR024NonBlockingWorkspaceTemplateUpgrade = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR024NonBlockingWorkspaceTemplateUpgrade as default
};
