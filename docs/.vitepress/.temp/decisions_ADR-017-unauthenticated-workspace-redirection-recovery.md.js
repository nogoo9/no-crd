import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-017: Unauthenticated Workspace Redirection Recovery","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-017-unauthenticated-workspace-redirection-recovery.md","filePath":"decisions/ADR-017-unauthenticated-workspace-redirection-recovery.md"}');
const _sfc_main = { name: "decisions/ADR-017-unauthenticated-workspace-redirection-recovery.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-017-unauthenticated-workspace-redirection-recovery" tabindex="-1">ADR-017: Unauthenticated Workspace Redirection Recovery <a class="header-anchor" href="#adr-017-unauthenticated-workspace-redirection-recovery" aria-label="Permalink to &quot;ADR-017: Unauthenticated Workspace Redirection Recovery&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-10</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>When a user attempts to access a protected workspace route (e.g. <code>/route/workspace-1/index.html</code>) without a valid session, the gateway backend intercepts the request via the routing proxy auth guard (<code>requireRouteAuth</code>).</p><p>Following <a href="/no-crd/decisions/ADR-013-workspace-app-authorization.html">ADR-013</a>, the backend handles unauthenticated browser document requests (<code>Accept: text/html</code>) by redirecting the user&#39;s browser to the dashboard root index containing a <code>redirect_uri</code> parameter:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /nocr/?redirect_uri=%2Fnocr%2Froute%2Fworkspace-1%2F</span></span></code></pre></div><p>However, because the Single Sign-On (SSO) client OIDC flow requires strict callback registration at a single centralized origin endpoint (e.g., <code>http://localhost:8080/nocr/</code> or the root index <code>/</code>), the dashboard cannot dynamically register wildcards for every user&#39;s dynamically-generated workspace pod ID. When the dashboard triggers the PKCE redirect to Keycloak, the <code>redirect_uri</code> parameter in the current browser URL query is lost upon redirecting back to the OIDC redirect URI.</p><p>We need a secure, reliable, and same-origin compliant mechanism to preserve the user&#39;s target workspace destination across the OIDC authentication redirect flow, and then seamlessly restore their navigation state by redirecting them back to the workspace once the token exchange succeeds.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Extend the frontend UI configuration and OIDC hooks to preserve and recover the <code>redirect_uri</code> target:</p><ol><li><p><strong>Safe Redirect Validation (CWE-601 Prevention)</strong>: Implement a strict safety filter in the client to ensure the redirect destination is safe. A redirect destination is considered safe only if:</p><ul><li>It is a relative path starting with a single <code>/</code> and NOT <code>//</code> (to prevent protocol-relative open redirects).</li><li>Or, when parsed as a full URL, its origin matches <code>window.location.origin</code>.</li></ul></li><li><p><strong>Mount Hook Parameter Preservation</strong>: On load, the dashboard parses the URL query parameter <code>redirect_uri</code>. If present and validated as safe, it is stored in <code>sessionStorage</code> under <code>nocr_post_login_redirect_uri</code> so it survives the OIDC redirect to the Identity Provider. The query parameter is then stripped from the address bar via <code>window.history.replaceState</code> to maintain clean URLs.</p></li><li><p><strong>Active Token Immediate Redirection</strong>: If the user already has a valid, unexpired token in local storage upon landing on <code>/nocr/?redirect_uri=...</code>, the dashboard bypasses rendering the dashboard view entirely and redirects the browser immediately back to the target workspace (appending the token as a query parameter or hash fragment depending on configuration).</p></li><li><p><strong>Callback Token Exchange Recovery</strong>: Once the OIDC code exchange callback succeeds and retrieves the access token, the app checks <code>sessionStorage</code> for the stored <code>nocr_post_login_redirect_uri</code>. If found, it clears the item from storage and immediately redirects the user back to the workspace with the newly acquired token, bypassing the dashboard homepage.</p></li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="_1-dynamic-wildcard-redirect-uris-in-keycloak" tabindex="-1">1. Dynamic Wildcard Redirect URIs in Keycloak <a class="header-anchor" href="#_1-dynamic-wildcard-redirect-uris-in-keycloak" aria-label="Permalink to &quot;1. Dynamic Wildcard Redirect URIs in Keycloak&quot;">​</a></h3><ul><li><strong>Pros</strong>: Keycloak directly handles redirection back to the workspace.</li><li><strong>Cons</strong>: Severe security risk. Keycloak does not support dynamic wildcard paths for redirect URIs as it exposes the authorization code to theft via open-redirect endpoints on the domain.</li><li><strong>Rejected</strong>: Security protocols mandate fixed or strictly matching redirect callback patterns.</li></ul><h3 id="_2-preserving-parameter-in-oidc-state" tabindex="-1">2. Preserving Parameter in OIDC <code>state</code> <a class="header-anchor" href="#_2-preserving-parameter-in-oidc-state" aria-label="Permalink to &quot;2. Preserving Parameter in OIDC \`state\`&quot;">​</a></h3><ul><li><strong>Pros</strong>: Relies strictly on standard OAuth protocol fields.</li><li><strong>Cons</strong>: Some Identity Providers restrict the character set or length of the <code>state</code> parameter, making complex path parameters fragile. It also exposes navigation details to the authorization server.</li><li><strong>Rejected</strong>: Browser-side tab-scoped storage (<code>sessionStorage</code>) is cleaner and keeps internal routing state local to the client.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Seamless User Experience</strong>: Users accessing a deep link to a workspace pod are automatically redirected to login and return directly to their target workspace without seeing the dashboard interface or manually re-navigating.</li><li><strong>Tab Isolation</strong>: Using <code>sessionStorage</code> instead of <code>localStorage</code> ensures that if a user opens multiple workspaces in separate tabs concurrently, their post-login destinations do not collide.</li><li><strong>Zero-Day Open-Redirect Mitigation</strong>: Same-origin checks ensure external attackers cannot use the gateway&#39;s dashboard page to bounce users to external malicious domains.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-017-unauthenticated-workspace-redirection-recovery.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR017UnauthenticatedWorkspaceRedirectionRecovery = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR017UnauthenticatedWorkspaceRedirectionRecovery as default
};
