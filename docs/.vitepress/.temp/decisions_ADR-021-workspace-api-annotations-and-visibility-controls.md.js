import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-021: Workspace API Annotations and Routing Proxy Visibility Controls","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-021-workspace-api-annotations-and-visibility-controls.md","filePath":"decisions/ADR-021-workspace-api-annotations-and-visibility-controls.md"}');
const _sfc_main = { name: "decisions/ADR-021-workspace-api-annotations-and-visibility-controls.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-021-workspace-api-annotations-and-routing-proxy-visibility-controls" tabindex="-1">ADR-021: Workspace API Annotations and Routing Proxy Visibility Controls <a class="header-anchor" href="#adr-021-workspace-api-annotations-and-routing-proxy-visibility-controls" aria-label="Permalink to &quot;ADR-021: Workspace API Annotations and Routing Proxy Visibility Controls&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-11</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Workspace applications run within ephemeral pods and expose web interfaces and helper APIs. To allow secure OIDC-integrated routing without hardcoding specific endpoints (such as <code>/stats</code> or <code>/last-activity</code>) in the gateway routing proxy, we need a flexible, annotation-driven mechanism.</p><p>Specifically:</p><ul><li>Workspace pod templates must be able to declare additional HTTP or WebSocket endpoints.</li><li>Each custom API requires specification of its port, path, HTTP methods, refresh patterns, and visibility controls.</li><li>Visibility controls must specify precisely who is allowed to access the route. We need to support owner-only endpoints, internal authenticated endpoints, administrator-restricted endpoints, and specific scope/role requirements or lists of user subjects.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>We establish a unified annotation namespace and parsing model to register and secure workspace APIs:</p><h3 id="_1-api-annotation-keys" tabindex="-1">1. API Annotation Keys <a class="header-anchor" href="#_1-api-annotation-keys" aria-label="Permalink to &quot;1. API Annotation Keys&quot;">​</a></h3><p>Additional workspace APIs are declared on template or workspace pod metadata annotations using the format: <code>nogoo9/api.&lt;api-name&gt;.&lt;field&gt;</code></p><p>The fields are:</p><ul><li><code>port</code>: The internal container port to route traffic to (required to register the API).</li><li><code>path</code>: The routing path suffix matching incoming requests (defaults to <code>/</code>).</li><li><code>desc</code>: A short description of the endpoint for tool description and catalog discovery.</li><li><code>method</code>: Comma-separated HTTP methods supported by the API (e.g., <code>GET,POST</code>, defaults to <code>*</code> / all methods).</li><li><code>refresh</code>: Auto-refresh interval (e.g. <code>30s</code>, <code>1m</code>) or <code>init</code> to query once.</li><li><code>visibility</code>: Access control mode restricting routing proxy access.</li></ul><h3 id="_2-visibility-access-control-matrix" tabindex="-1">2. Visibility Access Control Matrix <a class="header-anchor" href="#_2-visibility-access-control-matrix" aria-label="Permalink to &quot;2. Visibility Access Control Matrix&quot;">​</a></h3><p>The routing proxy (<code>proxyPreHandler</code> in HTTP and WebSocket upgrade checks) matches requests to the most specific registered subpath and enforces visibility rules:</p><table tabindex="0"><thead><tr><th style="${ssrRenderStyle({ "text-align": "left" })}">Visibility Value</th><th style="${ssrRenderStyle({ "text-align": "left" })}">Access Check Logic</th><th style="${ssrRenderStyle({ "text-align": "left" })}">Use Case</th></tr></thead><tbody><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>private</code> (default)</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Allowed only if the caller&#39;s <code>userSub</code> matches the workspace&#39;s <code>nogoo9/user-sub</code> label/annotation.</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Workspace owner data, main application console, or terminal access.</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>internal</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Allowed for any authenticated caller.</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Generic status flags, health metrics, or shared tools.</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>admin</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Allowed if caller is the workspace owner, OR has the admin scope AND the admin role.</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Admin-restricted metrics, monitoring, or debug helpers.</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>scope:&lt;scope_name&gt;</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Allowed if caller has the specified scope in their token (verified via <code>hasRequiredScope</code>).</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Clients with specific API permissions (e.g. <code>scope:mcp:read</code>).</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>role:&lt;role_name&gt;</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Allowed if caller has the specified role in their token (verified via <code>hasRequiredRole</code>).</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Users with specific organizational roles.</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">Comma-separated list</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Allowed if caller is the workspace owner, OR their <code>userSub</code> is in the list.</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Specific shared users (e.g., <code>user-sub-1,user-sub-2</code>).</td></tr></tbody></table><h3 id="_3-reserved-api-defaults" tabindex="-1">3. Reserved API Defaults <a class="header-anchor" href="#_3-reserved-api-defaults" aria-label="Permalink to &quot;3. Reserved API Defaults&quot;">​</a></h3><p>For backwards compatibility and out-of-the-box security:</p><ul><li>Registered APIs named <code>stats</code> or <code>last_activity</code> (or <code>last-activity</code>) automatically default to <code>admin</code> visibility if no custom visibility annotation is specified.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Hardcoded Proxy Route Rules</strong>: Hardcoding endpoints like <code>/stats</code> directly in the proxy would lock down the proxy design and prevent dynamic apps from exposing custom administrative metrics or dashboard pages.</li><li><strong>Separate Ingress Resources</strong>: Declaring ingress resources dynamically for every sub-API adds cluster routing overhead and bypasses our OIDC session cookie authentication layer.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Improved Extensibility</strong>: Developer-oriented templates can easily declare auxiliary endpoints with fine-grained access limits.</li><li><strong>Consistent Authentication</strong>: Custom subpaths leverage the central gateway OIDC/JWT validations seamlessly.</li></ul><h2 id="amendments" tabindex="-1">Amendments <a class="header-anchor" href="#amendments" aria-label="Permalink to &quot;Amendments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>2026-06-13</td><td>Promoted from <code>Proposed</code> to <code>Accepted</code>. Core annotation parsing (<code>parseWorkspaceApis</code>) is implemented in <code>src/mcp/templates.ts</code> and the <code>WORKSPACE_AUTH_MODE</code> annotation key is defined in <code>src/config/annotations.ts</code>. Proxy visibility enforcement and API route matching are operational.</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-021-workspace-api-annotations-and-visibility-controls.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR021WorkspaceApiAnnotationsAndVisibilityControls = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR021WorkspaceApiAnnotationsAndVisibilityControls as default
};
