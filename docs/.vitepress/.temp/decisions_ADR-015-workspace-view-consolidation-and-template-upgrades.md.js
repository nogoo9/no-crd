import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-015: Workspace View Consolidation, Safe Template Upgrades, and STDIO Auth Bypass","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-015-workspace-view-consolidation-and-template-upgrades.md","filePath":"decisions/ADR-015-workspace-view-consolidation-and-template-upgrades.md"}');
const _sfc_main = { name: "decisions/ADR-015-workspace-view-consolidation-and-template-upgrades.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-015-workspace-view-consolidation-safe-template-upgrades-and-stdio-auth-bypass" tabindex="-1">ADR-015: Workspace View Consolidation, Safe Template Upgrades, and STDIO Auth Bypass <a class="header-anchor" href="#adr-015-workspace-view-consolidation-safe-template-upgrades-and-stdio-auth-bypass" aria-label="Permalink to &quot;ADR-015: Workspace View Consolidation, Safe Template Upgrades, and STDIO Auth Bypass&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-08</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>As the <code>@nogoo9/no-crd</code> project matured, several development and operational gaps emerged:</p><ol><li><strong>UX Redundancy</strong>: The frontend UI maintained a separate &quot;Pods&quot; list and &quot;Workspaces&quot; card deck. Because every workspace map 1-to-1 to a pod, this created visual duplication and confusing navigation hierarchy.</li><li><strong>Template Evolution</strong>: When template images or environment defaults changed, there was no safe mechanism to upgrade existing, running workspaces to use the latest template definition.</li><li><strong>Data Loss Risks during Upgrades</strong>: Deleting a pod to update its spec could result in the loss of PersistentVolumeClaim (PVC) attachments and custom environment variables configured during the initial spawn.</li><li><strong>Kubernetes Locking Conflicts</strong>: Spawning the upgraded pod before the old one is completely removed causes PVC multi-attach locks and naming collisions.</li><li><strong>Local Development and STDIO Auth Barriers</strong>: When <code>AUTH_ENABLED</code> is active in the environment, local developers trying to connect via STDIO transports (such as the MCP Inspector or Claude Desktop) faced authorization barriers because STDIO has no protocol-level HTTP headers to forward JWT payloads.</li><li><strong>Cross-Origin Discovery (CORS)</strong>: MCP clients querying the protected resource metadata endpoint from a different origin (like the MCP Inspector UI running on port 6274) encountered CORS blockages on <code>/.well-known/oauth-protected-resource</code> when the server is routed with a base path prefix.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Consolidated UI &amp; Tooling</strong>: <ul><li>Removed the separate Pods table from the UI.</li><li>Merged logs streaming, pod events retrieval, and upgrade triggers directly inside the Workspace card view.</li><li>Registered a new <code>get_workspace_events</code> MCP tool to pull pod lifecycle events into the UI.</li></ul></li><li><strong>Template Versioning and Detection</strong>: <ul><li>Introduced a <code>nogoo9/template-version</code> annotation (defaulting to <code>&quot;1.0.0&quot;</code> if unspecified) on pod templates and spawned workspace pods.</li><li>Modified <code>list_workspaces</code> and <code>get_workspace</code> tools to compare a pod&#39;s current version against the registry&#39;s latest template version (<code>current !== latest</code>).</li><li>Outdated workspaces are returned with <code>isOutdated: true</code> and warning badges in the UI.</li></ul></li><li><strong>Safe Workspace Upgrade Flow (<code>upgrade_workspace</code> &amp; <code>upgrade_all_workspaces</code>)</strong>: <ul><li><strong>PVC Preservation</strong>: Resolved and preserved the exact volume mounts and PersistentVolumeClaims associated with the workspace pod.</li><li><strong>Environment Variable Merging</strong>: Preserved all custom environment variables supplied during spawn, while appending any new template-level defaults.</li><li><strong>Safe Pod Lifecycle Handling</strong>: Initiated pod deletion and polled <code>readNamespacedPod</code> until it threw a <code>404 Not Found</code> before spawning the new pod. This guarantees volume attachments are unlocked.</li><li><strong>Failure Handling</strong>: If recreating fails, the workspace transitions to <code>Failed</code> status and displays the error log.</li></ul></li><li><strong>Automatic STDIO Transport Auth Bypass</strong>: <ul><li>Configured <code>config.auth.enabled</code> to return <code>false</code> dynamically if <code>TRANSPORT</code> is <code>&quot;stdio&quot;</code>. This allows local developer tools (like the MCP Inspector) to run commands natively without authentication payloads, while preserving security since STDIO is strictly isolated to the host process.</li></ul></li><li><strong>Root-Level OAuth Metadata &amp; CORS Resolution</strong>: <ul><li>Intercepted requests to <code>/.well-known/oauth-protected-resource</code> and its subpaths (like <code>/mcp</code>) globally in the <code>onRequest</code> Fastify hook.</li><li>Resolved correct resource parameters, set <code>Access-Control-Allow-Origin: *</code>, and returned the JSON metadata directly to avoid prefix routing or 404 CORS failures.</li></ul></li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="direct-re-creation-without-pod-deletion-awaiting" tabindex="-1">Direct Re-creation without Pod Deletion Awaiting <a class="header-anchor" href="#direct-re-creation-without-pod-deletion-awaiting" aria-label="Permalink to &quot;Direct Re-creation without Pod Deletion Awaiting&quot;">​</a></h3><ul><li><strong>Why rejected</strong>: Kubernetes handles PV/PVC mounts via nodes. If a new pod is created with the same PVC before the old pod&#39;s termination lifecycle is completely finalized, Kubernetes throws a <code>Multi-Attach error for volume</code> and blocks the new pod indefinitely.</li></ul><h3 id="global-jwt-auth-disabling-for-dev-command" tabindex="-1">Global JWT Auth Disabling for Dev Command <a class="header-anchor" href="#global-jwt-auth-disabling-for-dev-command" aria-label="Permalink to &quot;Global JWT Auth Disabling for Dev Command&quot;">​</a></h3><ul><li><strong>Why rejected</strong>: Developers need to run and test Keycloak token authorization in the local <code>k3d</code> cluster (with <code>AUTH_ENABLED=true</code>). Disabling it globally would mask authentication bugs. Restricting the bypass specifically to <code>TRANSPORT: stdio</code> isolates the bypass to secure local processes.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Improved UX</strong>: The frontend interface is cleaner, presenting all workspace lifecycle info (logs, events, updates) in one unified place.</li><li><strong>Safe Upgrades</strong>: Outdated templates can be upgraded in one click with zero loss of user data (PVC files) or customized environments.</li><li><strong>Frictionless Integration</strong>: The MCP server works seamlessly with the standard MCP Inspector and Claude Desktop locally, while retaining robust JWT token checks in staging/production deployments.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-015-workspace-view-consolidation-and-template-upgrades.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR015WorkspaceViewConsolidationAndTemplateUpgrades = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR015WorkspaceViewConsolidationAndTemplateUpgrades as default
};
