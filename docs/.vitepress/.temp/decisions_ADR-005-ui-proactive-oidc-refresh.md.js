import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-005: Session Cookie Coverage for All Endpoints","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-005-ui-proactive-oidc-refresh.md","filePath":"decisions/ADR-005-ui-proactive-oidc-refresh.md"}');
const _sfc_main = { name: "decisions/ADR-005-ui-proactive-oidc-refresh.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-005-session-cookie-coverage-for-all-endpoints" tabindex="-1">ADR-005: Session Cookie Coverage for All Endpoints <a class="header-anchor" href="#adr-005-session-cookie-coverage-for-all-endpoints" aria-label="Permalink to &quot;ADR-005: Session Cookie Coverage for All Endpoints&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-28</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>When <code>AUTH_ENABLED=true</code>, the embedded UI obtains an OIDC access token via the PKCE authorization code flow. This token is stored in <code>localStorage</code> and sent as a <code>Bearer</code> token on MCP calls and as the initial <code>?token=</code> parameter when opening workspace routes.</p><p><strong>Problem</strong>: Short-lived access tokens (e.g. Keycloak&#39;s 5-minute default) expire while the user is actively using the dashboard. This causes:</p><ul><li>MCP tool calls to return 401</li><li>New workspace route navigations to fail</li><li>The UI to redirect back to the OIDC login page mid-session</li></ul><p>The stateless session cookie (ADR-002) needs to cover all endpoints — not just proxy workspace routes — so that direct MCP API calls survive token expiry.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Scope the <code>nocr_sess</code> session cookie to <code>Path=/</code> (root). The server&#39;s global <code>preHandler</code> auth hook checks <code>nocr_sess</code> before falling back to JWT verification, and mints a root-scoped <code>nocr_sess</code> on any successful JWT authentication.</p><h3 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h3><ol><li>User authenticates via OIDC → UI sends <code>Bearer</code> token → server verifies JWT</li><li>On successful JWT verification, the server mints <code>nocr_sess</code> with <code>Path=/</code> and <code>HttpOnly</code></li><li>On subsequent requests, if no <code>Bearer</code> token is present, the server checks <code>nocr_sess</code></li><li>Valid session cookie → claims extracted → request proceeds without OIDC round-trip</li><li>Cookie uses sliding window (default 30 min, configurable via <code>PROXY_SESSION_TTL</code>)</li></ol><h3 id="security-properties" tabindex="-1">Security properties <a class="header-anchor" href="#security-properties" aria-label="Permalink to &quot;Security properties&quot;">​</a></h3><ul><li><strong>HttpOnly</strong>: Cookie is invisible to JavaScript — immune to XSS token theft</li><li><strong>No sensitive tokens in localStorage</strong>: Only the short-lived access token (which is already stored for initial auth) and id_token remain in <code>localStorage</code>. No <code>refresh_token</code> is stored client-side.</li><li><strong>SameSite=Lax</strong>: Protects against CSRF on state-changing requests</li><li><strong>HMAC-signed</strong>: Cannot be forged without the server&#39;s signing key</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="a-client-side-refresh-token-in-localstorage" tabindex="-1">A. Client-side <code>refresh_token</code> in localStorage <a class="header-anchor" href="#a-client-side-refresh-token-in-localstorage" aria-label="Permalink to &quot;A. Client-side \`refresh_token\` in localStorage&quot;">​</a></h3><ul><li>Uses the OIDC <code>refresh_token</code> grant to silently refresh the access token before expiry</li><li><strong>Security risk</strong>: <code>localStorage</code> is XSS-accessible; refresh tokens are long-lived credentials that allow minting unlimited access tokens</li><li><strong>OWASP/RFC guidance</strong>: OAuth 2.0 for Browser-Based Apps explicitly warns against storing refresh tokens in <code>localStorage</code></li><li>Rejected: The HttpOnly <code>nocr_sess</code> cookie achieves the same goal without storing any sensitive credential in JavaScript-accessible storage</li></ul><h3 id="b-hidden-iframe-with-prompt-none" tabindex="-1">B. Hidden iframe with <code>prompt=none</code> <a class="header-anchor" href="#b-hidden-iframe-with-prompt-none" aria-label="Permalink to &quot;B. Hidden iframe with \`prompt=none\`&quot;">​</a></h3><ul><li>Pros: Doesn&#39;t require refresh_token; uses existing SSO session cookie</li><li>Cons: Requires IDP to support <code>prompt=none</code>; doesn&#39;t work if third-party cookies are blocked (Safari, Firefox strict mode); complex error handling</li><li>Rejected: Unreliable across browsers</li></ul><h3 id="c-server-side-refresh-proxy-holds-refresh-token" tabindex="-1">C. Server-side refresh (proxy holds refresh_token) <a class="header-anchor" href="#c-server-side-refresh-proxy-holds-refresh-token" aria-label="Permalink to &quot;C. Server-side refresh (proxy holds refresh_token)&quot;">​</a></h3><ul><li>Pros: Fully transparent to the UI</li><li>Cons: Originally rejected due to concerns over server state and security risk of key reuse.</li><li><strong>Update (ADR-013)</strong>: Subsequently adopted via an encrypted client-side cookie (<code>nocr_refresh</code>). Encrypting the refresh token with AES-256-GCM using a key derived via HKDF-SHA256 from the session secret avoids server-side state (keeping the gateway stateless) and prevents JavaScript access, satisfying both security and stateless requirements.</li></ul><h3 id="d-redirect-on-401-reactive" tabindex="-1">D. Redirect on 401 (reactive) <a class="header-anchor" href="#d-redirect-on-401-reactive" aria-label="Permalink to &quot;D. Redirect on 401 (reactive)&quot;">​</a></h3><ul><li>Pros: No timer; simple</li><li>Cons: Disruptive — user is redirected mid-session; in-flight operations lost</li><li>Rejected: Poor user experience</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>All endpoints (MCP, proxy, permissions, themes) accept <code>nocr_sess</code> as an authentication mechanism</li><li>No <code>refresh_token</code> is stored anywhere in the browser</li><li>Session lifetime is controlled server-side (sliding window via <code>PROXY_SESSION_TTL</code>)</li><li>The cookie is automatically sent by the browser on all same-origin requests — no special client-side code needed</li><li>The UI code is simple — no refresh timers, no token rotation logic</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-005-ui-proactive-oidc-refresh.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR005UiProactiveOidcRefresh = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR005UiProactiveOidcRefresh as default
};
