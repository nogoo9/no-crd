import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-002: Stateless Signed Session Cookies for Proxy Auth","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-002-stateless-session-cookies.md","filePath":"decisions/ADR-002-stateless-session-cookies.md"}');
const _sfc_main = { name: "decisions/ADR-002-stateless-session-cookies.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-002-stateless-signed-session-cookies-for-proxy-auth" tabindex="-1">ADR-002: Stateless Signed Session Cookies for Proxy Auth <a class="header-anchor" href="#adr-002-stateless-signed-session-cookies-for-proxy-auth" aria-label="Permalink to &quot;ADR-002: Stateless Signed Session Cookies for Proxy Auth&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-28</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The nogoo9 proxy routes HTTP and WebSocket traffic to workspace pods. Authentication is done via JWT tokens (from OIDC providers like Keycloak). The token is:</p><ol><li>Passed as a <code>?token=</code> query parameter on the initial workspace URL</li><li>Set as an <code>HttpOnly</code> cookie (<code>nocr_token</code>) scoped to the workspace path</li></ol><p><strong>Problem</strong>: Short-lived access tokens (e.g. Keycloak default 5-minute expiry) cause cascading failures:</p><ul><li>Sub-resource loads (CSS, JS, images) from the proxied workspace get 401s when the baked-in cookie expires</li><li>WebSocket connections fail mid-session</li><li>The UI enters a clear-token → reload → re-login loop</li></ul><p>We need a mechanism to keep workspace sessions alive independently of the JWT lifetime.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Issue a <strong>stateless, HMAC-signed session cookie</strong> (<code>nocr_sess</code>) containing a minimal claim set (<code>{ sub, roles, iat, exp }</code>) with its own TTL (default 30 minutes, sliding window). The cookie is signed with HMAC-SHA256 using a shared secret.</p><h3 id="why-stateless-not-server-side-sessions" tabindex="-1">Why stateless (not server-side sessions) <a class="header-anchor" href="#why-stateless-not-server-side-sessions" aria-label="Permalink to &quot;Why stateless (not server-side sessions)&quot;">​</a></h3><ul><li><strong>No Redis/shared store needed</strong> — each replica verifies the cookie independently</li><li><strong>No garbage collection</strong> — cookies self-expire</li><li><strong>O(1) per-request cost</strong> — HMAC verify is constant time</li><li><strong>Horizontally scalable</strong> — replicas just need the same signing key</li></ul><h3 id="signing-key-resolution-4-step-cascade" tabindex="-1">Signing key resolution (4-step cascade) <a class="header-anchor" href="#signing-key-resolution-4-step-cascade" aria-label="Permalink to &quot;Signing key resolution (4-step cascade)&quot;">​</a></h3><ol><li><code>PROXY_SESSION_SECRET</code> or <code>JWT_SECRET</code> env var (resolved as a single config field <code>config.auth.sessionSecret</code>; explicit, recommended for production)</li><li>Best-effort k8s Secret (<code>nogoo9-session-key</code>) — attempts read, then create with 409-conflict retry for race safety</li><li>Peer discovery — deterministic leader election among sibling pods via internal endpoint (see <a href="./ADR-003-peer-discovery-session-key.html">ADR-003</a>)</li><li>In-memory random key (fallback for first/solo pod)</li></ol><h3 id="cookie-lifecycle" tabindex="-1">Cookie lifecycle <a class="header-anchor" href="#cookie-lifecycle" aria-label="Permalink to &quot;Cookie lifecycle&quot;">​</a></h3><ul><li><strong>Mint</strong>: On first proxy request with valid JWT → extract claims → HMAC-sign → set <code>nocr_sess</code> cookie</li><li><strong>Verify</strong>: On subsequent requests → decode → verify HMAC → check exp → allow if valid</li><li><strong>Slide</strong>: On each valid request → re-set cookie with fresh <code>Max-Age</code> (sliding window)</li><li><strong>Refresh</strong>: When UI refreshes its JWT, next proxy request mints a new cookie</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="a-silent-iframe-refresh-only" tabindex="-1">A. Silent iframe refresh only <a class="header-anchor" href="#a-silent-iframe-refresh-only" aria-label="Permalink to &quot;A. Silent iframe refresh only&quot;">​</a></h3><ul><li>Pros: Purely client-side, partially implemented in PKCE flow</li><li>Cons: Only works for the UI dashboard, not for proxied iframes or WebSockets; requires OIDC provider to support <code>prompt=none</code></li><li>Rejected: Doesn&#39;t solve the core problem for workspace sub-resources</li></ul><h3 id="b-server-side-opaque-session-in-memory-map" tabindex="-1">B. Server-side opaque session (in-memory Map) <a class="header-anchor" href="#b-server-side-opaque-session-in-memory-map" aria-label="Permalink to &quot;B. Server-side opaque session (in-memory Map)&quot;">​</a></h3><ul><li>Pros: Most robust; fully decouples proxy auth from JWT</li><li>Cons: Requires shared state for multi-replica (Redis); cleanup timers; memory usage</li><li>Rejected: Adds infrastructure dependency; stateless approach achieves the same goal</li></ul><h3 id="c-token-refresh-endpoint-server-side-refresh-token-grant" tabindex="-1">C. Token refresh endpoint (server-side refresh_token grant) <a class="header-anchor" href="#c-token-refresh-endpoint-server-side-refresh-token-grant" aria-label="Permalink to &quot;C. Token refresh endpoint (server-side refresh_token grant)&quot;">​</a></h3><ul><li>Pros: Transparent to the user</li><li>Cons: Requires storing refresh tokens server-side (security risk); some OIDC providers don&#39;t issue refresh tokens for public clients</li><li>Rejected: Security risk of server-side refresh token storage outweighs benefits</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Proxy auth has two paths: signed session cookie (fast, preferred) → JWT validation (fallback)</li><li>Cookie size is small (~200 bytes) since we only include minimal claims</li><li>All replicas need the same signing key — resolved automatically via the 4-step cascade</li><li>WebSocket upgrades use <code>nocr_sess</code> cookie instead of raw JWT</li><li>Existing <code>nocr_token</code> cookie is still set for backward compatibility</li><li>Session invalidation on deploy (all pods restart → new key) is acceptable behavior</li></ul><h2 id="amendments" tabindex="-1">Amendments <a class="header-anchor" href="#amendments" aria-label="Permalink to &quot;Amendments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>2026-06-13</td><td>Corrected &quot;5-step cascade&quot; to &quot;4-step cascade&quot;. <code>PROXY_SESSION_SECRET</code> and <code>JWT_SECRET</code> are merged into a single <code>config.auth.sessionSecret</code> field in the implementation, making them one resolution step. Added k8s Secret 409-conflict retry detail and cross-reference to ADR-003 for peer discovery.</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-002-stateless-session-cookies.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR002StatelessSessionCookies = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR002StatelessSessionCookies as default
};
