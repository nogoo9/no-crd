import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Local Sandbox (k3d & Keycloak)","description":"","frontmatter":{},"headers":[],"relativePath":"developer/local-sandbox.md","filePath":"developer/local-sandbox.md"}');
const _sfc_main = { name: "developer/local-sandbox.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="local-sandbox-k3d-keycloak" tabindex="-1">Local Sandbox (k3d &amp; Keycloak) <a class="header-anchor" href="#local-sandbox-k3d-keycloak" aria-label="Permalink to &quot;Local Sandbox (k3d &amp; Keycloak)&quot;">​</a></h1><p>This guide covers setting up a local development sandbox using <code>k3d</code> (a lightweight wrapper to run k3s in Docker) and a containerized Keycloak SSO instance to test OIDC authentication and reverse proxy routing.</p><hr><h2 id="☸️-1-bootstrapping-the-local-k3d-cluster" tabindex="-1">☸️ 1. Bootstrapping the local k3d cluster <a class="header-anchor" href="#☸️-1-bootstrapping-the-local-k3d-cluster" aria-label="Permalink to &quot;☸️ 1. Bootstrapping the local k3d cluster&quot;">​</a></h2><p>The repository includes helper scripts to provision a local cluster with a built-in container registry and Keycloak:</p><h3 id="🚀-automatic-setup" tabindex="-1">🚀 Automatic Setup <a class="header-anchor" href="#🚀-automatic-setup" aria-label="Permalink to &quot;🚀 Automatic Setup&quot;">​</a></h3><p>The easiest way to bootstrap the cluster is using the Moon task runner:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># Spin up cluster, local registry, and deploy manifests</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">moon</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> k3d:setup</span></span></code></pre></div><p>This runs the script located in [infra/k3d/bootstrap.sh](file:///home/eterna2/github/nogoo9-no-crd/infra/k3d/bootstrap.sh) which:</p><ol><li>Creates a local registry named <code>k3d-registry.localhost</code> on port <code>5001</code>.</li><li>Spins up a <code>k3d</code> cluster named <code>nogoo9</code>.</li><li>Deploys the namespace <code>nogoo9</code> and binds the required RBAC ClusterRoles/Roles.</li><li>Spins up a containerized Keycloak SSO service in the namespace <code>security</code>.</li></ol><hr><h2 id="🔑-2-testing-oidc-auth-locally" tabindex="-1">🔑 2. Testing OIDC Auth Locally <a class="header-anchor" href="#🔑-2-testing-oidc-auth-locally" aria-label="Permalink to &quot;🔑 2. Testing OIDC Auth Locally&quot;">​</a></h2><p>The local Keycloak instance is preconfigured with:</p><ul><li><strong>Issuer URL</strong>: <code>http://localhost:8080/auth/realms/nogoo9</code></li><li><strong>PKCE Client ID</strong>: <code>nogoo9-public-client</code></li><li><strong>Redirect URI</strong>: <code>http://localhost:3000/*</code> (matches the local MCP dashboard port)</li></ul><h3 id="default-test-credentials" tabindex="-1">Default Test Credentials <a class="header-anchor" href="#default-test-credentials" aria-label="Permalink to &quot;Default Test Credentials&quot;">​</a></h3><p>Use the following accounts to validate roles and access hierarchies inside the local dashboard:</p><table tabindex="0"><thead><tr><th>Username</th><th>Password</th><th>Assigned Roles</th></tr></thead><tbody><tr><td><code>adminuser</code></td><td><code>admin</code></td><td><code>admin</code></td></tr><tr><td><code>writeuser</code></td><td><code>user</code></td><td><code>user</code></td></tr><tr><td><code>readuser</code></td><td><code>viewer</code></td><td><code>viewer</code></td></tr></tbody></table><h3 id="testing-endpoint-access" tabindex="-1">Testing Endpoint Access <a class="header-anchor" href="#testing-endpoint-access" aria-label="Permalink to &quot;Testing Endpoint Access&quot;">​</a></h3><p>Verify SSO logins by accessing: <code>http://localhost:3000/</code></p><p>For detailed client scopes configuration and custom role-claim mapping, see the <a href="./../deploy/sso-identity.html">SSO &amp; OIDC Authentication Guide</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("developer/local-sandbox.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const localSandbox = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  localSandbox as default
};
