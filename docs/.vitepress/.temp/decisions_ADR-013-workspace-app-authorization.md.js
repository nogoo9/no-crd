import { ssrRenderAttrs, ssrRenderAttr, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/no-crd/workspace_auth_architecture.png";
const __pageData = JSON.parse('{"title":"ADR-013: Workspace App Authorization Support","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-013-workspace-app-authorization.md","filePath":"decisions/ADR-013-workspace-app-authorization.md"}');
const _sfc_main = { name: "decisions/ADR-013-workspace-app-authorization.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-013-workspace-app-authorization-support" tabindex="-1">ADR-013: Workspace App Authorization Support <a class="header-anchor" href="#adr-013-workspace-app-authorization-support" aria-label="Permalink to &quot;ADR-013: Workspace App Authorization Support&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-02</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><img${ssrRenderAttr("src", _imports_0)} alt="Workspace Auth Architecture"></p><p>The <code>@nogoo9/no-crd</code> gateway routes HTTP and WebSocket traffic to dynamically spawned workspace pods under the path <code>/route/:workspaceId/*</code>. When authentication is enabled (<code>AUTH_ENABLED=true</code>), the gateway intercepts requests and validates the user&#39;s session using cookies (<code>nocr_token</code> and <code>nocr_sess</code>).</p><p>Applications running inside these workspaces (both client-side frontends and containerized backends) frequently need the user&#39;s authenticated JSON Web Token (JWT) to call external APIs, databases, or other services.</p><p>However:</p><ol><li><strong>Cookie Scoping &amp; Security</strong>: The <code>nocr_token</code> cookie containing the JWT is marked <code>HttpOnly</code> for XSS protection. Frontend JavaScript running inside the workspace cannot read it.</li><li><strong>Diverse Application Architectures</strong>: Workspaces run a variety of applications: <ul><li>Backend servers (Node.js, Python, Go) that make server-to-server calls.</li><li>Frontend Single-Page Apps (SPAs) running in the browser tab.</li></ul></li><li><strong>Principle of Least Privilege</strong>: Injecting credentials indiscriminately into every spawned workspace poses a security risk. The system must restrict credential exposure to workspaces that explicitly declare a requirement for them.</li><li><strong>Short-Lived Access Tokens</strong>: OIDC providers issue short-lived access tokens (e.g. Keycloak&#39;s 5-minute default). When these expire mid-session, the user experiences disruptive page redirects or broken API calls. The gateway must handle token renewal transparently without interrupting the user&#39;s workflow.</li></ol><p>We need a secure, standardized, and highly compatible mechanism to deliver the user&#39;s token and profile to workspace applications — and to keep those tokens fresh without user disruption.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Implement three complementary, opt-in authentication delivery mechanisms, governed by a new pod template annotation <code>nogoo9/workspace-auth-mode</code>. Additionally, implement <strong>proxy-level transparent token refresh</strong> to silently renew expired tokens.</p><h3 id="_1-the-auth-mode-annotation-nogoo9-workspace-auth-mode" tabindex="-1">1. The Auth Mode Annotation (<code>nogoo9/workspace-auth-mode</code>) <a class="header-anchor" href="#_1-the-auth-mode-annotation-nogoo9-workspace-auth-mode" aria-label="Permalink to &quot;1. The Auth Mode Annotation (\`nogoo9/workspace-auth-mode\`)&quot;">​</a></h3><p>To prevent credential leaks, the gateway will inspect the target workspace pod&#39;s annotations. Authentication mechanisms are only active if explicitly listed in a comma-separated format in the pod annotations:</p><ul><li><code>inject-headers</code>: Enables backend header injection.</li><li><code>token-api</code>: Enables path-scoped token, authorize redirect, and refresh endpoints.</li></ul><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">metadata</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  annotations</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    nogoo9/workspace-auth-mode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;inject-headers,token-api&quot;</span></span></code></pre></div><hr><h3 id="_2-delivery-mechanisms" tabindex="-1">2. Delivery Mechanisms <a class="header-anchor" href="#_2-delivery-mechanisms" aria-label="Permalink to &quot;2. Delivery Mechanisms&quot;">​</a></h3><h4 id="a-header-injection-backend-proxy" tabindex="-1">A. Header Injection (Backend/Proxy) <a class="header-anchor" href="#a-header-injection-backend-proxy" aria-label="Permalink to &quot;A. Header Injection (Backend/Proxy)&quot;">​</a></h4><p>When <code>inject-headers</code> is active, the HTTP and WebSocket proxies will inject the following headers into the forwarded request to the workspace container:</p><table tabindex="0"><thead><tr><th>Header</th><th>Description</th><th>Source</th></tr></thead><tbody><tr><td><code>X-User-Sub</code></td><td>The user&#39;s subject/identity ID</td><td><code>jwtPayload.sub</code></td></tr><tr><td><code>X-User-Roles</code></td><td>Comma-separated list of user roles</td><td><code>jwtPayload.roles</code></td></tr><tr><td><code>X-Workspace-Jwt</code></td><td>The raw JWT token value</td><td>Request bearer/cookie token</td></tr><tr><td><code>Authorization</code></td><td>Standard Bearer token header</td><td><code>Bearer &lt;JWT&gt;</code></td></tr></tbody></table><p><em>This enables server-side applications in the pod to instantly know the user context and use the JWT for outbound API calls.</em></p><h4 id="b-token-redirect-apis-frontend-spa-tab" tabindex="-1">B. Token &amp; Redirect APIs (Frontend SPA/Tab) <a class="header-anchor" href="#b-token-redirect-apis-frontend-spa-tab" aria-label="Permalink to &quot;B. Token &amp; Redirect APIs (Frontend SPA/Tab)&quot;">​</a></h4><p>When <code>token-api</code> is active, the gateway exposes three endpoints relative to the workspace path:</p><ul><li><code>GET /route/:workspaceId/_auth/token</code>: Returns the active JWT as JSON <code>{&quot;token&quot;: &quot;&lt;JWT&gt;&quot;}</code>. Since it is path-scoped, the browser automatically attaches the <code>nocr_token</code> cookie to authenticate the fetch.</li><li><code>GET /route/:workspaceId/_auth/authorize?redirect_uri=...</code>: Redirects the user&#39;s browser back to <code>redirect_uri</code> with the JWT in the hash fragment (e.g. <code>redirect_uri#token=&lt;JWT&gt;</code>), mirroring standard OAuth2 Implicit Grant behavior. <ul><li><em>Security</em>: The Gateway validates that <code>redirect_uri</code> is same-origin with the host to prevent open-redirect vulnerabilities.</li></ul></li><li><code>POST /route/:workspaceId/_auth/refresh</code>: Performs a background token refresh using the encrypted <code>nocr_refresh</code> cookie. Returns <code>{&quot;token&quot;: &quot;&lt;new JWT&gt;&quot;}</code> on success or <code>401</code> if the refresh token is expired/revoked. This allows SPAs to explicitly trigger a refresh without a page redirect.</li></ul><hr><h3 id="_3-proxy-level-transparent-token-refresh" tabindex="-1">3. Proxy-Level Transparent Token Refresh <a class="header-anchor" href="#_3-proxy-level-transparent-token-refresh" aria-label="Permalink to &quot;3. Proxy-Level Transparent Token Refresh&quot;">​</a></h3><p>The core mechanism for handling short-lived access tokens. The gateway acts as a <strong>Backend-for-Frontend (BFF) token mediator</strong>, performing OIDC refresh token exchanges server-side — completely transparent to downstream workspace applications.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p><strong>Separation of Concerns</strong>:</p><ul><li><strong>Opt-In Delivery (1 &amp; 2)</strong>: Exposing credentials (headers/API) to the workspace container remains strictly opt-in per-pod to protect against credential leaks.</li><li><strong>Global Session Refresh (3)</strong>: Transparent proxy-level refresh is a global gateway security feature. It is active for all requests to ensure user ownership checks and gateway session validation continue to function seamlessly after token expiration, regardless of whether the workspace has opted into credential delivery.</li><li><strong>Explicit Refresh Endpoint</strong>: The <code>POST /_auth/refresh</code> endpoint is part of the <code>token-api</code> mode. It allows frontend SPAs to explicitly trigger a refresh and get the new token value directly in their JavaScript memory.</li></ul></div><h4 id="a-encrypted-refresh-cookie-nocr-refresh" tabindex="-1">A. Encrypted Refresh Cookie (<code>nocr_refresh</code>) <a class="header-anchor" href="#a-encrypted-refresh-cookie-nocr-refresh" aria-label="Permalink to &quot;A. Encrypted Refresh Cookie (\`nocr_refresh\`)&quot;">​</a></h4><p>On initial OIDC login, the dashboard UI sends the refresh token to the gateway via <code>POST /auth/set-refresh</code>. The gateway:</p><ol><li>Encrypts the refresh token using <strong>AES-256-GCM</strong> with a key derived via <strong>HMAC-SHA256</strong> from the session secret (same key cascade as ADR-002) with domain separation label <code>&quot;nocr_refresh_encryption&quot;</code> to avoid key reuse with the HMAC-signed <code>nocr_sess</code>.</li><li>Stores it as an HttpOnly cookie: <code>nocr_refresh=&lt;encrypted&gt;; Path=/; SameSite=Lax; HttpOnly; Max-Age=604800</code>.</li></ol><p>The refresh token <strong>never touches <code>localStorage</code></strong> or any JavaScript-accessible storage.</p><h4 id="b-transparent-auth-hook-refresh" tabindex="-1">B. Transparent Auth Hook Refresh <a class="header-anchor" href="#b-transparent-auth-hook-refresh" aria-label="Permalink to &quot;B. Transparent Auth Hook Refresh&quot;">​</a></h4><p>The gateway&#39;s global <code>preHandler</code> auth hook is extended with a refresh step. When a request arrives:</p><ol><li>Try <code>Bearer</code> token / <code>?token=</code> parameter → JWT verify → mint <code>nocr_sess</code></li><li>Try <code>nocr_sess</code> cookie → HMAC verify</li><li>Try <code>nocr_token</code> cookie → JWT verify → mint <code>nocr_sess</code></li><li><strong>Try <code>nocr_refresh</code> cookie</strong> → decrypt → call OIDC <code>token_endpoint</code> with <code>grant_type=refresh_token</code> → on success: <ul><li>Verify the new access token</li><li>Mint new <code>nocr_sess</code>, <code>nocr_token</code>, and <code>nocr_refresh</code> cookies (with rotated refresh token)</li><li>Set <code>request.jwtPayload</code> and <code>request.token</code> with the fresh values</li></ul></li><li>Fail → return <code>401</code> (or redirect for browser document requests — see Section D)</li></ol><p>By the time any downstream route handler (proxy, <code>_auth/token</code>, etc.) executes, <code>request.jwtPayload</code> and <code>request.token</code> are already populated with valid values. <strong>Downstream applications require zero code changes.</strong></p><h4 id="c-refresh-token-rotation" tabindex="-1">C. Refresh Token Rotation <a class="header-anchor" href="#c-refresh-token-rotation" aria-label="Permalink to &quot;C. Refresh Token Rotation&quot;">​</a></h4><p>Each successful refresh automatically rotates the refresh token: the new refresh token from the OIDC provider replaces the old <code>nocr_refresh</code> cookie. This follows the OAuth 2.0 Security Best Current Practice (RFC 9700) to limit the window of exposure for any single refresh token.</p><h4 id="d-graceful-degradation" tabindex="-1">D. Graceful Degradation <a class="header-anchor" href="#d-graceful-degradation" aria-label="Permalink to &quot;D. Graceful Degradation&quot;">​</a></h4><p>The system degrades gracefully across multiple failure modes:</p><table tabindex="0"><thead><tr><th>Failure Mode</th><th>Behavior</th></tr></thead><tbody><tr><td><strong>No refresh token</strong> (OIDC provider doesn&#39;t issue one)</td><td>Falls back to <code>nocr_sess</code> session cookie (30-min sliding window from ADR-002)</td></tr><tr><td><strong>Refresh token expired/revoked</strong></td><td>For browser document requests (<code>GET</code> + <code>Accept: text/html</code>), redirects to <code>/?redirect_uri=&lt;current_url&gt;</code> for re-authentication. For API requests, returns <code>401</code>.</td></tr><tr><td><strong>OIDC provider unreachable</strong></td><td>Falls back to <code>nocr_sess</code> if still valid; otherwise returns <code>401</code></td></tr><tr><td><strong>Refresh timeout</strong> (&gt; 5 seconds)</td><td>Aborts refresh, falls back to <code>nocr_sess</code> or <code>401</code></td></tr></tbody></table><hr><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="_1-storing-refresh-tokens-in-cookies-or-localstorage-client-side" tabindex="-1">1. Storing Refresh Tokens in Cookies or LocalStorage (Client-Side) <a class="header-anchor" href="#_1-storing-refresh-tokens-in-cookies-or-localstorage-client-side" aria-label="Permalink to &quot;1. Storing Refresh Tokens in Cookies or LocalStorage (Client-Side)&quot;">​</a></h3><ul><li><strong>Pros</strong>: Allowed workspaces to directly request token refreshes.</li><li><strong>Cons</strong>: Severe security risk. Exposing long-lived refresh credentials to frontend storage makes them vulnerable to exfiltration via XSS.</li><li><strong>Rejected</strong>: Followed <a href="/no-crd/decisions/ADR-005-ui-proactive-oidc-refresh.html">ADR-005</a> which mandates no client-side refresh token storage. The encrypted <code>nocr_refresh</code> HttpOnly cookie achieves the same goal without JavaScript access.</li></ul><h3 id="_2-a-single-global-auth-api" tabindex="-1">2. A Single Global Auth API <a class="header-anchor" href="#_2-a-single-global-auth-api" aria-label="Permalink to &quot;2. A Single Global Auth API&quot;">​</a></h3><ul><li><strong>Pros</strong>: Simpler implementation.</li><li><strong>Cons</strong>: Did not support backends (which don&#39;t run in the browser and cannot fetch same-origin cookies) or independent browser tabs securely without redirection flows.</li><li><strong>Rejected</strong>: A hybrid approach supporting both direct header injection and redirection-based endpoints covers all relevant integration models.</li></ul><h3 id="_3-silent-sso-via-hidden-iframe-prompt-none" tabindex="-1">3. Silent SSO via Hidden Iframe (<code>prompt=none</code>) <a class="header-anchor" href="#_3-silent-sso-via-hidden-iframe-prompt-none" aria-label="Permalink to &quot;3. Silent SSO via Hidden Iframe (\`prompt=none\`)&quot;">​</a></h3><ul><li><strong>Pros</strong>: Standard OIDC silent authentication without storing refresh tokens.</li><li><strong>Cons</strong>: Relies on third-party cookies, which are increasingly blocked by modern browsers (Safari ITP, Firefox strict mode, Chrome 3PCD). Requires OIDC provider to support <code>prompt=none</code>. Complex error handling.</li><li><strong>Rejected</strong>: Unreliable across browsers and increasingly non-functional. The proxy-level refresh is browser-independent.</li></ul><h3 id="_4-redirect-only-recovery-reactive" tabindex="-1">4. Redirect-Only Recovery (Reactive) <a class="header-anchor" href="#_4-redirect-only-recovery-reactive" aria-label="Permalink to &quot;4. Redirect-Only Recovery (Reactive)&quot;">​</a></h3><ul><li><strong>Pros</strong>: No timer; simple implementation.</li><li><strong>Cons</strong>: Disruptive — user is redirected mid-session; in-flight operations and unsaved state are lost.</li><li><strong>Rejected as primary mechanism</strong>: Demoted to last-resort fallback (Section 3.D) for when both access token and refresh token have expired. The proxy-level refresh handles the common case transparently.</li></ul><h3 id="_5-client-side-popup-authentication" tabindex="-1">5. Client-Side Popup Authentication <a class="header-anchor" href="#_5-client-side-popup-authentication" aria-label="Permalink to &quot;5. Client-Side Popup Authentication&quot;">​</a></h3><ul><li><strong>Pros</strong>: Preserves main window state; works when third-party cookies are blocked.</li><li><strong>Cons</strong>: Popups are blocked by browsers unless triggered by user gesture; complex cross-window postMessage coordination; requires downstream apps to implement popup handling.</li><li><strong>Rejected</strong>: The proxy-level approach is simpler and requires zero downstream changes.</li></ul><hr><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Workspaces must opt-in to authentication by declaring the <code>nogoo9/workspace-auth-mode</code> annotation.</li><li>Backends in pods receive identity contexts automatically via request headers with zero required library setup.</li><li>Frontends running in standalone tabs can retrieve the token via simple fetch or OAuth-compatible redirects.</li><li>Redirect flows are secured against open redirects by validating hosts at the gateway boundary.</li><li><strong>Token expiry is handled transparently</strong> at the proxy level — downstream workspace applications require zero auth-related code.</li><li>The gateway requires runtime network access to the OIDC provider&#39;s token endpoint for refresh exchanges.</li><li>Refresh tokens are encrypted at rest in cookies — never stored in localStorage, server memory, or databases.</li><li>The <code>offline_access</code> scope must be requested during OIDC authorization to receive refresh tokens.</li><li>Logout clears all three cookies (<code>nocr_token</code>, <code>nocr_sess</code>, <code>nocr_refresh</code>).</li></ul><h2 id="amendments" tabindex="-1">Amendments <a class="header-anchor" href="#amendments" aria-label="Permalink to &quot;Amendments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>2026-06-13</td><td>Corrected §3A: refresh token encryption key derivation uses <strong>HMAC-SHA256</strong> (not HKDF-SHA256). Updated domain separation label from <code>&quot;nocr_refresh&quot;</code> to <code>&quot;nocr_refresh_encryption&quot;</code> to match the implementation in <code>src/k8s/session.ts</code>.</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-013-workspace-app-authorization.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR013WorkspaceAppAuthorization = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR013WorkspaceAppAuthorization as default
};
