import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"What's New","description":"","frontmatter":{},"headers":[],"relativePath":"whats-new.md","filePath":"whats-new.md"}`);
const _sfc_main = { name: "whats-new.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-s-new" tabindex="-1">What&#39;s New <a class="header-anchor" href="#what-s-new" aria-label="Permalink to &quot;What&#39;s New&quot;">​</a></h1><p>Welcome to the release notes and update history for <code>@nogoo9/no-crd</code>. Here you&#39;ll find details of new features, enhancements, and bug fixes introduced in each version.</p><h2 id="what-s-new-in-v0-14-0" tabindex="-1">What&#39;s New in v0.14.0 <a class="header-anchor" href="#what-s-new-in-v0-14-0" aria-label="Permalink to &quot;What&#39;s New in v0.14.0&quot;">​</a></h2><ul><li><strong>Multi-User Upgrade Access Controls</strong>: Strict RBAC permissions are now enforced for template upgrades. The <code>upgrade_all_workspaces</code> tool is restricted exclusively to administrators (<code>Forbidden</code> for non-admin accounts), while normal users can only upgrade their own workspaces 1-by-1 (<code>Access Denied</code> for non-admin cross-user upgrades).</li><li><strong>UI Header Action Scoping</strong>: The &quot;Upgrade All Outdated&quot; header button on the React dashboard is now conditionally rendered only for administrator accounts (<code>capabilities.isAdmin</code>).</li><li><strong>Owner Identity &amp; Routing Preservation</strong>: Single and bulk workspace upgrades retain original workspace owner identities (<code>nogoo9/user-sub</code>) and routing URLs (<code>workspacePath</code>/<code>previewPath</code>). When an administrator upgrades another user&#39;s workspace, the newly spawned pod retains the original owner&#39;s sub (<code>oldPodOwner</code>) for labels, annotations, and pod name prefix (<code>ws-{oldPodOwner}-...</code>), preventing owner mutation to admin.</li><li><strong>AsyncLocalStorage Context Propagation</strong>: Context routing now preserves caller JWT identity and role payload across <code>/mcp</code> HTTP JSON-RPC tool dispatch bounds via <code>requestContextStore</code>.</li><li><strong>Multi-User E2E Upgrade Test Suite</strong>: Expanded E2E validation script (<code>scripts/test-e2e-upgrade.ts</code>) to test non-admin rejections, admin cross-user upgrades, owner retention, full upgrade lifecycle completion (old pod termination, upgraded env marker verification), and RWO PVC recreate-style upgrade fallback.</li></ul><h2 id="what-s-new-in-v0-13-0" tabindex="-1">What&#39;s New in v0.13.0 <a class="header-anchor" href="#what-s-new-in-v0-13-0" aria-label="Permalink to &quot;What&#39;s New in v0.13.0&quot;">​</a></h2><ul><li><strong>Template Version &amp; Outdated Display in UI</strong>: The React dashboard now displays the active template version of each workspace pod (e.g. <code>v1.0.0</code>) alongside the target template version (e.g. <code>v1.0.0 → v1.1.0</code>) when a template update is available, making version management visually clear.</li><li><strong>Upgrade Failure Display in UI</strong>: If a background upgrade fails or times out (exceeding the 10-minute threshold), the React dashboard renders an inline alert block detailing the upgrade error (e.g., <code>ImagePullBackOff</code> logs) on the workspace card and the specifications details panel, helping users troubleshoot issues immediately.</li><li><strong>Dynamic Get Workspace Status Alignment</strong>: Fixed an inconsistency where the <code>get_workspace</code> tool bypassed the upgrade transition reconciliation loop. It now correctly returns <code>&quot;Upgrading&quot;</code> status during active migrations and dynamically cleans up duplicate/stale upgrade pods.</li><li><strong>Upgrade Demo Tasks with Delay</strong>: Added <code>trigger-upgrade-demo</code> and <code>revert-upgrade-demo</code> Moon tasks which apply ConfigMap manifests with a <code>30s</code> <code>initContainer</code> delay, allowing developers to test and witness the upgrading transition state on the UI easily.</li></ul><h2 id="what-s-new-in-v0-12-0" tabindex="-1">What&#39;s New in v0.12.0 <a class="header-anchor" href="#what-s-new-in-v0-12-0" aria-label="Permalink to &quot;What&#39;s New in v0.12.0&quot;">​</a></h2><ul><li><strong>Non-Blocking Workspace Template Upgrades</strong> (<a href="/no-crd/decisions/ADR-024-non-blocking-workspace-template-upgrade.html">ADR-024</a>): Workspace template upgrades are now executed asynchronously in the background. Calling <code>upgrade_workspace</code> immediately returns an <code>&quot;upgrading&quot;</code> status with a tracking ID, preventing client/network timeouts when pulling large container images.</li><li><strong>RWO Volume Recreate Fallback</strong>: If a workspace utilizes Persistent Volume Claims (PVCs) with <code>ReadWriteOnce</code> (RWO) mode, the Spawner automatically falls back to a recreate-style upgrade flow. It deletes the old pod immediately to release the volume lock, ensuring the new pod can mount the volume without conflicts.</li><li><strong>Side-by-Side Upgrade Readiness</strong>: For standard (non-RWO) workspaces, upgrading pods spawn side-by-side. The routing proxy and WebSocket proxy dynamically select and route traffic to the newest running and ready pod, ensuring a zero-downtime cutover.</li><li><strong>Upgrade Failure Logs</strong>: Detailed diagnostic error logs are now captured and annotated onto the old pod under <code>nogoo9/last-upgrade-error</code> if a background upgrade fails or times out (10-minute limit), preserving the existing workspace replica instead of causing a total outage.</li><li><strong>Upgrades Documentation</strong>: Added a comprehensive guide explaining template versioning rules, upgrade workflows, cutover behavior, and failure reconciliation.</li></ul><h2 id="what-s-new-in-v0-11-1" tabindex="-1">What&#39;s New in v0.11.1 <a class="header-anchor" href="#what-s-new-in-v0-11-1" aria-label="Permalink to &quot;What&#39;s New in v0.11.1&quot;">​</a></h2><ul><li><strong>Cookie TTL Alignment</strong>: Session cookies now dynamically match their token&#39;s actual lifetime instead of using static defaults. The <code>nocr_token</code> cookie derives its <code>Max-Age</code> from the JWT <code>exp</code> claim, and <code>nocr_refresh</code> from the IdP&#39;s <code>refresh_expires_in</code> field. This prevents stale cookies from outliving expired tokens and causing repeated failed refresh attempts. Configurable fallbacks are available via <code>PROXY_TOKEN_COOKIE_TTL</code> (default 24h) and <code>PROXY_REFRESH_COOKIE_TTL</code> (default 7d).</li><li><strong>Stale Refresh Cookie Cleanup</strong>: When the Identity Provider rejects a refresh token (e.g. <code>invalid_grant</code>), the gateway immediately clears the <code>nocr_refresh</code> cookie rather than retrying on every subsequent request.</li><li><strong>Refresh Token Rotation Safety</strong>: The gateway now safely supports strict refresh token rotation (where the IdP invalidates the old token on each refresh) via a singleflight deduplication pattern. Concurrent browser requests that all need a refresh are coalesced into a single IdP round-trip, preventing race conditions where a second request would find its token already revoked.</li></ul><h2 id="what-s-new-in-v0-11-0" tabindex="-1">What&#39;s New in v0.11.0 <a class="header-anchor" href="#what-s-new-in-v0-11-0" aria-label="Permalink to &quot;What&#39;s New in v0.11.0&quot;">​</a></h2><ul><li><strong>Health Check Dependent on Session Key Resolution</strong> (<a href="/no-crd/decisions/ADR-022-session-key-health-readiness.html">ADR-022</a>): Implemented dependency checks for <code>/healthz</code> and <code>/mcp/healthz</code> health check endpoints. The gateway will now fail with <code>503 Service Unavailable</code> if the session signing key is not yet resolved. This prevents traffic from being routed to uninitialized replicas during multi-replica deployments.</li><li><strong>Proxy Keep-Alive Configuration</strong>: Added a runtime config option <code>PROXY_KEEP_ALIVE</code> (defaults to <code>true</code>) and attached a keep-alive <code>http.Agent</code> to the routing proxy when active, reducing TCP handshake overhead for downstream workspace resources.</li><li><strong>Startup Sequence Flowchart</strong>: Documented the service startup sequence with a detailed Mermaid flowchart showing the handshake, leader election, and readiness verification flow.</li><li><strong>E2E Peer Discovery Script</strong>: Added an automated peer discovery verification script (<code>scripts/e2e-peer-discovery.sh</code>) to test multi-replica leader/follower negotiation under concurrent boot.</li></ul><h2 id="what-s-new-in-v0-10-0" tabindex="-1">What&#39;s New in v0.10.0 <a class="header-anchor" href="#what-s-new-in-v0-10-0" aria-label="Permalink to &quot;What&#39;s New in v0.10.0&quot;">​</a></h2><ul><li><strong>Fine-Grained Permissions &amp; Scope Checks</strong> (<a href="/no-crd/decisions/ADR-020-fine-grained-roles-template-ownership-and-api-visibility.html">ADR-020</a>): Refactored the authorization engine to support granular permissions checking. Promoted standard Readers (<code>viewer</code> role) to allow starting and stopping their own sandboxes, while Writers (<code>user</code> role) can additionally create and edit templates. Bypassed scope validation for credentials that completely lack a scope claim.</li><li><strong>Template Creator Tracking &amp; Immutability</strong> (<a href="/no-crd/decisions/ADR-020-fine-grained-roles-template-ownership-and-api-visibility.html">ADR-020</a>): Stamped all dynamic ConfigMap templates with the creator&#39;s user subject (<code>nogoo9/user-sub</code>). Standard users can only update or delete templates created by them, while local filesystem-loaded templates are made fully immutable and protected against updates or deletions.</li><li><strong>Workspace API Routing &amp; Visibility Annotations</strong> (<a href="/no-crd/decisions/ADR-021-workspace-api-annotations-and-visibility-controls.html">ADR-021</a>): Created a dedicated specification for dynamic workspace sub-API annotations. Added support for <code>scope:&lt;scope_name&gt;</code> and <code>role:&lt;role_name&gt;</code> visibility checks, allowing developers to restrict access to subpaths to callers possessing specific OIDC scopes or roles.</li><li><strong>UI Workspace &amp; Template Permissions Adaptation</strong> (<a href="/no-crd/decisions/ADR-020-fine-grained-roles-template-ownership-and-api-visibility.html">ADR-020</a>): Enhanced the React dashboard to display creator metadata, conditionally enable template deletion (trash icon) and workspace stop/upgrade actions according to the authenticated user&#39;s permissions and roles.</li><li><strong>Admin Visibility Check Fix</strong>: Fixed a bug where the <code>allowed</code> status was not assigned in the proxy authentication handler (<code>auth.ts</code>) and WebSocket upgrade proxy (<code>ws-proxy.ts</code>) when API visibility was set to <code>&quot;admin&quot;</code>, resolving access issues for administrator accounts on restricted endpoints (such as <code>stats</code> or <code>last_activity</code>).</li></ul><h2 id="what-s-new-in-v0-9-0" tabindex="-1">What&#39;s New in v0.9.0 <a class="header-anchor" href="#what-s-new-in-v0-9-0" aria-label="Permalink to &quot;What&#39;s New in v0.9.0&quot;">​</a></h2><ul><li><strong>Interactive Popup SSO Renewal &amp; Session Expiry Warnings</strong>: Added a top-level session expiration warning banner prompting users to renew their access session when expiring and OIDC refresh tokens are unavailable/disabled on the Identity Provider. Renewals run inside an auto-closing popup window triggered via voluntary user interaction to satisfy browser security bounds.</li><li><strong>Auto/Manual Refresh Dashboard Control</strong>: Added a premium segmented dashboard toggle enabling users to choose between automated data polling (every 5 seconds) and manually triggered data refresh, persisting preferences in <code>localStorage</code>.</li><li><strong>Maximize &amp; Reload Control for Inline Workspaces</strong>: Enhanced workspace preview modals with layout toggle options to maximize/restore view width and a toolbar reload option to quickly reload application frames.</li><li><strong>Keycloak Hostname and Path Synchronization</strong>: Integrated the <code>KC_HOSTNAME</code> environment variable mapping set to <code>http://localhost:8080/auth</code> to enforce unified issuer URLs (<code>http://localhost:8080/auth/realms/nogoo9</code>) for both external login pathways and backchannel cluster-to-cluster token refresh queries, resolving OIDC path stripping and <code>invalid_grant</code> errors.</li><li><strong>Dynamic Path-Scoped Cookie Routing</strong>: Extended cookie prefix path resolution to incorporate dynamic base-url subpaths (<code>Path=/nocr/</code>), preventing cookie visibility leakage or browser session collision across Dynamic Workspace views.</li><li><strong>Cat-Themed Favicon Packaging</strong>: Embedded a lightweight, custom vector cat-themed SVG favicon (using an inline base64 Data URL) inside the HTML templates, ensuring it is automatically packaged inside the NPM distribution package and the Docker container image without requiring external asset serving handlers.</li><li><strong>Official Fastify Rate Limiting Plugin Integration</strong>: Replaced the custom in-memory rate limiting implementation with the official <code>@fastify/rate-limit</code> plugin. This hardens sensitive authentication endpoints (token retrieval, OIDC authorize redirect, and OIDC refresh) using a parameterizable rate-limiter configuration (<code>RATE_LIMIT_MAX</code> and <code>RATE_LIMIT_WINDOW</code> environment variables) with proxy-aware client IP extraction to correctly identify clients behind reverse proxies.</li></ul><h2 id="what-s-new-in-v0-8-1" tabindex="-1">What&#39;s New in v0.8.1 <a class="header-anchor" href="#what-s-new-in-v0-8-1" aria-label="Permalink to &quot;What&#39;s New in v0.8.1&quot;">​</a></h2><ul><li><strong>NPM and GitHub Links &amp; Dynamic Version Reporting</strong>: Integrated direct navigation links to the <a href="https://github.com/nogoo9/no-crd" target="_blank" rel="noreferrer">GitHub Repository</a> and the <a href="https://www.npmjs.com/package/@nogoo9/no-crd" target="_blank" rel="noreferrer">npm Package</a> inside the gateway dashboard footer. Added a styled mono-badge displaying the dynamic server capabilities version returned by the backend (e.g. <code>no-crd v0.8.1</code>).</li><li><strong>SystemMetadata Backend Info</strong>: Added a <strong>no-crd Backend</strong> card inside the <code>SystemInfo</code> metadata modal showing <code>@nogoo9/no-crd</code> and its current active version, aligning with the new capabilities version payload field.</li><li><strong>Workspace Owner Validation &amp; Spawn Schema Mismatch</strong>: Fixed a schema validation mapping error by changing the frontend parameter from <code>targetUserSub</code> to <code>userSub</code> during workspace spawning. This ensures that dynamic workspaces inherit the proper owner context instantly on creation.</li><li><strong>Admin Layout Separation &amp; Duplication Prevention</strong>: Partitioned the workspaces grid for admin sessions so that admin-owned sandboxes appear in &quot;My Workspace Sandboxes&quot; and all other users&#39; sandboxes appear in &quot;Shared/Other Sandboxes&quot; without visual duplication or layout overlapping.</li><li><strong>Refresh Interval Annotation Registration</strong>: Registered the <code>nogoo9/api.&lt;api-name&gt;.refresh</code> annotation in <code>src/config/annotations.ts</code>, which automatically updates the permissions and annotations tables in user guides and developer docs.</li></ul><h2 id="what-s-new-in-v0-8-0" tabindex="-1">What&#39;s New in v0.8.0 <a class="header-anchor" href="#what-s-new-in-v0-8-0" aria-label="Permalink to &quot;What&#39;s New in v0.8.0&quot;">​</a></h2><ul><li><strong>Consolidated Workspace UI &amp; Event Logs</strong> (<a href="/no-crd/decisions/ADR-015-workspace-view-consolidation-and-template-upgrades.html">ADR-015</a>): Streamlined the frontend dashboard by removing the separate &quot;Pods&quot; list and merging log streaming, pod lifecycle events, and upgrade status directly into the single Workspace card view. Added a new <code>get_workspace_events</code> MCP tool to pull real-time Kubernetes pod events into the UI.</li><li><strong>Template Versioning and Outdated Detection</strong>: Introduced the <code>nogoo9/template-version</code> annotation (defaulting to <code>&quot;1.0.0&quot;</code>) on pod templates. The <code>list_workspaces</code> and <code>get_workspace</code> tools now compare the pod&#39;s running template version against the latest version available in the registry, returning <code>isOutdated: true</code> for older versions so the UI can display warnings and upgrade options.</li><li><strong>Safe Template Upgrades</strong>: Implemented a state-preserving workspace upgrade flow (<code>upgrade_workspace</code> and <code>upgrade_all_workspaces</code> MCP tools) that retains PersistentVolumeClaim (PVC) attachments and custom environment variables, polling Kubernetes until the old pod is fully terminated before spawning the new pod to prevent volume attach conflicts.</li><li><strong>Workspace Authentication Modes</strong>: Added support for <code>WORKSPACE_AUTH_MODE</code> configuration mapping, allowing workspaces to run in four modes: <code>inject-headers</code> (inject OIDC identity headers to downstream proxy), <code>same-origin</code> (redirect workspaces to local token-api for same-origin authentication), <code>token-api</code> (expose OIDC token generation directly via a secure local endpoint), and <code>no-auth</code> (bypass authorization checks entirely for routing proxy and delegate auth check directly to the workspace).</li><li><strong>Default Auth Mode and Bypasses</strong>: When <code>AUTH_ENABLED</code> is true, the <code>inject-headers</code> auth mode is enabled by default for all workspaces regardless of whether the annotation is provided. Added detailed annotations mapping documentation.</li><li><strong>Frictionless STDIO Developer Tools Support</strong>: Configured the server to dynamically bypass JWT token validation when running on <code>TRANSPORT=stdio</code>, allowing local debugging tools like the MCP Inspector and Claude Desktop to function seamlessly while preserving full authentication checks on network connections.</li><li><strong>Global OAuth Metadata Discovery</strong>: Implemented a global interception hook for <code>/.well-known/oauth-protected-resource</code> and its subpaths, returning discovery metadata with <code>Access-Control-Allow-Origin: *</code> to prevent CORS blocks from local developer interfaces.</li><li><strong>Collapsible Template Groups &amp; UI Polish</strong>: Enabled template group carets and headers to collapse/expand workspace templates, persisting their states in <code>localStorage</code>. Optimized workspace card footers into a split toolbar layout (developer utilities on the left, life cycle action on the right) with borderless flat buttons, terracotta hover overlays, and distinct icons.</li></ul><h2 id="what-s-new-in-v0-7-0" tabindex="-1">What&#39;s New in v0.7.0 <a class="header-anchor" href="#what-s-new-in-v0-7-0" aria-label="Permalink to &quot;What&#39;s New in v0.7.0&quot;">​</a></h2><ul><li><strong>Hardened Admin Access and Role Mapping</strong> (<a href="/no-crd/decisions/ADR-014-admin-access-hardening-and-role-mapping.html">ADR-014</a>): Added a dedicated <code>AUTH_REQUIRED_ADMIN_SCOPE</code> (defaulting to <code>&quot;nogoo9:admin&quot;</code>). All administrative actions now require both the admin role (<code>AUTH_ADMIN_ROLE</code>) and the admin scope. An admin scope hierarchy makes the admin scope a superset that automatically satisfies standard read and write scope requirements. Bypassed scope validation for tokens that do not possess any scope claim (backwards compatibility).</li><li><strong>SSO Redirection and Token Bootstrapping</strong>: Implemented a token-to-cookie bootstrapping flow for workspace proxy access. Direct workspace hits redirect unauthorized HTML requests back to the SSO sign-in page, which routes users back with query-parameter tokens to establish the session.</li><li><strong>Ada Space Inspired UI Realignment</strong>: Cleaned up the dashboard by removing redundant borders, improving layouts to prevent overlaps, and adding features like grid/list view layout toggles, client-side search bar filtering, and comfortable/compact density controls.</li><li><strong>Direct OAuth URL Configuration</strong>: Supported direct token/authorization endpoints as alternatives to OIDC discovery URLs.</li><li><strong>Husky &amp; Nano-Staged Git Hooks</strong>: Integrated git hooks to automate linting, formatting, and safety checks on commit.</li></ul><h2 id="what-s-new-in-v0-6-0" tabindex="-1">What&#39;s New in v0.6.0 <a class="header-anchor" href="#what-s-new-in-v0-6-0" aria-label="Permalink to &quot;What&#39;s New in v0.6.0&quot;">​</a></h2><ul><li><strong>Proxy-Level Transparent Token Refresh</strong>: Implemented a transparent, proxy-level OIDC token refresh flow in the BFF gateway. The gateway now automatically intercepts requests with expired JWT access tokens, exchanges the HttpOnly secure session refresh token for a new pair of access/refresh tokens, and updates the client cookies and session in-flight without interrupting the user.</li><li><strong>OIDC Scopes Parameterization</strong>: Parameterized the OIDC scopes requested by the frontend login flow via the <code>OAUTH_SCOPES</code> server environment variable (defaulting to <code>&quot;openid profile email offline_access&quot;</code>).</li><li><strong>Keycloak Offline Access Role Mapping</strong>: Explicitly added the <code>offline_access</code> role to the local dev realm configuration and assigned it to the default test users (<code>readuser</code>, <code>writeuser</code>, <code>adminuser</code>) to prevent <code>400 Bad Request</code> token exchange failures during OIDC PKCE flow.</li></ul><h2 id="what-s-new-in-v0-5-6" tabindex="-1">What&#39;s New in v0.5.6 <a class="header-anchor" href="#what-s-new-in-v0-5-6" aria-label="Permalink to &quot;What&#39;s New in v0.5.6&quot;">​</a></h2><ul><li><strong>Built-in Local Fallback Templates</strong>: Dynamically attach the <code>nogoo9/pod-template</code> label in the local template parser so that fallback workspaces automatically inherit the template discovery label without modifying the YAML files.</li></ul><h2 id="what-s-new-in-v0-5-5" tabindex="-1">What&#39;s New in v0.5.5 <a class="header-anchor" href="#what-s-new-in-v0-5-5" aria-label="Permalink to &quot;What&#39;s New in v0.5.5&quot;">​</a></h2><ul><li><strong>Template Label Merging</strong>: Corrected the bug where metadata labels defined in templates (both cluster-level ConfigMaps and local templates) were not merged into the spawned workspace pod&#39;s labels.</li><li><strong>Dynamic Init volume sharing</strong>: Added a new boolean annotation flag <code>nogoo9/init-share-volumes</code> (default <code>true</code>). Setting it to <code>&quot;false&quot;</code> disables the auto-application of the main container&#39;s volume mounts to the dynamically injected <code>spawner-init</code> container.</li></ul><h2 id="what-s-new-in-v0-5-4" tabindex="-1">What&#39;s New in v0.5.4 <a class="header-anchor" href="#what-s-new-in-v0-5-4" aria-label="Permalink to &quot;What&#39;s New in v0.5.4&quot;">​</a></h2><ul><li><strong>Fix &quot;Already Connected&quot; on Multi-Session</strong> (<a href="/no-crd/decisions/ADR-012-per-session-mcp-server-factory.html">ADR-012</a>): The MCP server threw <code>&quot;already connected transport, call close()&quot;</code> when a second client session connected. The shared <code>globalMcpServer</code> singleton has been replaced with a per-session factory pattern, following the official MCP SDK convention. Each session now gets its own <code>McpServer</code> instance, and startup validation uses a throwaway server that is discarded after RBAC checks pass.</li><li><strong>Configurable UI Branding</strong>: Set <code>UI_TITLE</code> and <code>UI_SUBTITLE</code> environment variables to customize the dashboard header for white-label deployments. The branding is served via <code>/healthz</code> and applied at runtime — no rebuild required.</li><li><strong>Version Badge in UI Header</strong>: The dashboard header now shows the current server version, fetched at runtime from <code>/healthz</code>. GitHub and npm icon links are also displayed for quick navigation.</li><li><strong><code>/healthz</code> Returns Version &amp; Branding</strong>: The health check endpoint now returns <code>{ status, version, branding: { title, subtitle } }</code>, making version and branding information available to clients and monitoring tools.</li><li><strong>Centralized <code>APP_VERSION</code> Constant</strong>: A single <code>src/version.ts</code> module is the runtime source of truth for the application version, used by the MCP server constructor and <code>/healthz</code>. The <code>/bump</code> workflow updates it alongside <code>package.json</code> and <code>server.json</code>.</li><li><strong>&quot;Powered by nogoo9&quot; Footer</strong>: A subtle footer with a link to the GitHub repository is shown at the bottom of the dashboard.</li></ul><h2 id="what-s-new-in-v0-5-3" tabindex="-1">What&#39;s New in v0.5.3 <a class="header-anchor" href="#what-s-new-in-v0-5-3" aria-label="Permalink to &quot;What&#39;s New in v0.5.3&quot;">​</a></h2><ul><li><strong>Production-Safe UI Transport</strong>: Removed the hardcoded <code>http://localhost:3000/mcp</code> fallback from the dashboard&#39;s HTTP transport client. The UI now exclusively uses the same-origin relative path derived from <code>BASE_URL</code>, eliminating <code>ERR_CONNECTION_REFUSED</code> errors and startup delays when deployed behind an ingress or reverse proxy.</li><li><strong>Subpath-Aware Logout</strong>: The UI logout button now correctly prefixes the <code>/logout</code> fetch call with <code>BASE_URL</code>, fixing silent 404 failures when the server is hosted under a subpath (e.g., <code>/gateway/no-crd</code>).</li><li><strong>Fix Infinite OIDC Refresh Loop</strong>: When the MCP endpoint returned <code>401 Unauthorized</code>, the UI called <code>window.location.reload()</code> which raced with the OIDC redirect flow, causing the page to loop endlessly without ever reaching the Identity Provider. The 401 handlers now show the login overlay instead, allowing the OIDC <code>triggerRedirect()</code> to execute normally.</li></ul><h2 id="what-s-new-in-v0-5-2" tabindex="-1">What&#39;s New in v0.5.2 <a class="header-anchor" href="#what-s-new-in-v0-5-2" aria-label="Permalink to &quot;What&#39;s New in v0.5.2&quot;">​</a></h2><ul><li><strong>Graceful Error Handling</strong>: MCP tool handlers (<code>current_namespace</code>, <code>get_capabilities</code>) no longer crash on authentication failures or K8s API timeouts. All error paths now return structured <code>errorResult</code> responses that the UI can render gracefully.</li><li><strong>Server Startup Resilience</strong>: The MCP server now boots even when RBAC permission evaluation fails at startup (e.g., K8s API temporarily unreachable during pod initialization). It degrades to diagnostic tools only (<code>check_permissions</code>, <code>get_capabilities</code>, <code>current_namespace</code>, <code>list_templates</code>, <code>get_template</code>, <code>list_registry_images</code>) and operators can use these tools to troubleshoot.</li><li><strong>Permission Denial Test Matrix</strong>: Comprehensive test coverage for 6 RBAC denial scenarios, ensuring every combination of permission grants/denials results in correct tool gating without server crashes.</li></ul><h2 id="what-s-new-in-v0-5-1" tabindex="-1">What&#39;s New in v0.5.1 <a class="header-anchor" href="#what-s-new-in-v0-5-1" aria-label="Permalink to &quot;What&#39;s New in v0.5.1&quot;">​</a></h2><ul><li><strong>Graceful ConfigMap Template Fallback</strong> (<a href="/no-crd/decisions/ADR-010-graceful-configmap-template-fallback.html">ADR-010</a>): Template tools now degrade gracefully when the service account lacks <code>configmaps</code> RBAC permissions. <code>list_templates</code> catches ConfigMap errors and continues to return local/built-in templates with a warning. <code>spawn_workspace</code> falls back to local templates when ConfigMap reads fail.</li><li><strong>Template Read Tools Always Available</strong>: <code>list_templates</code> and <code>get_template</code> are no longer gated behind ConfigMap permissions — they are unconditionally registered, ensuring agents always have access to the local and built-in template catalog even in minimal RBAC deployments.</li></ul><h2 id="what-s-new-in-v0-5-0" tabindex="-1">What&#39;s New in v0.5.0 <a class="header-anchor" href="#what-s-new-in-v0-5-0" aria-label="Permalink to &quot;What&#39;s New in v0.5.0&quot;">​</a></h2><ul><li><strong>Managed-Only Pod Access Control</strong> (<a href="/no-crd/decisions/ADR-008-managed-only-pod-access-control.html">ADR-008</a>): Pod tools (<code>list_pods</code>, <code>get_pod</code>, <code>delete_pod</code>, <code>patch_pod</code>, <code>get_pod_logs</code>) now only operate on pods labeled <code>nogoo9/managed-by=nogoo9-spawner</code> when <code>MANAGED_ONLY=true</code> (default). No one bypasses this — not even admins.</li><li><strong>Unmanaged Pod Count</strong>: <code>list_pods</code> reports <code>unmanagedCount</code> — the number of pods in the namespace not managed by this server — without leaking details.</li><li><strong>Auto-Label on Create</strong>: <code>create_pod</code> automatically applies the <code>nogoo9/managed-by=nogoo9-spawner</code> label to all new pods, ensuring they are visible under managed-only mode.</li><li><strong>Server Capabilities Endpoint</strong>: New <code>get_capabilities</code> MCP tool returns <code>{ enabledTools, managedOnly, authEnabled, isAdmin }</code> so UI clients can adapt their rendering.</li><li><strong>Capabilities-Aware UI</strong>: The dashboard now disables buttons (Delete, Stop, Spawn, Logs) when the corresponding MCP tool is not in the user&#39;s <code>enabledTools</code> list. Disabled buttons show a &quot;Insufficient permissions&quot; tooltip.</li><li><strong>Eager MCP Server Initialization</strong> (<a href="/no-crd/decisions/ADR-009-eager-startup-health-check.html">ADR-009</a>): The HTTP transport now creates the MCP server and evaluates RBAC permissions at startup rather than lazily on the first request. If the Kubernetes API is unreachable, the server exits immediately with an actionable error instead of silently failing on every request.</li><li><strong>Startup Health Check</strong>: A K8s API connectivity probe (<code>listPods limit=1</code>) runs before the MCP server is created. Failed deployments now produce clear log messages with hints for common issues (ECONNREFUSED, Unauthorized, missing RBAC bindings).</li></ul><h2 id="what-s-new-in-v0-4-1" tabindex="-1">What&#39;s New in v0.4.1 <a class="header-anchor" href="#what-s-new-in-v0-4-1" aria-label="Permalink to &quot;What&#39;s New in v0.4.1&quot;">​</a></h2><ul><li><strong>Schema-Driven CLI Wrapper</strong>: Refactored the command-line utility entrypoint to dynamically load parameter types, defaults, and validations directly from active configuration schemas, completely removing hardcoded flags logic.</li><li><strong>Centralized Keys Map</strong>: Replaced all hardcoded magic strings for Kubernetes labels and annotations with a single, unified reference map <code>ANNOTATION_KEYS</code> in <code>src/config/annotations.ts</code>.</li><li><strong>Encapsulated Config Directory</strong>: Moved and consolidated all configuration logic directly inside the <code>src/config/</code> directory, removing the redundant <code>src/config.ts</code> wrapper file.</li></ul><h2 id="what-s-new-in-v0-4-0" tabindex="-1">What&#39;s New in v0.4.0 <a class="header-anchor" href="#what-s-new-in-v0-4-0" aria-label="Permalink to &quot;What&#39;s New in v0.4.0&quot;">​</a></h2><ul><li><strong>Local &amp; Built-In Templates</strong>: Added support for reading workspace Pod templates from a local directory (<code>TEMPLATES_DIR</code> environment variable) or using the built-in catalog in <code>templates/</code>. These are merged seamlessly with cluster-level ConfigMap templates, with ConfigMaps taking higher priority in case of name collisions.</li><li><strong>Three-Source Theme Merge Engine</strong>: Implemented a visual theme provider. Themes are dynamically scanned and merged from three layers: cluster ConfigMaps (<code>THEMES_CONFIGMAP</code>), custom directories (<code>THEMES_DIR</code>), and built-in fallback themes.</li><li><strong>Robust Packaged UI Asset Resolution</strong>: Hardened path resolution (<code>DIST_DIR</code>) and asset loading (<code>resolveBuiltinDir</code>) in both flat and nested environments. This guarantees that running from compiled bundles, published npm packages (<code>nocrd9</code>), and Docker containers will correctly resolve static UI assets, templates, and themes out of the box.</li><li><strong>Server &amp; Routes Modularization</strong>: Refactored the core HTTP/HTTPS server in <code>src/server/</code> into sub-modules (<code>mcp.ts</code>, <code>proxy.ts</code>, <code>static.ts</code>, <code>themes.ts</code>, <code>auth.ts</code>) to improve codebase legibility, test isolation, and route organization.</li><li><strong>NPM Publish Safety Hook</strong>: Added <code>&quot;prepublishOnly&quot;: &quot;bun run build&quot;</code> in <code>package.json</code> to prevent publishing packages with missing or stale frontend assets.</li><li><strong>Open WebUI Workspace Sandboxing</strong>: Replaced browser-based WebContainers guide with a containerized <code>open-webui</code> template supporting persistent SQLite data mappings and local k3d registry bootstrap.</li><li><strong>Workspace ID Auto-Generation</strong>: Spawn modal automatically generates valid DNS-1123 compliant workspace resource IDs, prefixed with a sanitized user OIDC identity from the JWT payload and safely truncated to prevent length issues.</li><li><strong>Dynamic Context Warnings</strong>: Added dynamic validation for required context variables, including a visible warning note to caution users that plain-text secrets will be visible in the pod spec.</li><li><strong>MCP Server Metadata Description (<code>server.json</code>)</strong>: Added a standard <code>server.json</code> file to describe server capabilities, parameters, and environment variables for automated registry publishing and CI/CD.</li><li><strong>Directory Layout Restructuring</strong>: Renamed the <code>deploy/</code> directory to <code>kubernetes/manifest/</code> to align with future Helm charts and package organization.</li><li><strong>YAML Pod Spec Parser</strong>: Fixed the <code>spawn_workspace</code> spec parser to run <code>parseSpecString</code> instead of raw <code>JSON.parse</code>, enabling YAML templates to load successfully without crashing on non-JSON start characters.</li></ul><hr><h2 id="what-s-new-in-v0-3-0" tabindex="-1">What&#39;s New in v0.3.0 <a class="header-anchor" href="#what-s-new-in-v0-3-0" aria-label="Permalink to &quot;What&#39;s New in v0.3.0&quot;">​</a></h2><ul><li><strong>GitHub Actions Security Tooling</strong>: Standardized workflows with <code>actionlint</code> and <code>zizmor</code> security scanning, enforced strict job-level least-privilege permissions, and SHA-pinned Github Actions.</li><li><strong>K3d Keycloak Service for E2E OAuth</strong>: Built a comprehensive local OIDC testing environment with a containerized Keycloak deployment (<code>quay.io/keycloak/keycloak:26.0</code>) pre-configured with a PKCE-ready public client and test users to validate authorization flows locally.</li><li><strong>Authentication &amp; Authorization Hardening</strong>: <ul><li>Enforced per-user isolation for all raw pod tools (<code>list_pods</code>, <code>get_pod</code>, <code>create_pod</code>, <code>delete_pod</code>, <code>patch_pod</code>, and <code>get_pod_logs</code>) using label selector filtering, alongside admin role escalation via JWT claims.</li><li>Added cookie-based session extraction (<code>nocr_token</code>) inside the routing proxy, allowing relative sub-resources (JS, CSS, images) inside routed workspaces to load securely on new tabs.</li><li>Introduced parameterizable OAuth scope checks (<code>AUTH_REQUIRED_READ_SCOPE</code>, <code>AUTH_REQUIRED_WRITE_SCOPE</code>, and <code>AUTH_SCOPE_JSONPATH</code>) to segregate read-only client permissions from mutation/execution client permissions.</li></ul></li><li><strong>New <code>get_workspace</code> Tool</strong>: Implemented a tool that retrieves workspace IP, container port, status, annotations, and preview metadata for a single workspace.</li><li><strong>UI Theme &amp; PKCE Authorization</strong>: <ul><li>Integrated a premium dark/light/system theme toggle.</li><li>Implemented client-side PKCE authorization redirect login.</li><li>Built an in-dashboard workspace file preview rendering engine supporting sandboxed HTML iframes and custom Markdown formatting.</li></ul></li><li><strong>Bun WebSocket Upgrade Compatibility Warning</strong>: Documented a critical regression in the Bun runtime compatibility layer (<code>oven-sh/bun#28871</code>) where asynchronous upgrade handshakes drop incoming frames, and proposed a native <code>Bun.serve</code> WebSocket proxy architecture to mitigate this.</li></ul><hr><h2 id="what-s-new-in-v0-2-0" tabindex="-1">What&#39;s New in v0.2.0 <a class="header-anchor" href="#what-s-new-in-v0-2-0" aria-label="Permalink to &quot;What&#39;s New in v0.2.0&quot;">​</a></h2><ul><li><strong>Composable Programmatic SDK</strong>: Refactored <code>src/index.ts</code> to export clean programmatic APIs for spawning, stopping, and listing workspaces/templates. Developers can pass custom <code>KubeConfig</code> or pre-configured client APIs to run custom pod lifecycles programmatically. The server startup logic is isolated in <code>src/server-entry.ts</code>.</li><li><strong>Subpath Prefix Support</strong>: Configurable <code>BASE_URL</code> allows hosting the dashboard and server behind an enterprise reverse proxy subpath (e.g. <code>/gateway/no-crd</code>) without needing path rewrite rules.</li><li><strong>Dynamic Routing Proxy</strong>: A built-in JWT-authorized workspace router <code>/route/:workspaceId/*</code> routes internal HTTP traffic natively to the target pod IP in the cluster. Ensures that the pod&#39;s owner claim matches the token&#39;s subject.</li><li><strong>RFC 9728 &amp; Extended Auth</strong>: Fully compliant with RFC 9728 by serving <code>/.well-known/oauth-protected-resource</code> discovery metadata. Propagates JWT identity context across direct tools calls using <code>AsyncLocalStorage</code>.</li><li><strong>State Setup &amp; Backup Examples</strong>: Provides robust examples under <code>examples/</code> demonstrating how to pull code from Git at workspace startup (<code>initContainers</code>) and how to run automated backup scripts to S3/MinIO on workspace termination (<code>preStop</code> sidecars).</li><li><strong>Dashboard Upgrade</strong>: Re-engineered UI featuring a visual user identity badge, token settings modal, YAML/JSON spec drawer viewer, dynamic template creation form, and live toast notification banners.</li></ul><hr><h2 id="what-s-new-in-v0-1-1" tabindex="-1">What&#39;s New in v0.1.1 <a class="header-anchor" href="#what-s-new-in-v0-1-1" aria-label="Permalink to &quot;What&#39;s New in v0.1.1&quot;">​</a></h2><ul><li><strong>NPM Publishing Improvements:</strong> Updated <code>package.json</code> with standard publishing metadata fields (<code>homepage</code>, <code>repository</code>, <code>bugs</code>, <code>publishConfig</code>, <code>keywords</code>, and <code>files</code>) to ensure clean distribution.</li><li><strong>Documentation Portal Linkage:</strong> Embedded direct links to the published GitHub Pages documentation site in the README and project badges.</li></ul><hr><h2 id="what-s-new-in-v0-1-0-initial-release" tabindex="-1">What&#39;s New in v0.1.0 (Initial Release) <a class="header-anchor" href="#what-s-new-in-v0-1-0-initial-release" aria-label="Permalink to &quot;What&#39;s New in v0.1.0 (Initial Release)&quot;">​</a></h2><p><code>@nogoo9/no-crd</code> is designed for agent-driven, on-demand pod orchestration in Kubernetes (k8s/k3s) <strong>without Custom Resource Definitions (CRDs)</strong>. It provides a lightweight MCP server enabling AI agents and API clients to dynamically spin up, inspect, and teardown ephemeral sandboxes.</p><h3 id="key-capabilities" tabindex="-1">Key Capabilities <a class="header-anchor" href="#key-capabilities" aria-label="Permalink to &quot;Key Capabilities&quot;">​</a></h3><h4 id="_1-zero-custom-resource-definitions-crds" tabindex="-1">1. Zero Custom Resource Definitions (CRDs) <a class="header-anchor" href="#_1-zero-custom-resource-definitions-crds" aria-label="Permalink to &quot;1. Zero Custom Resource Definitions (CRDs)&quot;">​</a></h4><ul><li>Operates entirely against core Kubernetes APIs using native resources (<code>Pods</code>, <code>ConfigMaps</code>, and <code>ServiceAccounts</code>).</li><li>Ensures portability across cloud platforms (EKS, GKE, K3s) and works inside locked-down environments with restricted cluster RBAC policies.</li></ul><h4 id="_2-reusable-configmap-based-templates" tabindex="-1">2. Reusable ConfigMap-Based Templates <a class="header-anchor" href="#_2-reusable-configmap-based-templates" aria-label="Permalink to &quot;2. Reusable ConfigMap-Based Templates&quot;">​</a></h4><ul><li>Pod definitions are templated as standard Kubernetes ConfigMaps labeled with <code>nogoo9/pod-template=true</code>.</li><li>Supports full template lifecycle management: listing, retrieval, inline overriding, creation, updating, and deletion.</li></ul><h4 id="_3-agent-workspace-spawner" tabindex="-1">3. Agent Workspace Spawner <a class="header-anchor" href="#_3-agent-workspace-spawner" aria-label="Permalink to &quot;3. Agent Workspace Spawner&quot;">​</a></h4><ul><li>Simplifies agent workspaces lifecycle.</li><li>Handles user identity extraction from JWT tokens (to prevent tenant sandbox collision).</li><li>Automatically provisions IAM Role-bound ServiceAccounts.</li><li>Hooks up container initialization (<code>initContainers</code>) and graceful termination (<code>preStop</code> sidecars).</li></ul><h4 id="_4-isomorphic-kubernetes-certificate-transport" tabindex="-1">4. Isomorphic Kubernetes Certificate Transport <a class="header-anchor" href="#_4-isomorphic-kubernetes-certificate-transport" aria-label="Permalink to &quot;4. Isomorphic Kubernetes Certificate Transport&quot;">​</a></h4><ul><li>Solves Node-specific HTTPS Agent compatibility issues on native Bun and Deno <code>fetch</code> engines.</li><li>Intercepts outbound requests and translates certificates seamlessly for Bun TLS options and Deno HTTP client configurations.</li></ul><h4 id="_5-dynamic-rbac-based-permission-checking" tabindex="-1">5. Dynamic RBAC-Based Permission Checking <a class="header-anchor" href="#_5-dynamic-rbac-based-permission-checking" aria-label="Permalink to &quot;5. Dynamic RBAC-Based Permission Checking&quot;">​</a></h4><ul><li>Proactively queries the Kubernetes API using <code>SelfSubjectAccessReview</code> at startup.</li><li>Dynamically enables or disables specific MCP tools depending on active RBAC capabilities, avoiding runtime API authorization crashes.</li></ul><h4 id="_6-embedded-react-pod-manager-ui" tabindex="-1">6. Embedded React Pod Manager UI <a class="header-anchor" href="#_6-embedded-react-pod-manager-ui" aria-label="Permalink to &quot;6. Embedded React Pod Manager UI&quot;">​</a></h4><ul><li>Bundles a responsive web application dashboard served directly as an MCP application resource.</li><li>Allows visual browsing of active pods, template configmaps, and real-time logs.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("whats-new.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const whatsNew = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  whatsNew as default
};
