import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-019: Split-Network OIDC Issuer Alignment and Path-Scoped Cookie Routing","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-019-split-network-oidc-issuer-and-cookie-path-alignment.md","filePath":"decisions/ADR-019-split-network-oidc-issuer-and-cookie-path-alignment.md"}');
const _sfc_main = { name: "decisions/ADR-019-split-network-oidc-issuer-and-cookie-path-alignment.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-019-split-network-oidc-issuer-alignment-and-path-scoped-cookie-routing" tabindex="-1">ADR-019: Split-Network OIDC Issuer Alignment and Path-Scoped Cookie Routing <a class="header-anchor" href="#adr-019-split-network-oidc-issuer-alignment-and-path-scoped-cookie-routing" aria-label="Permalink to &quot;ADR-019: Split-Network OIDC Issuer Alignment and Path-Scoped Cookie Routing&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-10</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>In the <code>nogoo9/no-crd</code> platform, OIDC authentication has been extended to support workspace authorization and token delegation. However, deploying the architecture in local k3d Kubernetes clusters exposed two critical network boundary and configuration challenges:</p><ol><li><p><strong>Split-Network OIDC Refresh Mismatch</strong>:</p><ul><li>Browser-based OAuth flows use the external Keycloak address mapped to host localhost (<code>http://localhost:8080</code>), generating tokens with <code>iss: http://localhost:8080/auth/realms/nogoo9</code>.</li><li>The backend server validates and refreshes tokens using Keycloak&#39;s internal cluster DNS address (<code>http://keycloak.nogoo9.svc.cluster.local:8080</code>).</li><li>By default in development mode, Keycloak dynamically computes its issuer URL from the incoming Host header. Consequently, during a backend-to-backend token refresh request, Keycloak computed the expected issuer to be the internal service URL, saw the mismatch against the incoming refresh token&#39;s external issuer, and returned a <code>401 Unauthorized</code> (<code>invalid_grant: Invalid token issuer</code>).</li></ul></li><li><p><strong>OIDC Dynamic Relative Path Stripping</strong>:</p><ul><li>Setting Keycloak&#39;s frontend hostname strictly to a domain/host (e.g. <code>http://localhost:8080</code>) overrides its path detection. Under Keycloak 26&#39;s Hostname v2 SPI, the relative path prefix configured via <code>--http-relative-path=/auth</code> gets stripped from generated authorization endpoints and redirects, causing <code>404 Not Found</code> errors on frontchannel login redirection.</li></ul></li><li><p><strong>Subpath Router Cookie Scoping</strong>:</p><ul><li>Workspaces are routed under dynamic subpaths (e.g., <code>/nocr/route/:workspaceId/</code>). If cookies (<code>nocr_token</code>, <code>nocr_refresh</code>, <code>nocr_sess</code>) are set without an explicit prefix path, the browser might scope them to incorrect subpaths or fail to propagate them to adjacent sibling workspaces, breaking token verification and transparent refresh validation.</li></ul></li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Force Keycloak Frontend Hostname &amp; Path</strong>: <ul><li>Configured the <code>KC_HOSTNAME</code> environment variable to <code>http://localhost:8080/auth</code> in the Keycloak Deployment manifest. This forces Keycloak to generate and validate the issuer URL <code>http://localhost:8080/auth/realms/nogoo9</code> consistently for both external browser-based flows and backchannel server-side token refresh operations.</li></ul></li><li><strong>Explicit Base Prefix Cookie Pathing</strong>: <ul><li>Updated the backend gateway&#39;s cookie generation logic to append the dynamic prefix (<code>basePrefix</code>) to cookie paths (e.g. <code>Path=/nocr/</code>). This ensures cookies are scoped correctly across all workspace subpaths, preventing isolation leakage and trailing-slash routing conflicts.</li></ul></li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Host Header Overriding</strong>: Forcing the <code>Host: localhost:8080</code> header in backend fetch requests to Keycloak. This was rejected because it relies on mock header parameters rather than configuring OIDC server endpoints properly, which is brittle and hard to maintain across environments.</li><li><strong>Root Keycloak Routing</strong>: Removing the <code>/auth</code> path prefix. This was rejected because it violates Keycloak&#39;s default path configuration in existing local environments, breaking alignment with the gateway configurations.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Robust Token Refresh</strong>: Token refresh operations succeed transparently behind the gateway proxy without throwing validation conflicts.</li><li><strong>Consistent Issuer Verification</strong>: Token issuers match regardless of the routing layer or client type used for the handshake.</li><li><strong>Accurate Browser Scoping</strong>: Path-scoped cookies are correctly handled by the browser, preserving session integrity when users navigate workspaces.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-019-split-network-oidc-issuer-and-cookie-path-alignment.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR019SplitNetworkOidcIssuerAndCookiePathAlignment = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR019SplitNetworkOidcIssuerAndCookiePathAlignment as default
};
