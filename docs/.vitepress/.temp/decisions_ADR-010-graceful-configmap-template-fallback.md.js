import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-010: Graceful ConfigMap Template Fallback","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-010-graceful-configmap-template-fallback.md","filePath":"decisions/ADR-010-graceful-configmap-template-fallback.md"}');
const _sfc_main = { name: "decisions/ADR-010-graceful-configmap-template-fallback.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-010-graceful-configmap-template-fallback" tabindex="-1">ADR-010: Graceful ConfigMap Template Fallback <a class="header-anchor" href="#adr-010-graceful-configmap-template-fallback" aria-label="Permalink to &quot;ADR-010: Graceful ConfigMap Template Fallback&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-29</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The MCP server&#39;s template system has three sources for pod templates, in priority order:</p><ol><li><strong>ConfigMap templates</strong> — Stored in-cluster as ConfigMaps with label <code>nogoo9/pod-template=true</code>. Requires <code>list</code>/<code>get</code> RBAC permissions on <code>configmaps</code>.</li><li><strong>Local templates</strong> — JSON/YAML files in a configured <code>TEMPLATES_DIR</code> on disk.</li><li><strong>Built-in templates</strong> — Bundled with the server in the built-in templates directory.</li></ol><p>When the service account lacks <code>configmaps</code> RBAC permissions (a valid minimal deployment scenario — the operator only wants pod management without template ConfigMaps), several tools fail hard:</p><ul><li><strong><code>list_templates</code></strong>: The <code>listTemplateMaps()</code> call throws a 403, and the outer catch returns an <code>errorResult</code> — discarding the local/built-in templates that would have been appended afterward.</li><li><strong><code>spawn_workspace</code></strong> with <code>templateRef</code>: The <code>readTemplateMap()</code> call throws a 403, and the handler returns an error — even if the template exists locally.</li><li><strong><code>create_pod_from_template</code></strong>: Already handles this correctly — uses <code>.catch(() =&gt; null)</code> on the ConfigMap read and falls through to <code>findLocalTemplate()</code>.</li></ul><p>The permission system at startup correctly disables tools that require unavailable verbs (e.g., <code>create_template</code> won&#39;t be registered without <code>create configmaps</code>). However, <code>list_templates</code> and <code>get_template</code> are also disabled when <code>list</code>/<code>get configmaps</code> permissions are missing — preventing access to local/built-in templates entirely.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Make the template system <strong>always return local/built-in templates</strong>, regardless of ConfigMap access:</p><h3 id="_1-list-templates-—-graceful-configmap-failure" tabindex="-1">1. <code>list_templates</code> — Graceful ConfigMap failure <a class="header-anchor" href="#_1-list-templates-—-graceful-configmap-failure" aria-label="Permalink to &quot;1. \`list_templates\` — Graceful ConfigMap failure&quot;">​</a></h3><p>Wrap the <code>listTemplateMaps()</code> call in a try/catch. On failure (403 or any error):</p><ul><li>Set <code>maps = []</code> and continue to the local/built-in template merge path.</li><li>Log a warning: <code>&quot;ConfigMap template listing failed (likely missing RBAC permissions). Local/built-in templates still available.&quot;</code></li><li>Include a warning prefix in the response text so the caller (agent or UI) is aware ConfigMap templates are unavailable.</li></ul><p>Local/built-in templates are always appended in the merge step, with ConfigMap templates taking priority on name collisions when available.</p><h3 id="_2-spawn-workspace-—-local-template-fallback" tabindex="-1">2. <code>spawn_workspace</code> — Local template fallback <a class="header-anchor" href="#_2-spawn-workspace-—-local-template-fallback" aria-label="Permalink to &quot;2. \`spawn_workspace\` — Local template fallback&quot;">​</a></h3><p>When <code>readTemplateMap()</code> fails for a <code>templateRef</code>, attempt <code>findLocalTemplate()</code> before returning an error. This mirrors the existing pattern in <code>create_pod_from_template</code>.</p><h3 id="_3-findlocaltemplate-extraction" tabindex="-1">3. <code>findLocalTemplate</code> extraction <a class="header-anchor" href="#_3-findlocaltemplate-extraction" aria-label="Permalink to &quot;3. \`findLocalTemplate\` extraction&quot;">​</a></h3><p>Move <code>findLocalTemplate()</code> from a private function in <code>templates.ts</code> to an exported function in <code>~/k8s/local-templates.ts</code>, re-exported via <code>~/k8s/index.ts</code>. This enables reuse in the spawner module.</p><h3 id="_4-permission-gated-registration-adjustment" tabindex="-1">4. Permission-gated registration adjustment <a class="header-anchor" href="#_4-permission-gated-registration-adjustment" aria-label="Permalink to &quot;4. Permission-gated registration adjustment&quot;">​</a></h3><p>The <code>list_templates</code> and <code>get_template</code> tools should remain registerable even when <code>configmaps</code> permissions are missing, since they can serve local/built-in templates. This requires either:</p><ul><li>Removing the configmap permission requirement from these tools in <code>REQUIRED_PERMISSIONS</code>, or</li><li>Unconditionally registering these two tools (they handle the missing permission gracefully at runtime).</li></ul><p><strong>Chosen approach</strong>: Remove the configmap permission gate for <code>list_templates</code> and <code>get_template</code> (since they work without it), keeping configmap permissions only for write tools and <code>create_pod_from_template</code>.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="require-configmaps-permissions-for-all-template-operations" tabindex="-1">Require configmaps permissions for all template operations <a class="header-anchor" href="#require-configmaps-permissions-for-all-template-operations" aria-label="Permalink to &quot;Require configmaps permissions for all template operations&quot;">​</a></h3><ul><li>Pros: Simple — if you want templates, you need ConfigMap access.</li><li>Cons: Prevents using local/built-in templates in minimal deployments; the permission system completely disables template tools even though they could partially work.</li><li><strong>Rejected</strong>: Too restrictive. Local templates are a first-class feature and should not be gated behind ConfigMap RBAC.</li></ul><h3 id="separate-tool-registration-for-local-vs-configmap-templates" tabindex="-1">Separate tool registration for local vs. ConfigMap templates <a class="header-anchor" href="#separate-tool-registration-for-local-vs-configmap-templates" aria-label="Permalink to &quot;Separate tool registration for local vs. ConfigMap templates&quot;">​</a></h3><ul><li>Pros: Clean separation of concerns.</li><li>Cons: Doubles the tool surface area (e.g., <code>list_local_templates</code> + <code>list_configmap_templates</code>); agents would need to know which to call; breaks existing UI expectations.</li><li><strong>Rejected</strong>: Adds complexity for agents and users. A unified view with graceful degradation is simpler.</li></ul><h3 id="always-disable-template-tools-when-configmap-permissions-are-missing-current-behavior" tabindex="-1">Always disable template tools when configmap permissions are missing (current behavior) <a class="header-anchor" href="#always-disable-template-tools-when-configmap-permissions-are-missing-current-behavior" aria-label="Permalink to &quot;Always disable template tools when configmap permissions are missing (current behavior)&quot;">​</a></h3><ul><li>Pros: No partial-state scenarios; tools either fully work or don&#39;t exist.</li><li>Cons: Loses access to local/built-in templates; forces operators to grant ConfigMap access even if they only use local templates.</li><li><strong>Rejected</strong>: This is the bug we&#39;re fixing. Local templates should always be accessible.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong><code>list_templates</code> and <code>get_template</code> are always available</strong>, even without ConfigMap RBAC. They return local/built-in templates in all cases, plus ConfigMap templates when permissions allow.</li><li><strong><code>spawn_workspace</code></strong> can use any template ref — ConfigMap, local, or built-in — with automatic fallback.</li><li><strong>Write tools</strong> (<code>create_template</code>, <code>update_template</code>, <code>delete_template</code>) remain correctly disabled when ConfigMap write permissions are unavailable.</li><li><strong>Operators</strong> deploying with minimal RBAC (pods-only) will see template tools working with local/built-in templates and clear warnings about missing ConfigMap access.</li><li><strong>Warning messages</strong> in tool responses give agents and the UI visibility into the degraded state without failing the operation.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-010-graceful-configmap-template-fallback.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR010GracefulConfigmapTemplateFallback = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR010GracefulConfigmapTemplateFallback as default
};
