import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-011: UI BASE_URL Contract and Cookie Path Consistency","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-011-ui-base-url-and-cookie-path-consistency.md","filePath":"decisions/ADR-011-ui-base-url-and-cookie-path-consistency.md"}');
const _sfc_main = { name: "decisions/ADR-011-ui-base-url-and-cookie-path-consistency.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-011-ui-base-url-contract-and-cookie-path-consistency" tabindex="-1">ADR-011: UI BASE_URL Contract and Cookie Path Consistency <a class="header-anchor" href="#adr-011-ui-base-url-contract-and-cookie-path-consistency" aria-label="Permalink to &quot;ADR-011: UI BASE_URL Contract and Cookie Path Consistency&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-29</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The server supports a <code>BASE_URL</code> environment variable (e.g., <code>/gateway/no-crd</code>) that prefixes all HTTP routes when deployed behind a reverse proxy or API gateway. Fastify registers all routes under <code>{ prefix: basePrefix }</code>, so a route defined as <code>/mcp</code> is served at <code>/gateway/no-crd/mcp</code>.</p><p>The UI dashboard runs in the browser and needs to make API calls to the server. It also constructs workspace links and handles OIDC redirect flows. All of these must resolve correctly regardless of whether <code>BASE_URL</code> is set or not.</p><p>Two issues were identified during an audit of the UI&#39;s HTTP calls:</p><ol><li><p><strong>Hardcoded <code>localhost:3000</code> fallback</strong>: The UI&#39;s HTTP transport initialization tried <code>http://localhost:3000/mcp</code> as a fallback endpoint. This is unreachable in production or k3d ingress deployments (port 3000 is only accessible inside the cluster), causing <code>ERR_CONNECTION_REFUSED</code> errors and unnecessary startup delays.</p></li><li><p><strong><code>/logout</code> missing <code>basePath</code></strong>: The logout button fetched <code>/logout</code> without the <code>basePath</code> prefix, resulting in a 404 when <code>BASE_URL</code> is configured.</p></li></ol><p>A third concern was raised about cookie <code>Path</code> values: the proxy sets <code>nocr_token</code> cookies with <code>Path=/route/{id}/</code> (without <code>basePrefix</code>), and the logout handler clears them with the same raw path. The question was whether this mismatch with the actual URL path (which includes <code>basePrefix</code>) would cause cookies to persist after logout.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-ui-basepath-contract" tabindex="-1">1. UI <code>basePath</code> Contract <a class="header-anchor" href="#_1-ui-basepath-contract" aria-label="Permalink to &quot;1. UI \`basePath\` Contract&quot;">​</a></h3><p>The server injects <code>window.__NOCR_BASE_URL__</code> into the HTML page via <code>loadUiHtml()</code>. The UI reads this as <code>basePath</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> basePath</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (window </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">as</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> any</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">).__NOCR_BASE_URL__ </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">||</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><strong>All UI fetch calls to server endpoints MUST prefix their paths with <code>basePath</code>.</strong> This includes:</p><table tabindex="0"><thead><tr><th>Endpoint</th><th>Path Pattern</th></tr></thead><tbody><tr><td>MCP transport</td><td><code>\${basePath}/mcp</code></td></tr><tr><td>Routing proxy links</td><td><code>\${basePath}/route/\${id}/\${path}</code></td></tr><tr><td>Theme list</td><td><code>\${basePath}/api/themes</code></td></tr><tr><td>Theme CSS file</td><td><code>\${basePath}/api/themes/\${id}.css</code></td></tr><tr><td>Logout</td><td><code>\${basePath}/logout</code></td></tr></tbody></table><p>OIDC redirect URIs use <code>window.location.origin + window.location.pathname</code>, which implicitly includes the base path since the page itself is served under it.</p><h3 id="_2-no-hardcoded-localhost-urls" tabindex="-1">2. No Hardcoded <code>localhost</code> URLs <a class="header-anchor" href="#_2-no-hardcoded-localhost-urls" aria-label="Permalink to &quot;2. No Hardcoded \`localhost\` URLs&quot;">​</a></h3><p>The <code>http://localhost:3000/mcp</code> fallback was removed. The relative same-origin path <code>\${basePath}/mcp</code> works in all deployment scenarios:</p><ul><li><strong>Local dev</strong> (<code>bun run dev:bun</code>): origin is <code>http://localhost:3000</code>, <code>basePath</code> is <code>&quot;&quot;</code>, resolves to <code>/mcp</code> ✓</li><li><strong>k3d ingress</strong> (<code>localhost:8080</code>): origin is <code>http://localhost:8080</code>, <code>basePath</code> is <code>&quot;&quot;</code>, resolves to <code>/mcp</code> via ingress ✓</li><li><strong>Subpath proxy</strong> (<code>example.com/gateway/no-crd</code>): <code>basePath</code> is <code>/gateway/no-crd</code>, resolves to <code>/gateway/no-crd/mcp</code> ✓</li></ul><h3 id="_3-cookie-path-consistency" tabindex="-1">3. Cookie Path Consistency <a class="header-anchor" href="#_3-cookie-path-consistency" aria-label="Permalink to &quot;3. Cookie Path Consistency&quot;">​</a></h3><p>Analysis confirmed that cookie paths are <strong>consistent</strong> between set and clear operations:</p><table tabindex="0"><thead><tr><th>Operation</th><th>Location</th><th>Cookie Path</th></tr></thead><tbody><tr><td><strong>Set</strong> <code>nocr_token</code></td><td><code>proxy.ts</code> onResponse</td><td><code>Path=\${basePrefix}/route/\${id}/</code></td></tr><tr><td><strong>Clear</strong> <code>nocr_token</code></td><td><code>mcp.ts</code> logoutHandler</td><td><code>Path=\${basePrefix}/route/\${id}/</code></td></tr><tr><td><strong>Set</strong> <code>nocr_sess</code></td><td><code>auth.ts</code> preHandler</td><td><code>Path=/</code></td></tr><tr><td><strong>Clear</strong> <code>nocr_sess</code></td><td><code>mcp.ts</code> logoutHandler</td><td><code>Path=/</code></td></tr></tbody></table><p>Both set and clear use the same paths (with <code>basePrefix</code> for workspace-scoped cookies). This works because:</p><ul><li>Fastify&#39;s <code>{ prefix: basePrefix }</code> affects URL routing, not the <code>Set-Cookie</code> header value.</li><li>The <code>Path</code> attribute in <code>Set-Cookie</code> is a literal string sent to the browser.</li><li>The browser matches cookies based on the <code>Path</code> value, not the URL the response came from.</li><li>Since both operations write the same <code>Path</code> value, clearing always matches what was set.</li></ul><blockquote><p><strong>Note</strong>: As of <a href="./ADR-019-split-network-oidc-issuer-and-cookie-path-alignment.html">ADR-019</a>, workspace-scoped cookies (<code>nocr_token</code>) include the <code>basePrefix</code> to ensure correct browser scoping across subpath deployments. The original design used raw paths without <code>basePrefix</code>, but this was corrected when split-network OIDC deployments exposed cookie isolation issues.</p></blockquote><p><strong>Important caveat</strong>: If the reverse proxy rewrites cookie paths (some do), this could break. The current design assumes the proxy is transparent to <code>Set-Cookie</code> headers, which is the standard behavior for k8s Ingress controllers and most API gateways.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="prefix-cookie-paths-with-baseprefix" tabindex="-1">Prefix cookie paths with <code>basePrefix</code> <a class="header-anchor" href="#prefix-cookie-paths-with-baseprefix" aria-label="Permalink to &quot;Prefix cookie paths with \`basePrefix\`&quot;">​</a></h3><ul><li>Pros: Cookies would match the actual URL path visible in the browser</li><li>Cons: <s>Unnecessary</s> — Initially rejected, but later adopted via <a href="./ADR-019-split-network-oidc-issuer-and-cookie-path-alignment.html">ADR-019</a> when subpath deployments exposed cookie scoping issues.</li></ul><h3 id="keep-the-localhost-3000-fallback-behind-a-dev-mode-flag" tabindex="-1">Keep the <code>localhost:3000</code> fallback behind a dev-mode flag <a class="header-anchor" href="#keep-the-localhost-3000-fallback-behind-a-dev-mode-flag" aria-label="Permalink to &quot;Keep the \`localhost:3000\` fallback behind a dev-mode flag&quot;">​</a></h3><ul><li>Pros: Would still work for the niche case of opening the UI from a different dev server</li><li>Cons: No real use case — in local dev the page is served from <code>localhost:3000</code> so the relative path works. The flag adds complexity for zero practical benefit.</li><li>Rejected: YAGNI. The relative path covers all real scenarios.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>All UI API calls are now audited and consistently use <code>basePath</code></li><li>No hardcoded localhost URLs remain in production code</li><li>Cookie path behavior is documented with inline comments referencing this ADR</li><li>Future developers modifying cookie-setting code should maintain the path consistency documented here</li></ul><h2 id="amendments" tabindex="-1">Amendments <a class="header-anchor" href="#amendments" aria-label="Permalink to &quot;Amendments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>2026-06-13</td><td>Updated cookie path table to reflect that <code>nocr_token</code> now uses <code>\${basePrefix}/route/\${id}/</code> (with basePrefix), as corrected by ADR-019. Updated Alternative A to note it was later adopted. Added cross-reference to ADR-019.</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-011-ui-base-url-and-cookie-path-consistency.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR011UiBaseUrlAndCookiePathConsistency = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR011UiBaseUrlAndCookiePathConsistency as default
};
