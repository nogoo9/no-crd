import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-022: Session Key Dependent Health Check Readiness","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-022-session-key-health-readiness.md","filePath":"decisions/ADR-022-session-key-health-readiness.md"}');
const _sfc_main = { name: "decisions/ADR-022-session-key-health-readiness.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-022-session-key-dependent-health-check-readiness" tabindex="-1">ADR-022: Session Key Dependent Health Check Readiness <a class="header-anchor" href="#adr-022-session-key-dependent-health-check-readiness" aria-label="Permalink to &quot;ADR-022: Session Key Dependent Health Check Readiness&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-12</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>During multi-replica deployments or rolling updates, pods coordinate using a leader election strategy to negotiate and share the exact same session secret key. Follower pods retrieve this key by polling the leader&#39;s <code>/internal/session-key</code> endpoint.</p><p>Previously, the readiness check (<code>/healthz</code> and <code>/mcp/healthz</code>) would report <code>status: &quot;ok&quot;</code> (200) as soon as the Fastify server port was successfully bound, regardless of whether the session key resolution process was complete.</p><p>If a follower pod started receiving client traffic before it completed peer discovery to resolve the session key, incoming client requests containing signed OIDC session cookies could not be validated or decrypted. This caused spurious 401/403 errors and redirection loops during replica scaling.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Modify the health check endpoints (<code>/healthz</code> and <code>/mcp/healthz</code>) in [mcp.ts](file:///home/eterna2/github/nogoo9-no-crd/src/server/routes/mcp.ts) to verify the status of the session key:</p><ol><li>Retrieve the session key using <code>getSessionKey()</code>.</li><li>If the key is empty (<code>&quot;&quot;</code>), return a status of <code>503 Service Unavailable</code> with a structured error payload:<div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;status&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;error&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;message&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Session key not resolved yet&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div></li><li>Once the key is resolved (via env, k8s Secret, or peer discovery), return the standard <code>200 OK</code> health status.</li></ol><p>This ensures that Kubernetes does not route public traffic to a booting replica until it has aligned on the session key with its peers.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="rely-solely-on-tcp-port-liveness-readiness" tabindex="-1">Rely solely on TCP port liveness/readiness <a class="header-anchor" href="#rely-solely-on-tcp-port-liveness-readiness" aria-label="Permalink to &quot;Rely solely on TCP port liveness/readiness&quot;">​</a></h3><ul><li><strong>Rejected</strong>: Binds to the port immediately, exposing a race window where incoming requests fail decrypting cookies due to a missing session key.</li></ul><h3 id="block-fastify-listen-until-the-key-is-resolved" tabindex="-1">Block Fastify <code>.listen()</code> until the key is resolved <a class="header-anchor" href="#block-fastify-listen-until-the-key-is-resolved" aria-label="Permalink to &quot;Block Fastify \`.listen()\` until the key is resolved&quot;">​</a></h3><ul><li><strong>Rejected</strong>: If follower pods cannot bind their port, they cannot serve the internal key endpoint, causing a deadlock where no pod can establish/share the key. Binding the port but returning 503 on health checks solves this.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Ingress controllers and Kubernetes readiness probes will block traffic to a booting replica until it has adopted the session key.</li><li>The window of session decryption errors during rolling updates is eliminated.</li><li>Internal communication between peer pods is enabled before the pod is marked ready.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-022-session-key-health-readiness.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR022SessionKeyHealthReadiness = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR022SessionKeyHealthReadiness as default
};
