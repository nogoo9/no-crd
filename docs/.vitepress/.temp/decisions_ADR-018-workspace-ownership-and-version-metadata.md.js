import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-018: Workspace Owner Association and Server Metadata Reporting","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-018-workspace-ownership-and-version-metadata.md","filePath":"decisions/ADR-018-workspace-ownership-and-version-metadata.md"}');
const _sfc_main = { name: "decisions/ADR-018-workspace-ownership-and-version-metadata.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-018-workspace-owner-association-and-server-metadata-reporting" tabindex="-1">ADR-018: Workspace Owner Association and Server Metadata Reporting <a class="header-anchor" href="#adr-018-workspace-owner-association-and-server-metadata-reporting" aria-label="Permalink to &quot;ADR-018: Workspace Owner Association and Server Metadata Reporting&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-09</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>In the <code>nogoo9/no-crd</code> platform, users can dynamically spawn, route to, and manage ephemeral workspaces. With authentication enabled, it is critical that:</p><ol><li><strong>Workspace Owner Association</strong>: A workspace has a defined owner (the authenticated user&#39;s <code>sub</code>) at the exact moment of creation. Without this association, workspaces would be orphaned, violating access control.</li><li><strong>Dashboard Isolation vs. Admin View</strong>: Standard users must only see workspaces they own on the dashboard. Admins require capability to see all active workspaces, partitioned cleanly to prevent layout clutter and duplication.</li><li><strong>Capability &amp; Version Reporting</strong>: Frontend clients need a runtime-agnostic way to inspect the server version of <code>@nogoo9/no-crd</code> and key repository links (NPM, GitHub) for diagnostics.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Schema and Parameter Alignment</strong>: Corrected the parameter passed during <code>spawn_workspace</code> from <code>targetUserSub</code> to <code>userSub</code> in the frontend <code>app.tsx</code> to match the backend validation schema. This ensures the backend spawner correctly assigns user subjects during workspace initialization.</li><li><strong>Schema Capability Version</strong>: Extended the <code>get_capabilities</code> tool response and the <code>GetCapabilitiesOutputSchema</code> in <code>src/mcp/namespace.ts</code> to return the server version (<code>APP_VERSION</code> from <code>src/version.ts</code>).</li><li><strong>List Workspace schema mapping</strong>: Enabled the <code>list_workspaces</code> tool to populate the <code>userSub</code>, <code>owner</code>, <code>creationTime</code>, and <code>description</code> fields. This ensures that even starting/pending workspaces (which have no running container details yet) are correctly mapped to their owners on list loads.</li><li><strong>Layout Partitioning</strong>: Filtered <code>activeUserWorkspaces</code> in the UI to match <code>ws.userSub === displayUser</code>. For administrators, other tenants&#39; sandboxes are rendered in a separate <code>&quot;Shared/Other Sandboxes (Admin Mode)&quot;</code> section, preventing duplicate items in the primary view.</li><li><strong>Interactive Version/Links UI</strong>: Integrated NPM and GitHub SVG icon links with micro-animations on hover, along with a <code>no-crd vX.Y.Z</code> version badge in the footer and a <strong>no-crd Backend</strong> card inside the <code>SystemInfoModal</code>.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Stateless UI versioning</strong>: Hardcoding the version string in the frontend build. This was rejected because it causes discrepancies if the frontend bundle is cached or if the UI is run out-of-sync with the MCP host.</li><li><strong>Backend-only filtering</strong>: Allowing the backend to restrict listing entirely. We opted for a hybrid approach: the backend restricts list scope for non-admins, while the frontend handles layout categorization (My vs. Shared) for admins.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Secure tenant isolation</strong>: Zero leakage of active/pending workspaces to other non-admin tenants.</li><li><strong>Improved visibility</strong>: Clear differentiation between owned and shared workspaces for administrators.</li><li><strong>Runtime diagnostic transparency</strong>: Instant correlation of frontend client with backend server version via the footer and System Info modal.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-018-workspace-ownership-and-version-metadata.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR018WorkspaceOwnershipAndVersionMetadata = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR018WorkspaceOwnershipAndVersionMetadata as default
};
