import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-014: Hardened Administrator Access with Scope and Role Mapping","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-014-admin-access-hardening-and-role-mapping.md","filePath":"decisions/ADR-014-admin-access-hardening-and-role-mapping.md"}');
const _sfc_main = { name: "decisions/ADR-014-admin-access-hardening-and-role-mapping.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-014-hardened-administrator-access-with-scope-and-role-mapping" tabindex="-1">ADR-014: Hardened Administrator Access with Scope and Role Mapping <a class="header-anchor" href="#adr-014-hardened-administrator-access-with-scope-and-role-mapping" aria-label="Permalink to &quot;ADR-014: Hardened Administrator Access with Scope and Role Mapping&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-07</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>In the pod lifecycle orchestrator, security constraints are enforced at the Model Context Protocol (MCP) server tool level and the HTTP/WebSocket routing proxy level. Historically:</p><ul><li>Administrators only needed the admin role (configured via <code>AUTH_ADMIN_ROLE</code>, defaulting to <code>&quot;admin&quot;</code> or <code>&quot;nogoo9-admin&quot;</code>) to bypass tenancy filtering and manage resources of other users.</li><li>Scope checks validated standard scopes (<code>&quot;nogoo9:read&quot;</code> / <code>&quot;nogoo9:write&quot;</code>), but there was no dedicated scope required for administrative endpoints.</li><li>Under v0.7.0, we parameterized default roles/scopes (migrating defaults to <code>viewer</code> for read, <code>user</code> for write, and <code>admin</code> for admin actions).</li></ul><p>We need to harden administrative operations to prevent privilege escalation. Specifically:</p><ul><li>Admin actions (listing all workspaces/pods, deleting resources of other users, spawning workspaces with custom owner subjects) must require both the admin role and a specific admin scope (<code>AUTH_REQUIRED_ADMIN_SCOPE</code>, default <code>&quot;nogoo9:admin&quot;</code>).</li><li>The admin scope should satisfy standard read and write scope checks so that admin clients do not need to request separate read/write scopes explicitly.</li><li>Tokens completely lacking a scope claim must bypass scope validation, allowing fallback role configuration to determine access.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Implement a hardened admin access policy and scope hierarchy in the authentication module:</p><ol><li><p><strong>Admin Scope Verification</strong>: Introduce <code>AUTH_REQUIRED_ADMIN_SCOPE</code> (defaulting to <code>&quot;nogoo9:admin&quot;</code>). Refactor <code>verifyAccessOrThrow</code> to validate both:</p><ul><li>The admin role (<code>AUTH_ADMIN_ROLE</code>) is present.</li><li>The admin scope (<code>AUTH_REQUIRED_ADMIN_SCOPE</code>) is present.</li></ul></li><li><p><strong>Scope Hierarchy Resolution</strong>: Update <code>hasRequiredScope</code> so that if the requested scope is a standard read/write scope, it returns <code>true</code> if the payload contains the configured admin scope.</p></li><li><p><strong>Scope Bypass for Scope-less Tokens</strong>: If the token payload contains no scope claim (e.g. <code>scope</code> or <code>scp</code> claim is missing, null, or empty), <code>hasRequiredScope</code> automatically returns <code>true</code>, allowing the role-check phase of validation to decide access.</p></li><li><p><strong>Integration with MCP Tools</strong>: Refactor all administrative authorization checks in pod tools (<code>src/mcp/pods.ts</code>) and spawner tools (<code>src/mcp/spawner.ts</code>) to use <code>verifyAccessOrThrow(jwtPayload, &quot;admin&quot;)</code>.</p></li><li><p><strong>Sandbox Infrastructure alignment</strong>: Add <code>&quot;nogoo9:admin&quot;</code> as a client scope in Keycloak configuration (<code>realm-configmap.yaml</code>), default client scopes, and define it in the MCP server deployment environment specs.</p></li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="relying-solely-on-oauth-scopes-for-admin-operations" tabindex="-1">Relying solely on OAuth Scopes for Admin operations <a class="header-anchor" href="#relying-solely-on-oauth-scopes-for-admin-operations" aria-label="Permalink to &quot;Relying solely on OAuth Scopes for Admin operations&quot;">​</a></h3><ul><li><strong>Pros</strong>: Cleaner separation from roles.</li><li><strong>Cons</strong>: Bypasses Keycloak role mappings which are preferred for group-based administration.</li><li><strong>Rejected</strong>: Requiring both role and scope provides defense-in-depth, matching standard delegated authorization patterns (where scopes denote client application capabilities and roles denote user authorization).</li></ul><h3 id="strict-disjunction-requiring-admin-client-to-possess-all-scopes" tabindex="-1">Strict Disjunction (Requiring admin client to possess all scopes) <a class="header-anchor" href="#strict-disjunction-requiring-admin-client-to-possess-all-scopes" aria-label="Permalink to &quot;Strict Disjunction (Requiring admin client to possess all scopes)&quot;">​</a></h3><ul><li><strong>Pros</strong>: Simple validation code.</li><li><strong>Cons</strong>: Administrative clients must explicitly specify <code>&quot;nogoo9:read nogoo9:write nogoo9:admin&quot;</code>, leading to verbose tokens and configuration.</li><li><strong>Rejected</strong>: A scope hierarchy where <code>&quot;nogoo9:admin&quot;</code> acts as a superset is cleaner and simplifies client integration.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Admin operations are secured against tokens that have only the admin role but standard read/write scopes.</li><li>Bypassing scope checks for scope-less tokens ensures backward compatibility for credentials that rely exclusively on role assignments.</li><li>Keycloak configurations, sandbox deployments, and E2E authentication scripts require updates to request and map the <code>&quot;nogoo9:admin&quot;</code> scope.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-014-admin-access-hardening-and-role-mapping.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR014AdminAccessHardeningAndRoleMapping = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR014AdminAccessHardeningAndRoleMapping as default
};
