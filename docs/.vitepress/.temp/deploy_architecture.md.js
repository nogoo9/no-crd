import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderAttr, ssrRenderSuspense, ssrRenderComponent, ssrRenderStyle } from "vue/server-renderer";
import { _ as _imports_1 } from "./architecture_diagram.BcRKCGwj.js";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"System Design & Core Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"deploy/architecture.md","filePath":"deploy/architecture.md"}');
const _sfc_main = { name: "deploy/architecture.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="system-design-core-architecture" tabindex="-1">System Design &amp; Core Architecture <a class="header-anchor" href="#system-design-core-architecture" aria-label="Permalink to &quot;System Design &amp; Core Architecture&quot;">​</a></h1><p><code>@nogoo9/no-crd</code> is designed to be hosted in a Kubernetes cluster as a multi-tenant platform service. It empowers developers and AI agents to spin up containerized workspaces dynamically and route traffic securely to those containers without exposing cluster-level operators or custom resource definitions (CRDs).</p><p>This guide walks you through the end-to-end implementation details of the routing proxy, the Backend for Frontend (BFF) authentication flows, the spawner&#39;s pod transformation mechanics, and the cluster-wide session key resolution system.</p><hr><h2 id="🏗️-architecture-topology" tabindex="-1">🏗️ Architecture Topology <a class="header-anchor" href="#🏗️-architecture-topology" aria-label="Permalink to &quot;🏗️ Architecture Topology&quot;">​</a></h2><p>The service sits between users (or their IDE-based AI agents) and the Kubernetes API server, acting as both an MCP server gateway and a reverse routing proxy:</p><p><img${ssrRenderAttr("src", _imports_1)} alt="Architecture Topology"></p><h3 id="key-internal-components" tabindex="-1">Key Internal Components <a class="header-anchor" href="#key-internal-components" aria-label="Permalink to &quot;Key Internal Components&quot;">​</a></h3><ol><li><strong>MCP Server Gateway</strong>: Processes incoming tool requests via Stdio or SSE transport.</li><li><strong>HTTP Routing Proxy</strong>: A built-in Fastify-based proxy that maps incoming subpath traffic (<code>/route/&lt;workspace-id&gt;/*</code>) directly to the corresponding Pod IP inside the cluster.</li><li><strong>Auth Engine (RBAC/ABAC)</strong>: Validates OpenID Connect (OIDC) JWT claims, matches scopes/roles, and maps identity to Kubernetes namespaces.</li><li><strong>Workspace Spawner</strong>: Interacts with the Kubernetes API to orchestrate Pod lifecycles, inject init-containers, mount volumes, and manage ServiceAccounts.</li></ol><hr><h2 id="🔌-end-to-end-workspace-proxying" tabindex="-1">🔌 End-to-End Workspace Proxying <a class="header-anchor" href="#🔌-end-to-end-workspace-proxying" aria-label="Permalink to &quot;🔌 End-to-End Workspace Proxying&quot;">​</a></h2><p>The reverse proxy allows direct web or WebSocket access to containerized workspace interfaces (e.g. terminals, Obsidian, or VNC desktops) via the URL path:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>http://&lt;mcp-gateway-host&gt;/route/&lt;workspace-id&gt;/&lt;subpath&gt;</span></span></code></pre></div><h3 id="_1-request-interception-authentication-guard" tabindex="-1">1. Request Interception &amp; Authentication Guard <a class="header-anchor" href="#_1-request-interception-authentication-guard" aria-label="Permalink to &quot;1. Request Interception &amp; Authentication Guard&quot;">​</a></h3><p>The proxy flow is managed by <code>proxyPreHandler</code> in [auth.ts](file:///home/eterna2/github/nogoo9-no-crd/src/server/auth.ts#L464) and registered in [proxy.ts](file:///home/eterna2/github/nogoo9-no-crd/src/server/routes/proxy.ts#L396).</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-59",
        class: "mermaid",
        graph: "sequenceDiagram%0A%20%20%20%20autonumber%0A%20%20%20%20actor%20User%20as%20Browser%20%2F%20Client%0A%20%20%20%20participant%20Proxy%20as%20Proxy%20Gateway%0A%20%20%20%20participant%20K8s%20as%20Kubernetes%20API%0A%20%20%20%20participant%20Pod%20as%20Workspace%20Pod%0A%0A%20%20%20%20User-%3E%3EProxy%3A%20GET%20%2Froute%2Fworkspace-abc%2Findex.html%0A%20%20%20%20Proxy-%3E%3EK8s%3A%20Query%20Pod%20with%20labels%20Type%3Dworkspace%2C%20ID%3Dworkspace-abc%0A%20%20%20%20alt%20Pod%20not%20found%0A%20%20%20%20%20%20%20%20Proxy--%3E%3EUser%3A%20404%20Not%20Found%0A%20%20%20%20else%20Pod%20found%20(Phase%20!%3D%20Running%20or%20PodIP%20missing)%0A%20%20%20%20%20%20%20%20Proxy--%3E%3EUser%3A%20503%20Service%20Unavailable%20(Waiting%20to%20boot)%0A%20%20%20%20end%0A%20%20%20%20Proxy-%3E%3EProxy%3A%20Verify%20User%20JWT%2FCookies%20against%20Pod%20Owner%20(nogoo9%2Fuser-sub)%0A%20%20%20%20alt%20Owner%20mismatch%20or%20token%20invalid%0A%20%20%20%20%20%20%20%20Proxy--%3E%3EUser%3A%20403%20Forbidden%0A%20%20%20%20else%20Verification%20successful%0A%20%20%20%20%20%20%20%20Proxy-%3E%3EPod%3A%20Forward%20request%20to%20Pod%20IP%20(port%203000)%0A%20%20%20%20%20%20%20%20Pod--%3E%3EProxy%3A%20Return%20response%20stream%0A%20%20%20%20%20%20%20%20Proxy-%3E%3EUser%3A%20Pipe%20response%20(set%20nocr_token%20cookie)%0A%20%20%20%20end%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h3 id="_2-token-bootstrap-path-scoped-cookies" tabindex="-1">2. Token Bootstrap &amp; Path-Scoped Cookies <a class="header-anchor" href="#_2-token-bootstrap-path-scoped-cookies" aria-label="Permalink to &quot;2. Token Bootstrap &amp; Path-Scoped Cookies&quot;">​</a></h3><p>Because standard web elements (like <code>&lt;a&gt;</code> links or <code>&lt;iframe&gt;</code> tags) cannot send custom authorization headers, the gateway implements a path-scoped cookie bootstrapping protocol:</p><ol><li><strong>Redirect Bootstrap</strong>: When a user launches a workspace, the dashboard redirects them to <code>/route/&lt;workspace-id&gt;/?token=&lt;JWT_TOKEN&gt;</code>.</li><li><strong>Cookie Issuance (<code>nocr_token</code>)</strong>: The proxy intercepts this request, validates the token, and writes an HttpOnly, path-scoped cookie:<div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Set-Cookie</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nocr_token=&lt;JWT_TOKEN&gt;; Path=/route/&lt;workspace-id&gt;/; SameSite=Lax; HttpOnly; Max-Age=&lt;ttl&gt;</span></span></code></pre></div>The <code>Max-Age</code> is derived from the JWT <code>exp</code> claim (remaining seconds), falling back to the <code>PROXY_TOKEN_COOKIE_TTL</code> config (default 86400s).</li><li><strong>Path-Scoped Exclusivity</strong>: Because the cookie&#39;s path is strictly limited to <code>/route/&lt;workspace-id&gt;/</code>, other workspaces cannot see or retrieve this token, preventing cross-workspace token leaks.</li><li><strong>Header Injections</strong>: For all requests flowing through to the upstream pod, the proxy extracts user details from the active credentials and injects these headers into the request: <ul><li><code>x-user-sub</code>: The OIDC subject of the owner.</li><li><code>x-user-roles</code>: Comma-separated list of the user&#39;s OIDC roles.</li><li><code>x-workspace-jwt</code>: The raw JWT token (if <code>AUTH_INJECT_WORKSPACE_JWT</code> is <code>true</code>).</li><li><code>authorization</code>: Rewritten as <code>Bearer &lt;JWT_TOKEN&gt;</code>.</li></ul></li></ol><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p><strong>WebSocket Limitations under Bun</strong>: Due to a socket write regression in Bun&#39;s Node compatibility layer, upgraded WebSocket connections will drop data. If your workspaces require WebSockets (e.g. VS Code, VNC, web terminals), you <strong>must</strong> run the gateway using <strong>Node.js</strong> (e.g. <code>npx tsx src/server-entry.ts</code>).</p></div><hr><h2 id="🔒-backend-for-frontend-bff-auth-flows" tabindex="-1">🔒 Backend for Frontend (BFF) &amp; Auth Flows <a class="header-anchor" href="#🔒-backend-for-frontend-bff-auth-flows" aria-label="Permalink to &quot;🔒 Backend for Frontend (BFF) &amp; Auth Flows&quot;">​</a></h2><p>The built-in visual React Dashboard in [app.tsx](file:///home/eterna2/github/nogoo9-no-crd/src/ui/app.tsx) handles user logins and active token maintenance.</p><h3 id="authentication-handshake" tabindex="-1">Authentication Handshake <a class="header-anchor" href="#authentication-handshake" aria-label="Permalink to &quot;Authentication Handshake&quot;">​</a></h3>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-129",
        class: "mermaid",
        graph: "sequenceDiagram%0A%20%20%20%20autonumber%0A%20%20%20%20actor%20User%20as%20Developer%20%2F%20Agent%0A%20%20%20%20participant%20UI%20as%20React%20Dashboard%20(app.tsx)%0A%20%20%20%20participant%20BFF%20as%20Gateway%20Backend%20(auth.ts)%0A%20%20%20%20participant%20IdP%20as%20Identity%20Provider%20(e.g.%20Keycloak)%0A%0A%20%20%20%20User-%3E%3EUI%3A%20Click%20Login%0A%20%20%20%20UI-%3E%3EIdP%3A%20Authorization%20request%20(PKCE%20flow%20redirect)%0A%20%20%20%20IdP--%3E%3EUI%3A%20Callback%20with%20Auth%20Code%20(%3Fcode%3D...)%0A%20%20%20%20UI-%3E%3EBFF%3A%20POST%20%2Fapi%2Fauth%2Ftoken%20%7B%20code%2C%20verifier%20%7D%0A%20%20%20%20BFF-%3E%3EIdP%3A%20Exchange%20Auth%20Code%20for%20Access%20%26%20Refresh%20Tokens%0A%20%20%20%20IdP--%3E%3EBFF%3A%20Access%20Token%20%2B%20Refresh%20Token%0A%20%20%20%20BFF-%3E%3EBFF%3A%20Encrypt%20Refresh%20Token%20(AES-256-GCM)%0A%20%20%20%20BFF--%3E%3EUI%3A%20Return%20Access%20Token%20%2B%20Set-Cookie%3A%20nocr_refresh%20(HttpOnly)%20%26%20nocr_sess%20(HMAC-signed)%0A%20%20%20%20UI-%3E%3EUI%3A%20Store%20Access%20Token%20in-memory%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h3 id="token-maintenance-proactive-silent-refresh" tabindex="-1">Token Maintenance &amp; Proactive Silent Refresh <a class="header-anchor" href="#token-maintenance-proactive-silent-refresh" aria-label="Permalink to &quot;Token Maintenance &amp; Proactive Silent Refresh&quot;">​</a></h3><ol><li><strong>In-Memory JWT</strong>: The OIDC Access Token (<code>access_token</code>) is stored strictly in client-side Javascript.</li><li><strong>Encrypted Refresh Cookie (<code>nocr_refresh</code>)</strong>: The OIDC Refresh Token is encrypted via <strong>AES-256-GCM</strong> using a key derived from the gateway&#39;s session secret (preventing client-side JS access) and saved as a secure, HttpOnly cookie.</li><li><strong>Transparent Gateway Refresh</strong>: If a workspace request arrives with an expired access token or without one, the gateway&#39;s auth hook in [auth.ts](file:///home/eterna2/github/nogoo9-no-crd/src/server/auth.ts#L244) detects the <code>nocr_refresh</code> cookie. It decrypts it, exchanges it with the Identity Provider (IdP) for a fresh access token, and transparently updates the cookies (<code>nocr_sess</code> and <code>nocr_refresh</code>) without interrupting the user session.</li></ol><h3 id="cookie-ttl-alignment" tabindex="-1">Cookie TTL Alignment <a class="header-anchor" href="#cookie-ttl-alignment" aria-label="Permalink to &quot;Cookie TTL Alignment&quot;">​</a></h3><p>Each cookie&#39;s <code>Max-Age</code> is dynamically derived from the actual token lifetime rather than using a static value:</p><table tabindex="0"><thead><tr><th>Cookie</th><th>TTL Source</th><th>Config Fallback</th><th>Default</th></tr></thead><tbody><tr><td><code>nocr_token</code></td><td>JWT <code>exp</code> claim (remaining seconds)</td><td><code>PROXY_TOKEN_COOKIE_TTL</code></td><td>86400 (24h)</td></tr><tr><td><code>nocr_refresh</code></td><td>IdP&#39;s <code>refresh_expires_in</code> response field</td><td><code>PROXY_REFRESH_COOKIE_TTL</code></td><td>604800 (7d)</td></tr><tr><td><code>nocr_sess</code></td><td>Always uses config value</td><td><code>PROXY_SESSION_TTL</code></td><td>1800 (30m)</td></tr></tbody></table><p>This alignment prevents a common mismatch where the cookie outlives the token it contains — which would cause the gateway to repeatedly send expired tokens to the IdP on every request until the cookie naturally expires.</p><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>If your Identity Provider does not return <code>refresh_expires_in</code> in its token response (e.g. Auth0, some Okta configurations), set <code>PROXY_REFRESH_COOKIE_TTL</code> to match your IdP&#39;s refresh token lifetime. Keycloak returns this field automatically.</p></div><h3 id="stale-cookie-cleanup" tabindex="-1">Stale Cookie Cleanup <a class="header-anchor" href="#stale-cookie-cleanup" aria-label="Permalink to &quot;Stale Cookie Cleanup&quot;">​</a></h3><p>When the gateway attempts a transparent refresh and the IdP <strong>rejects</strong> the refresh token (e.g. <code>invalid_grant</code> due to token expiration or revocation), the gateway immediately clears the stale <code>nocr_refresh</code> cookie:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Set-Cookie</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nocr_refresh=; Path=/; SameSite=Lax; HttpOnly; Max-Age=0</span></span></code></pre></div><p>Without this cleanup, every subsequent request would make a futile round-trip to the IdP that&#39;s guaranteed to fail, adding unnecessary latency and IdP load. After clearing, the user is redirected to re-authenticate via the normal SSO flow.</p><h3 id="refresh-token-rotation-singleflight-deduplication" tabindex="-1">Refresh Token Rotation &amp; Singleflight Deduplication <a class="header-anchor" href="#refresh-token-rotation-singleflight-deduplication" aria-label="Permalink to &quot;Refresh Token Rotation &amp; Singleflight Deduplication&quot;">​</a></h3><p>The gateway fully supports <strong>refresh token rotation</strong> — where the IdP issues a new refresh token with every refresh grant and invalidates the old one. This is a common security hardening (e.g. Keycloak&#39;s &quot;Revoke Refresh Token&quot; setting).</p><p>The challenge with rotation is <strong>concurrent requests</strong>: if multiple browser requests arrive simultaneously with the same expired access token, they would all try to use the same refresh token. With strict rotation, only the first succeeds — the rest would find the token already revoked.</p><p>To prevent this, the gateway implements a <strong>singleflight pattern</strong> in [auth.ts](file:///home/eterna2/github/nogoo9-no-crd/src/server/auth.ts#L118):</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-248",
        class: "mermaid",
        graph: "sequenceDiagram%0A%20%20%20%20autonumber%0A%20%20%20%20participant%20A%20as%20Request%20A%0A%20%20%20%20participant%20B%20as%20Request%20B%0A%20%20%20%20participant%20C%20as%20Request%20C%0A%20%20%20%20participant%20GW%20as%20Gateway%20(singleflight)%0A%20%20%20%20participant%20IdP%20as%20Identity%20Provider%0A%0A%20%20%20%20A-%3E%3EGW%3A%20refresh(token_v1)%0A%20%20%20%20Note%20over%20GW%3A%20No%20in-flight%20request%20%E2%86%92%20start%20refresh%0A%20%20%20%20GW-%3E%3EIdP%3A%20POST%20%2Ftoken%20(grant_type%3Drefresh_token)%0A%20%20%20%20B-%3E%3EGW%3A%20refresh(token_v1)%0A%20%20%20%20Note%20over%20GW%3A%20In-flight%20hit%20%E2%86%92%20coalesce%0A%20%20%20%20C-%3E%3EGW%3A%20refresh(token_v1)%0A%20%20%20%20Note%20over%20GW%3A%20In-flight%20hit%20%E2%86%92%20coalesce%0A%20%20%20%20IdP--%3E%3EGW%3A%20token_v2%20(rotated)%0A%20%20%20%20GW--%3E%3EA%3A%20token_v2%0A%20%20%20%20GW--%3E%3EB%3A%20token_v2%20(same%20result)%0A%20%20%20%20GW--%3E%3EC%3A%20token_v2%20(same%20result)%0A%20%20%20%20Note%20over%20GW%3A%20Cleanup%20in-flight%20entry%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>All concurrent requests for the same refresh token share a single IdP round-trip. The in-flight map is keyed by the decrypted refresh token and automatically cleaned up after resolution or rejection.</p><blockquote><blockquote><blockquote><blockquote><blockquote><blockquote><blockquote><p>8e23f37 (fix(auth): align cookie TTLs with token lifetimes)</p></blockquote></blockquote></blockquote></blockquote></blockquote></blockquote></blockquote><hr><h2 id="☸️-template-to-pod-transformation-engine" tabindex="-1">☸️ Template-to-Pod Transformation Engine <a class="header-anchor" href="#☸️-template-to-pod-transformation-engine" aria-label="Permalink to &quot;☸️ Template-to-Pod Transformation Engine&quot;">​</a></h2><p>When a user calls <code>spawn_workspace</code> (implemented in [spawner.ts](file:///home/eterna2/github/nogoo9-no-crd/src/mcp/spawner.ts#L938)), the spawner executes a multi-phase compilation flow to construct the final Kubernetes <code>Pod</code> manifest:</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-276",
        class: "mermaid",
        graph: "graph%20TD%0A%20%20%20%20TemplateRef%5B%221.%20Read%20Template%20Source%20(ConfigMap%20Spec%20or%20local%20file)%22%5D%20--%3E%20Interpolate%5B%222.%20Interpolate%20Variables%20(%24%7B%7Buser%7D%7D%2C%20%24%7B%7Bworkspace_id%7D%7D)%22%5D%0A%20%20%20%20Interpolate%20--%3E%20ApplyAnnotations%5B%223.%20Apply%20Spawner%20Annotations%22%5D%0A%20%20%20%20ApplyAnnotations%20--%3E%20SA_Check%7B%224.%20Check%20'nogoo9%2Fiam-role-arn'%22%7D%0A%20%20%20%20SA_Check%20--%20Yes%20--%3E%20ProvisionSA%5B%225.%20Provision%20ServiceAccount%20%2B%20Annotation%22%5D%0A%20%20%20%20SA_Check%20--%20No%20--%3E%20StampMeta%5B%226.%20Stamp%20Metadata%20Labels%20%26%20Annotations%22%5D%0A%20%20%20%20ProvisionSA%20--%3E%20StampMeta%0A%20%20%20%20StampMeta%20--%3E%20Deploy%5B%227.%20Post%20Pod%20Manifest%20(createNamespacedPod)%22%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h3 id="spawner-annotation-mutations" tabindex="-1">Spawner Annotation Mutations <a class="header-anchor" href="#spawner-annotation-mutations" aria-label="Permalink to &quot;Spawner Annotation Mutations&quot;">​</a></h3><p>During step 3, the engine in [annotations.ts](file:///home/eterna2/github/nogoo9-no-crd/src/k8s/annotations.ts#L13) reads specific template annotations to morph the pod specification:</p><ul><li><strong>Init Container Injection (<code>nogoo9/init-image</code> &amp; <code>nogoo9/init-command</code>)</strong>: Appends a dynamic container named <code>spawner-init</code> to <code>initContainers</code>. If <code>nogoo9/init-share-volumes</code> is not explicitly set to <code>&quot;false&quot;</code>, it inherits all volume mounts of the primary container, letting you run git clones or asset downloads before the main app boots.</li><li><strong>Pre-Stop Cleanups (<code>nogoo9/pre-stop-command</code>)</strong>: Allows graceful state synchronization. <ul><li><em>Inline mode (default)</em>: Inserts a <code>lifecycle.preStop.exec</code> command directly into the primary container.</li><li><em>Sidecar mode (<code>nogoo9/pre-stop-sidecar-image</code>)</em>: Adds a companion container named <code>spawner-sidecar</code>. The pre-stop command is attached to the sidecar, which sleeps infinitely during standard execution but triggers its cleanup execution upon container termination.</li></ul></li><li><strong>AWS IAM Role Integration (<code>nogoo9/iam-role-arn</code>)</strong>: When specified, the spawner automatically creates a Kubernetes <code>ServiceAccount</code> stamped as:<div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">apiVersion</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">v1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">ServiceAccount</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">metadata</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">ws-sa-&lt;workspace-id&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  annotations</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    eks.amazonaws.com/role-arn</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&lt;role-arn-value&gt;</span></span></code></pre></div>The pod spec&#39;s <code>serviceAccountName</code> is then rewritten to bind this account, mapping AWS IAM credentials natively via EKS IRSA.</li></ul><hr><h2 id="🔑-session-secret-negotiation-cluster-peer-discovery" tabindex="-1">🔑 Session Secret Negotiation &amp; Cluster Peer Discovery <a class="header-anchor" href="#🔑-session-secret-negotiation-cluster-peer-discovery" aria-label="Permalink to &quot;🔑 Session Secret Negotiation &amp; Cluster Peer Discovery&quot;">​</a></h2><p>All session cookies (<code>nocr_sess</code> and <code>nocr_refresh</code>) are signed/encrypted via HMAC-SHA256 and AES-256-GCM. To support horizontal pod scaling (multi-replica gateway setups) without requiring database backends, replicas dynamically negotiate and share a unified session key on boot.</p><h3 id="the-4-step-resolution-cascade" tabindex="-1">The 4-Step Resolution Cascade <a class="header-anchor" href="#the-4-step-resolution-cascade" aria-label="Permalink to &quot;The 4-Step Resolution Cascade&quot;">​</a></h3><p>The resolution flow resides in <code>resolveSessionSecret</code> inside [session.ts](file:///home/eterna2/github/nogoo9-no-crd/src/k8s/session.ts#L46):</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-329",
        class: "mermaid",
        graph: "graph%20TD%0A%20%20%20%20Start%5B%22Boot%20Process%22%5D%20--%3E%20EnvCheck%7B%221.%20Check%20Environment%20Variables%22%7D%0A%20%20%20%20EnvCheck%20--%20%22PROXY_SESSION_SECRET%20%2F%20JWT_SECRET%20set%22%20--%3E%20AdoptEnv%5B%22Adopt%20Env%20Key%22%5D%0A%20%20%20%20EnvCheck%20--%20Not%20set%20--%3E%20SecretCheck%7B%222.%20Check%20Kubernetes%20Secret%20(nogoo9-session-key)%22%7D%0A%20%20%20%20SecretCheck%20--%20%22Secret%20exists%20%2F%20RBAC%20writable%22%20--%3E%20AdoptSecret%5B%22Adopt%20Secret%20Key%20%2F%20Create%20if%20missing%22%5D%0A%20%20%20%20SecretCheck%20--%20%22403%20Forbidden%20(restricted%20RBAC)%22%20--%3E%20PeerCheck%7B%223.%20Peer%20Discovery%20(Oldest%20pod%20HTTP%20%2Finternal%2Fsession-key)%22%7D%0A%20%20%20%20PeerCheck%20--%20%22Successfully%20queried%20peer%20pod%22%20--%3E%20AdoptPeer%5B%22Adopt%20Sibling%20Key%22%5D%0A%20%20%20%20PeerCheck%20--%20%22No%20peers%20%2F%20Connection%20timeout%22%20--%3E%20InMemory%5B%224.%20In-Memory%20Random%20Key%20(Single-replica%20fallback)%22%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h3 id="eager-blocking-healthchecks" tabindex="-1">Eager Blocking Healthchecks <a class="header-anchor" href="#eager-blocking-healthchecks" aria-label="Permalink to &quot;Eager Blocking Healthchecks&quot;">​</a></h3><p>Until <code>resolveSessionSecret</code> resolves a key, the <code>/healthz</code> and <code>/mcp/healthz</code> endpoints respond with a <strong><code>503 Service Unavailable</code></strong> status:</p><ol><li>Under Kubernetes, a booting pod remains in the &quot;Unhealthy&quot; ingress pool.</li><li>The ingress router blocks external user traffic from routing to this replica.</li><li>Once the key is resolved (either successfully reading from a secret, querying a peer, or falling back to in-memory generation), <code>/healthz</code> switches to <strong><code>200 OK</code></strong>.</li><li>The pod shifts to &quot;Ready&quot; status and is safely added to the traffic rotation.</li></ol><hr><h2 id="🔍-troubleshooting-debugging" tabindex="-1">🔍 Troubleshooting &amp; Debugging <a class="header-anchor" href="#🔍-troubleshooting-debugging" aria-label="Permalink to &quot;🔍 Troubleshooting &amp; Debugging&quot;">​</a></h2><p>If workspaces fail to route or compile, use this checklist to narrow down the root cause:</p><h3 id="_1-ingress-and-cookie-routing-loop" tabindex="-1">1. Ingress and Cookie Routing Loop <a class="header-anchor" href="#_1-ingress-and-cookie-routing-loop" aria-label="Permalink to &quot;1. Ingress and Cookie Routing Loop&quot;">​</a></h3><p>If OIDC redirects loop infinitely, ensure that:</p><ul><li><code>BASE_URL</code> matches your ingress routing prefix.</li><li>The browser cookie path rules align. Cookies like <code>nocr_token</code> are path-scoped. If the gateway is running behind a proxy subpath (e.g. <code>/gateway/no-crd</code>), the cookie path prefix is adjusted accordingly to prevent the browser from omitting the token on subsequent resource calls (see [ADR-011](file:///home/eterna2/github/nogoo9-no-crd/docs/decisions/ADR-011-ui-base-url-and-cookie-path-consistency.md)).</li></ul><h3 id="_2-session-secret-adoption-failure" tabindex="-1">2. Session Secret Adoption Failure <a class="header-anchor" href="#_2-session-secret-adoption-failure" aria-label="Permalink to &quot;2. Session Secret Adoption Failure&quot;">​</a></h3><p>Check pod logs for: <code>&quot;Session key generated in-memory. Multi-replica deployments should set PROXY_SESSION_SECRET...&quot;</code> If you see this warning across multiple replicas, they do not share a session key. A cookie signed by replica A will be rejected as corrupted by replica B. Set <code>PROXY_SESSION_SECRET</code> explicitly in the container deployment <code>env</code> list.</p><h3 id="_3-permission-errors-during-pod-spawning" tabindex="-1">3. Permission Errors during Pod Spawning <a class="header-anchor" href="#_3-permission-errors-during-pod-spawning" aria-label="Permalink to &quot;3. Permission Errors during Pod Spawning&quot;">​</a></h3><p>If spawning fails, run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">kubectl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> auth</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> can-i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pods</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --as=system:serviceaccount:</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">namespace</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">:nogoo-mcp</span></span></code></pre></div><p>The server checks RBAC permissions eagerly on startup. Ensure the service account running the gateway pod has the permissions described in the <a href="./rbac-permissions.html">RBAC Permissions Guide</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("deploy/architecture.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const architecture = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  architecture as default
};
