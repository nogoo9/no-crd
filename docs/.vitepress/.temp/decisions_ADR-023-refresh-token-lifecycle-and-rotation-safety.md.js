import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-023: Dynamic Cookie TTL Alignment, Stale Cookie Cleanup, and Singleflight Refresh Token Rotation Safety","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-023-refresh-token-lifecycle-and-rotation-safety.md","filePath":"decisions/ADR-023-refresh-token-lifecycle-and-rotation-safety.md"}');
const _sfc_main = { name: "decisions/ADR-023-refresh-token-lifecycle-and-rotation-safety.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-023-dynamic-cookie-ttl-alignment-stale-cookie-cleanup-and-singleflight-refresh-token-rotation-safety" tabindex="-1">ADR-023: Dynamic Cookie TTL Alignment, Stale Cookie Cleanup, and Singleflight Refresh Token Rotation Safety <a class="header-anchor" href="#adr-023-dynamic-cookie-ttl-alignment-stale-cookie-cleanup-and-singleflight-refresh-token-rotation-safety" aria-label="Permalink to &quot;ADR-023: Dynamic Cookie TTL Alignment, Stale Cookie Cleanup, and Singleflight Refresh Token Rotation Safety&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-06-14</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>We use OIDC/OAuth authentication for users, storing the access token in a path-scoped cookie (<code>nocr_token</code>) and the encrypted refresh token in a root-scoped cookie (<code>nocr_refresh</code>).</p><p>Prior to this decision, the authentication flow suffered from three distinct issues:</p><ol><li><strong>Static Cookie TTLs</strong>: Both the <code>nocr_token</code> and <code>nocr_refresh</code> cookies had static/hardcoded <code>Max-Age</code> values (e.g., 86400 seconds for access token, 604800 seconds for refresh token). When the actual tokens issued by the Identity Provider (IdP) had different lifetimes (e.g., access tokens expiring in 5 minutes, or refresh tokens expiring in 24 hours), the browser would keep expired cookies or drop valid ones prematurely.</li><li><strong>Futile Client/Server Refresh Loops</strong>: When a refresh token expired or was invalidated (e.g., OIDC logout or IdP session termination), the IdP would return an error (such as <code>invalid_grant</code>). The proxy server would log the failure but leave the <code>nocr_refresh</code> cookie in the user&#39;s browser. The client SPA would continually invoke the refresh endpoint on subsequent API calls, causing repeated futile round-trips to the IdP.</li><li><strong>Refresh Token Rotation Race Conditions</strong>: To protect against token theft, many IdPs enforce strict <strong>Refresh Token Rotation (RTR)</strong>, where using a refresh token yields a new refresh token and invalidates the old one. If a user has multiple concurrent requests (such as a dashboard loading multiple workspace widgets simultaneously), several requests would trigger transparent refreshes at the same time using the same old refresh token. The first request to reach the IdP would succeed and rotate the token, but the subsequent concurrent requests would fail with <code>invalid_grant</code> (since they used the now-invalidated old token), resulting in immediate session termination.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Implement three cohesive updates to the session/cookie lifecycle:</p><ol><li><strong>Dynamic Cookie TTLs</strong>: <ul><li>Extract the access token (<code>nocr_token</code>) Max-Age dynamically from the JWT <code>exp</code> claim. If unavailable or invalid, fall back to a configurable default (<code>PROXY_TOKEN_COOKIE_TTL</code>).</li><li>Extract the refresh token (<code>nocr_refresh</code>) Max-Age dynamically from the IdP token response&#39;s <code>refresh_expires_in</code> claim. If unavailable, fall back to a configurable default (<code>PROXY_REFRESH_COOKIE_TTL</code>).</li></ul></li><li><strong>Stale Cookie Cleanup</strong>: <ul><li>When token refresh fails, explicitly clear the <code>nocr_refresh</code> cookie in the response (<code>Max-Age=0</code> and an expired date). This terminates the futile loop and forces the client to re-authenticate cleanly.</li></ul></li><li><strong>Singleflight Deduplication</strong>: <ul><li>Wrap the OIDC refresh exchange in a singleflight pattern. Maintain a map of active refresh promises (<code>inflightRefreshes: Map&lt;string, Promise&lt;RefreshResult&gt;&gt;</code>) keyed by the decrypted refresh token.</li><li>Concurrent refresh requests for the same token coalesce into a single promise, executing exactly one IdP token exchange. When the exchange completes, all waiting requests receive the same rotated credentials and matching cookies, avoiding rotation race conditions.</li></ul></li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="mutex-locking" tabindex="-1">Mutex Locking <a class="header-anchor" href="#mutex-locking" aria-label="Permalink to &quot;Mutex Locking&quot;">​</a></h3><ul><li><strong>Rejected</strong>: Mutex locking blocks subsequent requests, causing them to wait sequentially and potentially trigger another refresh with the <em>new</em> token sequentially, which is unnecessary and introduces latency/contention. Singleflight is simpler and faster since concurrent requests just share the result of the single in-flight OIDC request.</li></ul><h3 id="long-lived-static-ttls" tabindex="-1">Long-Lived Static TTLs <a class="header-anchor" href="#long-lived-static-ttls" aria-label="Permalink to &quot;Long-Lived Static TTLs&quot;">​</a></h3><ul><li><strong>Rejected</strong>: Setting long static TTLs violates the principle of least privilege, exposes the application to session replay risks if tokens are revoked early, and does not align with the OIDC provider&#39;s actual session configuration.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Resiliency during strict refresh token rotation is greatly improved. Concurrent client requests no longer trigger authentication failures.</li><li>Reduced load on the OIDC Identity Provider since duplicate refresh requests are deduplicated.</li><li>Self-healing session cleanup: clients are immediately prompted to re-authenticate when their refresh token becomes invalid, rather than getting stuck in a loop.</li><li>Precise cookie lifetime management matching OIDC provider session durations.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-023-refresh-token-lifecycle-and-rotation-safety.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR023RefreshTokenLifecycleAndRotationSafety = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR023RefreshTokenLifecycleAndRotationSafety as default
};
