import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-004: Three-Source Theme Merge with Built-In Fallback","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-004-three-source-theme-merge.md","filePath":"decisions/ADR-004-three-source-theme-merge.md"}');
const _sfc_main = { name: "decisions/ADR-004-three-source-theme-merge.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-004-three-source-theme-merge-with-built-in-fallback" tabindex="-1">ADR-004: Three-Source Theme Merge with Built-In Fallback <a class="header-anchor" href="#adr-004-three-source-theme-merge-with-built-in-fallback" aria-label="Permalink to &quot;ADR-004: Three-Source Theme Merge with Built-In Fallback&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-28</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The embedded UI serves themes via <code>/api/themes</code> (list) and <code>/api/themes/:themeId</code> (retrieve CSS). Until v0.3.x, themes were loaded from either a Kubernetes ConfigMap (<code>THEMES_CONFIGMAP</code>) or a local directory (<code>THEMES_DIR</code>), but never both, and there was no built-in default beyond the hardcoded &quot;Claude&quot; theme.</p><p>In v0.4.0, we ship 10 pre-built CSS themes with the npm package (in <code>themes/</code>). We need a merge strategy that:</p><ul><li>Gives operators full control (ConfigMap and custom dir override built-in themes)</li><li>Ensures every deployment has a good default set of themes even without configuration</li><li>Allows operators to <strong>replace</strong> a built-in theme with their own version (same id)</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Merge themes from <strong>three sources</strong> in priority order, deduplicated by theme id:</p><ol><li><strong>ConfigMap</strong> (highest priority) — via <code>THEMES_CONFIGMAP</code> env var</li><li><strong>Custom directory</strong> — via <code>THEMES_DIR</code> env var</li><li><strong>Built-in directory</strong> (lowest priority) — resolved at startup from the package&#39;s <code>themes/</code> directory</li></ol><p>When listing, each source is scanned in order. If a theme id has already been seen from a higher-priority source, the lower-priority version is skipped. When retrieving a single theme, sources are checked in the same order; the first match wins.</p><h3 id="priority-rationale" tabindex="-1">Priority rationale <a class="header-anchor" href="#priority-rationale" aria-label="Permalink to &quot;Priority rationale&quot;">​</a></h3><ul><li>ConfigMap is highest because it&#39;s the standard k8s-native configuration mechanism and can be updated without redeploying the pod</li><li>Custom directory is second because it represents operator customization baked into the image or mounted at runtime</li><li>Built-in is last because it&#39;s the package default that should be overridable</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="single-source-only-existing-behavior" tabindex="-1">Single source only (existing behavior) <a class="header-anchor" href="#single-source-only-existing-behavior" aria-label="Permalink to &quot;Single source only (existing behavior)&quot;">​</a></h3><ul><li>Pros: Simplest</li><li>Cons: No built-in themes; every deployment needs explicit theme configuration</li><li>Rejected: Poor out-of-the-box experience</li></ul><h3 id="merge-with-no-deduplication" tabindex="-1">Merge with no deduplication <a class="header-anchor" href="#merge-with-no-deduplication" aria-label="Permalink to &quot;Merge with no deduplication&quot;">​</a></h3><ul><li>Pros: Shows all themes from all sources</li><li>Cons: Confusing when multiple themes share the same id but have different CSS</li><li>Rejected: Users would see duplicates in the theme picker</li></ul><h3 id="explicit-override-flag-on-themes" tabindex="-1">Explicit &quot;override&quot; flag on themes <a class="header-anchor" href="#explicit-override-flag-on-themes" aria-label="Permalink to &quot;Explicit &quot;override&quot; flag on themes&quot;">​</a></h3><ul><li>Pros: More control over which themes to replace</li><li>Cons: Over-engineered; id-based deduplication with source priority achieves the same result naturally</li><li>Rejected: Unnecessary complexity</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Every deployment ships with 10 themes out of the box (no configuration needed)</li><li>Operators can override any built-in theme by placing a <code>.css</code> file with the same id in <code>THEMES_DIR</code> or ConfigMap</li><li>The <code>config.ui.builtinThemesDir</code> is resolved at startup via <code>resolveBuiltinDir()</code>, which walks up from <code>__dirname</code> to find the package&#39;s <code>themes/</code> directory</li><li>Built-in themes are included in the npm package&#39;s <code>files</code> field</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-004-three-source-theme-merge.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR004ThreeSourceThemeMerge = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR004ThreeSourceThemeMerge as default
};
