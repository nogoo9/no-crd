import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR-009: Eager MCP Server Initialization with K8s Health Check","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/ADR-009-eager-startup-health-check.md","filePath":"decisions/ADR-009-eager-startup-health-check.md"}');
const _sfc_main = { name: "decisions/ADR-009-eager-startup-health-check.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-009-eager-mcp-server-initialization-with-k8s-health-check" tabindex="-1">ADR-009: Eager MCP Server Initialization with K8s Health Check <a class="header-anchor" href="#adr-009-eager-mcp-server-initialization-with-k8s-health-check" aria-label="Permalink to &quot;ADR-009: Eager MCP Server Initialization with K8s Health Check&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted</p><h2 id="date" tabindex="-1">Date <a class="header-anchor" href="#date" aria-label="Permalink to &quot;Date&quot;">​</a></h2><p>2026-05-29</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>When deploying to a new Kubernetes cluster, the MCP server would start up, bind to the HTTP port, and report &quot;listening&quot; — but the first request would fail silently because the Kubernetes API was unreachable (wrong service account, missing RBAC, network policy, etc.).</p><p>The root cause was the <strong>lazy initialization</strong> pattern in <code>startHttpServer()</code>:</p><ol><li><code>initK8sContext()</code> only loads the kubeconfig (no network call).</li><li>The Fastify server binds to the port immediately.</li><li><code>createMcpServer()</code> is deferred to the first HTTP request inside <code>getMcpServerAndTransport()</code>.</li><li><code>evaluatePermissions()</code> — which actually calls the K8s API — only runs inside <code>createMcpServer()</code>.</li></ol><p>This created two failure modes:</p><ul><li><strong>Silent startup</strong>: The server reports healthy but every request fails.</li><li><strong>CrashLoopBackOff loops</strong>: Kubernetes restarts the pod because health probes fail on first request, so operators see the init logs repeating without reaching the &quot;listening&quot; log.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Move to <strong>eager initialization</strong> at startup:</p><ol><li><strong>K8s API health check</strong>: Before creating the MCP server, probe the Kubernetes API with a single <code>listNamespacedPod(limit: 1)</code> call. If it fails, throw immediately with an actionable error message.</li><li><strong>Eager MCP server creation</strong>: In non-stateless mode, <code>createMcpServer()</code> is called during <code>startHttpServer()</code>, before binding the port. This validates RBAC permissions upfront.</li><li><strong>Actionable error diagnostics</strong>: The <code>main().catch()</code> handler in <code>server-entry.ts</code> now logs structured error messages with HINT diagnostics for common failure modes (ECONNREFUSED, Unauthorized).</li></ol><p>The startup sequence is now:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>startHttpServer()</span></span>
<span class="line"><span>  → Validate K8s API connectivity (listPods probe)</span></span>
<span class="line"><span>  → Create MCP server (evaluatePermissions)</span></span>
<span class="line"><span>  → Register UI app</span></span>
<span class="line"><span>  → Bind Fastify to port</span></span>
<span class="line"><span>  → Log &quot;listening&quot;</span></span></code></pre></div><p>If any step fails, the process exits with a clear error before binding the port.</p><h3 id="stateless-mode-exception" tabindex="-1">Stateless mode exception <a class="header-anchor" href="#stateless-mode-exception" aria-label="Permalink to &quot;Stateless mode exception&quot;">​</a></h3><p>In <code>STATELESS=true</code> mode, a new MCP server is created per-request by design, so eager creation is skipped. The K8s health check still runs.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="keep-lazy-initialization-with-a-separate-readiness-probe" tabindex="-1">Keep lazy initialization with a separate readiness probe <a class="header-anchor" href="#keep-lazy-initialization-with-a-separate-readiness-probe" aria-label="Permalink to &quot;Keep lazy initialization with a separate readiness probe&quot;">​</a></h3><ul><li>Pros: No startup delay; K8s readiness probe handles the problem.</li><li>Cons: Requires a custom <code>/healthz</code> endpoint; the actual failure reason is hidden from logs; operators still have to correlate probe failures with K8s API issues.</li><li><strong>Rejected</strong>: Adds complexity without solving the observability problem.</li></ul><h3 id="retry-loop-with-backoff-at-startup" tabindex="-1">Retry loop with backoff at startup <a class="header-anchor" href="#retry-loop-with-backoff-at-startup" aria-label="Permalink to &quot;Retry loop with backoff at startup&quot;">​</a></h3><ul><li>Pros: Handles transient API server unavailability (e.g., during cluster bootstrap).</li><li>Cons: Masks permanent failures; delays pod startup; K8s already handles restarts via CrashLoopBackOff.</li><li><strong>Rejected</strong>: Fail-fast is better for declarative infrastructure. K8s restartPolicy provides the retry mechanism.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The server <strong>will not start</strong> if the K8s API is unreachable. This is intentional — it&#39;s better to fail visibly than to start and silently break on every request.</li><li>Startup time increases slightly (one API call + permission evaluation). This is negligible (~100ms on healthy clusters).</li><li><code>getMcpServerAndTransport()</code> still creates new servers in stateless mode and for test DI — the fallback path is preserved.</li><li>Error messages now include actionable hints (&quot;Check KUBERNETES_SERVICE_HOST, service account mount, and network policies&quot;).</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("decisions/ADR-009-eager-startup-health-check.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ADR009EagerStartupHealthCheck = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ADR009EagerStartupHealthCheck as default
};
