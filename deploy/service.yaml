apiVersion: v1
kind: Service
metadata:
  name: no-crd-mcp
  namespace: nogoo9
  labels:
    app.kubernetes.io/name: no-crd
    app.kubernetes.io/part-of: nogoo9
spec:
  selector:
    app: nogoo9-mcp
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
